# 여정 인덱스 — 블로그 원고용 (2026-09-08 ~ 09-16)

> 목적: 이 기간의 트러블슈팅·결정을 **에피소드 단위**로 묶고, 각 에피소드가 어느 문서·수치·실수·교훈으로 뒷받침되는지 한눈에 보이게 한다. 블로그(회고록, [N일차], ~5천자, 3편 리듬)로 옮길 때 이 표의 "근거" 열을 그대로 인용한다. 모든 수치는 해당 문서에 측정 조건(UTC/KST, 창, 환경)과 함께 있다.

## 에피소드 목록

| # | 에피소드 | 한 줄 | 핵심 수치 | 실수·오판(솔직히) | 근거 문서 |
|---|---|---|---|---|---|
| 1 | 호가 확장 사전 검증 | 코드로 답할 수 있는 건 코드로, 나머지는 실측(WS 한도·287마켓·압축·시간대) | 5연결/s 429, 287마켓 단일 커넥션 누락 0, orderbook.15 153.6→261.7 msg/s(시간대), .5/.15/.30 = 432/1,051/1,998B | 야간 샘플을 세션 시각 때문에 못 함 → cron으로 보완 | `~/cdc-orderbook-probe/REPORT.md`, docs/09 |
| 2 | 8월 "건수 반토막" — 유실인가 지연인가 | 거래소 일봉과 두 기준(적재시각/체결시각)으로 대조 → 유실 0, 최대 36.9h 적재 지연 | 큐 최대 1,082,811행, 배수 처리량 7.95 rows/s(CV 0.047 = 포화), 이론 상한 10 rows/s | producer 재생성으로 8월 로그 소실 → ClickHouse로 백로그 재구성 | docs/08 |
| 3 | Flink 상태 596MB의 정체 | 누수가 아니라 RocksDB MANIFEST 비대. canonical savepoint는 20KB | 체크포인트 625MB → 17.8KB, e2e 1,388ms → 51ms, task heap 25.6MiB → 692MiB | — | docs/09 작업5, docs/10 |
| 4 | mem_limit로 n8n 24분 크래시 | 스왑 상태 RSS 기준 1.5배 규칙 → V8 힙 상한 312MB에 걸림 | exit 134 ×57회, 알림 24분 중단 | 규칙의 전제(RSS = 워킹셋) 오류. 사용자 cron이 편집 중 compose를 먼저 적용 | docs/09 작업4, README 이슈6 |
| 5 | ClickHouse system 로그 15.9GiB와 뮤테이션 OOM | MODIFY TTL이 유발한 MATERIALIZE TTL 뮤테이션이 1.13GiB 한도 초과·재시도 → KILL, DROP PARTITION으로 대체 | 15.9 → 2.4GiB | 메모리 한도 있는 서버에서 대용량 뮤테이션 착수 | docs/09 작업4 |
| 6 | n8n 이중 실행 | 미니PC와 Oracle 양쪽에서 같은 알림 워크플로우가 매분 실행 | 30회/30분 양쪽 동일, 3ms 차 | 6월 이관 때 "stop만"이 지켜지지 않음 | worklog 09-09 04:39 |
| 7 | 호가 경로 설계와 용량의 실측 | MySQL/Debezium 미경유 결정, 압축이 추정보다 훨씬 좋음 | 39.7 B/스냅샷(추정 4.3배 → 실측 14.2배), Kafka zstd 449 B/msg, 원본 7일 5.5GB | 사전 추정이 4배 보수적 | docs/11, REPORT §4-B |
| 8 | 전 코인 확장과 삭제 부산물 | 정리 EVENT 상향, tombstone 비활성, dry-run 1h | 체결 토픽 84%가 delete+tombstone, 25.5~31 rows/s, 알림 20건/h | — | worklog 09-09 04:35~05:57 |
| 9 | **유실 사고 #2: sequential_id 마켓 간 충돌** | 원장 대조 94.7% → "업비트 문제"로 오판 → 독립 수신기 실험으로 철회 → 스키마 원인 → 무중단 수정 → 7일 백필 → 100% | 287마켓 2.94%(BTC 10.3%), 5코인 6.71%, 백필 210,469행, 재대조 100.0% | 오판 2건(duplicates 정상 기록, 소스 갭 결론), 백필 창 하한 실수로 중복 53,012 → 삭제 | docs/13, README 이슈7 |
| 10 | 7일 무변경 관찰 | 가설·임계값 선고정, 5분 87지표 + 일일 요약 + 일일 원장 대조 | 관찰 결과는 docs/14 | 시각 대기 루프 자정 버그로 잡 2개 조기 실행 | docs/12, docs/14 |
| 11 | 재생 실험 도구 | 녹화·재생·측정기, dry-run 검증, 코퍼스 고정 | 계단 페이싱 50/100/200 정확, 429 충돌 경험 | REST 작업 동시 실행 → 429 | `~/pipeline-load-lab` |
| 12 | 결정의 기록 | CDC 정체성(인위적 소스 인정), 브로커 3→실험→1, KRaft는 실험 뒤, 저장 계층(hot→cold), CDC 유의미화 A+B | 결정 표 31건, 설정값 근거 표 29행 | — | worklog 결정 표 |

## 블로그 3편 제안 (리듬 유지)

1. **"유실인 줄 알았던 지연" (에피 2 + 3)** — 거래소 시각과 적재 시각을 나눠 보는 순간 사고의 정체가 바뀐 이야기. Flink 상태 625MB의 정체까지 "지표가 보는 곳이 잘못됐다"는 한 주제.
2. **"3%가 조용히 사라지고 있었다" (에피 9)** — 원장 대조 → 오판 → 독립 수신기 → 스키마 → 백필. 틀린 결론을 실험으로 뒤집은 과정과 백필 실수까지 그대로.
3. **"7일 동안 아무것도 바꾸지 않기" (에피 7·8·10·12)** — 실측으로 용량표를 갈아엎고, 가설을 먼저 적고 관찰하고, 결정을 근거와 함께 남긴 이야기. 실무 관행과의 차이(단일 호스트, 인위적 CDC 소스)를 인정하는 톤.

## 블로그 5~8편 제안 (2026-09-17 추가, 에피 13~18)

기존 초안: 확장기 1~4편(에피 1~12) + 중복 감사. 오늘까지의 에피 13~18 은 4편 분량(각 4~5천자, 회고록·존댓말·[N일차]).

| 편 | 제목(안) | 에피 | 뼈대 | 핵심 숫자·표 (출처) |
|---|---|---|---|---|
| 5 | "기준을 정답에서 역산하다" | 16 + 18(후반) | 규칙 3개에 근거가 없음을 인정 → "이력 없음" 단정 → 번들 추적으로 6개월 15,588건 → ±50/100/200% 역산 → 체결 단위로 구현했더니 86번 울림 → 분 종가로 바꾸자 거래소와 시각까지 일치 | p50 51.7/101.2/196.9%, 3,220/3,221, LSK 86→9, 18:49=18:49 (docs/16, docs/22 §4-3) |
| 6 | "100%였는데 틀렸다, 백필이 오염시켰다" | 14 + 15 | 7일 100% 대조 → DAG 첫 실행에서 유실 2건(분모) → BFC 6일·재연결 5초 → 백필 160행이 오탐 10건 → 늦은 이벤트 가드 | 261,713행, 16.06%, 오탐 반반, 5.87%/4.8s (docs/17, docs/20) |
| 7 | "여유 85배라는 문장을 만들기까지" | 17 | 왜 재생인가 → 50~200/s 무변화 → Kafka 19k/65k → 10k/s 정체 → 병목이 예상과 달랐다 → 배치 한 줄 → 측정 도구가 실험을 흔든 사건 3개 | 209,999=209,999, 26.5→0.28s, 147% 의 정체, lag 톱니 (docs/23 §1~5) |
| 8 | "무손실은 어느 경로의 이야기였나" | 18 | 브로커 1대 정지 무손실 → 사용자 질문 "호가가 되돌릴 수 없는 데이터 아닌가" → 전체 정지 3분: 체결 3,307=3,307, 호가 35,838 전부 유실 → 두 번째 원인(메타데이터 블립) → 60초 재실험 유실 0, 그런데 Connect 유령 상태 → 3→1 축소와 "Kafka 는 원본인가" | 35,838, 1,413=1,413, 중복 27, 105/105 RF1 (docs/23 §7, docs/24) |

리듬: 5·6·7 을 한 묶음(기준 → 검증 → 부하), 8 은 단독으로 무겁게. 각 편 끝에 "실무에선 흔한 지점인가" 표(docs/23 §5-3)를 인용.

## 인용 시 주의

- 수치 옆에 항상 조건(시간대 KST/UTC, 창 길이, 287마켓/5코인, 어떤 실측 명령)을 붙인다. 모든 수치의 조건은 해당 문서 표에 있다.
- 오판·실수는 지우지 않는다(에피 4·5·9·10·11). 정정 과정이 이야기의 본체다.
- "실무 관행"으로 쓴 부분(저장 계층, DELETE 대신 파티션 만료, CDC 용도)은 일반 지식이지 이 세션 실측이 아니다 — 문장에서 구분한다.

## 13. 기준의 근거를 찾다 (2026-09-16)
- 이상탐지 3규칙의 근거 부재 인정 → 업비트 감시정책 원문(계정 단위, 재현 불가) → 시장경보제도 재현으로 전환 → "이력 없음" 단정 → 사용자 재확인 요구 → 웹 번들에서 `crix/market-event-records` 발견(6개월 15,588건) → 백테스트로 ±50%/±100%, 7일 평균 ×4 도출. 문서: docs/16, worklog 09-16. 블로그 5편 후보.

## 14. 100%였는데 틀렸다 (2026-09-16)
- 관찰 7일 내내 원장 대조 100.0% → DAG 로 옮긴 첫 실행에서 유실 2건. 원인은 임계값이 아니라 **분모**(비교 마켓 목록을 우리 DB 에서 만들었음).
- KRW-BFC 상장 후 6일 무수집 261,713건 + 재연결 5초가 45초 창의 16.06%. 둘 다 복구.
- 문서: docs/17, docs/18 §1-1. 블로그 6편 후보 — "지표가 통과하는데 데이터가 틀린 경우".

## 15. 백필이 스트림을 오염시킨다 (2026-09-16)
- 백필 160행 → 알림 10건이 정확히 반반(옛 체결 vs 현재가 / 실시간 체결 vs 백필된 옛 가격). Flink keyed state 가 덮인 것.
- 한 번의 백필이 두 번 오탐. 09-10 의 1,687건도 같은 기전이었음을 사후 특정.
- gap-fill 자동화 설계를 바꾼 발견. 문서: docs/17 §4-4, docs/18 §1-2. 블로그 7편 후보.

## 16. 기준을 정답에서 역산하다 (2026-09-16)
- 이상탐지 3규칙에 근거 없음을 인정 → 업비트 시장경보 재현으로 전환 → "이력 없음" 단정 → 웹 번들 추적으로 6개월 15,588건 발견 → ±50/±100/±200%, 7일 평균 ×4 도출 → 자기 감사(표본 독립성·1분봉 독립 검증·라벨 정렬 민감도).
- 문서: docs/16, docs/16-appendix, docs/18 §1-3·§2. 블로그 5편 후보.

## 17. 임계점을 찾다 (2026-09-17)
- 녹화-재생 부하 실험 5시나리오: 50~200/s 무변화 → Kafka 단독 19k/65k msg/s → 브로커 1대 5분 정지 무손실(MySQL 12,849 = ClickHouse 12,849) → 2,000/s 무변화 → 5,000/s 실시간, **10,000/s 에서 정체**(e2e p95 26.5s).
- 병목은 예상(ClickHouse)과 달리 **소스 체인에 묶인 동기 JDBC 싱크**: 서브태스크 busy ≈ inserts/s × insert 지연, 파티션 3개/서브태스크 2개 편중. 배치 200→1,000 한 줄로 정체 5→0, e2e p95 26.5→0.28s.
- 측정 도구가 실험을 흔든 사건 3건: lag 톱니(체크포인트 커밋), docker-exec CLI JVM 이 브로커 CPU 147% 로 보임(브로커는 23%), pgrep/pkill 자기 셸. "0 이면 안 됐다"·"지연이 낮으면 여유"·"백프레셔 0 이면 여유" 셋 다 틀린 직관.
- 동거 부하 분리: 정체가 "언제" 터지는지는 MySQL 정리 이벤트(10분마다 DELETE 4만)가 정했다. 타 프로젝트 영향 없음.
- 문서: docs/15 §7, docs/23. 블로그 8편 후보 — "여유 85배라는 문장을 만들기까지".

## 18. 되돌릴 수 없는 데이터는 어느 쪽인가 (2026-09-17)
- 브로커 3→1 축소를 묻다가 사용자 통찰: "호가가 원래 DE 가 기대하는 되돌릴 수 없는 데이터 아닌가". 맞았다 — 체결은 MySQL 원장 + 거래소 REST 이중 안전망, 호가는 둘 다 없음. 그런데 호가 경로가 더 얇았다.
- 브로커 전체 3분 정지 실측: 체결 3,307 = 3,307 무손실(Connect 는 FAILED 조차 안 됨), 호가 **35,838건 전부 유실**. 유실은 예상한 타임아웃(1.45만)보다 **브로커 재기동 순간의 메타데이터 블립**(librdkafka "partition 6→0" → 큐 2.1만 폐기)이 더 컸다. 1브로커면 재시작마다 오는 사건.
- 실측 전에 유실 창 기록 모델(`dq_orderbook_gaps_daily`)을 만들어 두어, 이 정지가 "06:38:50 +216s·추정 38,664" 로 자동 기록됐다(실제 35,838). 30일 전수에서 09-12 의 6초 유실도 처음 드러남.
- 같은 날 PRICE_24H 도 "체결 단위 판정 → 분 종가" 로 바꿨다: 동등성 재계산이 LSK 하루 86회 플래핑을 드러냈고, 분 종가로 바꾸자 거래소 지정과 건수·시각(18:49/01:47/22:21)이 정확히 일치. 기준의 이유 셋(정답 해상도·임계 근거·참조 대칭)을 docs/22 §2 에.
- 문서: docs/22 §4, docs/23 §5-3·§7. 블로그 9편 후보 — "무손실은 어느 경로의 이야기였나".

## 19. 브로커 셋은 무엇을 주고 있었나 (2026-09-17~18)
- "3브로커가 의미 있었나" → 장애 처리 경로 확인 1회(리더 이동·ISR·회복 15초) 뒤엔 형식. 전체 정지 3분 실측(체결 무손실·호가 35,838 유실) → 무정지 재할당 3→1, 24h 대조 100%, 재기동 14초·유실 0·중복 239.
- 문서: docs/24. 실수: 측정 CLI JVM 이 브로커 CPU 로 보임(docs/23).

## 20. 중복을 저장 층에서 지우다 (2026-09-18)
- 재전송 중복 27+239 실측 → ReplacingMergeTree, 24M 행 INSERT 가 메모리 한도 초과 → 일 슬라이스, MV DETACH→차이분→EXCHANGE→잔여→ATTACH 무정지 12분.
- 문서: docs/25.

## 21. 삭제 이벤트 46% 와 응급 처치 (2026-09-18)
- 24h 토픽 4.1M 중 46% 가 삭제 이벤트(Flink 가 버림). 정리 DELETE 59s→1s, 커넥터 skipped.operations=d, binlog 30일. "거울 vs 보관소"로 CDC 모드를 처음 구분.
- 문서: docs/26.

## 22. 되튐의 원인은 스프레드가 아니었다 (2026-09-18)
- 체결×호가 분 마트(10일 3.78M 행) → EURC 되튐은 스프레드가 아니라 체결/깊이(≥300bp 분 중앙값 1.0). dim_markets 로 상장일·커버리지 공백(BFC 6일)이 숫자로.
- 문서: docs/27, docs/26 §4.

## 23. 첫 동등성 판정 — 종가의 정의 (2026-09-19)
- 70/68/66, 미매칭 6 = 경계 1분 뒤집힘 3쌍. 참조 동일·늦은 도착 0 → 원인은 "도착 순 마지막"이 종가였던 것. 이벤트 순(sequential_id)으로 v2.1.1, 하네스 테스트 2개 추가.
- 문서: docs/22 §4-4. 실수: 빌드 확인 전 커밋 2건.

## 24. 시각이 다섯 개인데 키엔 하나도 없었다 (2026-09-19)
- 2층 설계 검토에서 "실무는 시각이 핵심" → 시각 5개 표(거래소·수신·적재·binlog·Flink). 체결 시각 일 파티션·(trade_id, upbit_timestamp) PK·UNIQUE(market, sequential_id, upbit_timestamp)·새 컬럼 3. 드라이런에서 파티션 경계 5일 오차 발견. MySQL RENAME 스왑(06:32:51, 차이분 18초), ClickHouse EXCHANGE(114M 행 9분 복사, 218일 uniq 대조 0, 1시간 조회 1.0→0.10s), 시각 6개 첫 실측(82ms/1.1s/1.7s).
- 문서: docs/28 A-0~A-8. 실수: 테스트 프로시저를 복사 중인 표에 걸어 파티션 삭제, verify 규칙 두 번 정정, 월별 uniqExact 메모리 초과.

## 25. Kafka 를 쓰고는 있었나 (2026-09-19)
- "메시지 설계가 적절한가, 냉정하게" → 브로커는 문제 아님, 키 trade_id(재정렬 5.87%)·무압축·바이트 보존·삭제 반영·DLQ 없음. 창1(키=market·zstd·7일·DLQ 토픽) → 창2(파서 DLQ+카운터, 새 컬럼 통과, 5분 처리 시간 집계 폐기, uid 명시). 보안은 "비용"이 아니라 위협 모델로 재판단 → LAN 노출 실측.
- 사용자 질문 "분산의 정수에 맞게 쓰나 / 없이도 됐나 / 끝까지 같나" → 분산 성질은 안 쓰고 로그 성질을 쓴다. 리밸런스 실측 제안은 Flink 정적 할당이라 철회.
- 문서: docs/29. 실수: docker exec -i, 메트릭 id 공백→_.

## 26. "정말 못 받아오나" — 전수 조사 (2026-09-19)
- 사용자: "전에 네 말만 믿었다가 아니었던 적이 많다." 문서+실제 소켓 구독 두 갈래. Upbit: 체결 취소·정정 없음(대조 68,088셀 우리>거래소 0)이나 candle 갱신·ticker market_state·announcement CREATED/UPDATED·myOrder 6상태 있음. Binance: depth diff 6초 1,632 갱신 중 762 삭제, amend keepPriority(in-place UPDATE), Spot Testnet·Demo Mode 로 실돈 없이 주문 생애주기.
- 세 번째 "부재 단정" 사례. 문서: docs/28 B-0~B-0-3.

## 27. CDC 가 처음으로 변경을 캡처하다 (2026-09-19)
- 원장 4테이블(거울/보관소) → 두 번째 커넥터(삭제 유지) → Kafka 엔진 MV → RMT(version, is_deleted). 키 없는 c/u/d 스모크 → 키 등록 → 주문 71→340, 전이 5종, 3자 대조 불일치 0, 거래소→Kafka 219ms. 케이스(거울 두 번째)·경보 SCD·교차 거래소 신호(같은 코인 단위만).
- 문서: docs/28 B-1~C-3. 실수 7건(clientOrderId 규칙, REST 서명 순서, dbt 별칭, _timestamp_ms 초 잘림, docker cp 경로, NOTIONAL, 폴링 필터) + 발견: ch_inserted_at 은 폴링 시작 시각.

## 블로그 9~12편 제안 (2026-09-20 추가, 에피 19~27)
| 편 | 제목(안) | 에피 | 뼈대 |
|---|---|---|---|
| 9 | "브로커 셋은 무엇을 주고 있었나" | 19·20·21 | 형식뿐인 복제를 실측으로 확인하고 줄이기, 저장 층 중복 제거, 삭제 46% |
| 10 | "시각이 다섯 개인데 키엔 하나도 없었다" | 23·24 | 종가의 정의 → 시각 표 → 무정지 재설계 두 번 → 1.0s→0.10s |
| 11 | "Kafka 를 쓰고는 있었나" | 25 | 로그 vs 분산, DLQ, 철회한 제안, 위협 모델 |
| 12 | "정말 못 받아오나 — 그리고 CDC 가 처음 변경을 캡처한 날" | 26·27 | 전수 조사로 뒤집힌 판단, 거래소 엔진이 바꾼 상태를 3자 대조로 증명 |

## 28. 스택을 해체하고 볼륨을 채우다, 그리고 감시가 죽었다 (2026-09-20)
- "각 스택이 진짜 일하나" → 볼륨으로 일하는 건 호가 경로뿐, Kafka 상한의 1%. 분산은 안 살리고 파티션·병렬·슬롯 산수를 살린다 → Binance 493 심볼 체결(36→300~830/s) + 상위 10 호가장 재구성(키별 상태, gap 0). 첫 시간 대조 491 심볼 100.000%.
- 슬롯 산수 실수(병렬 3, 빈 슬롯 2 → 재시작 루프인데 2/3 는 적재됨), Kafka CPU 착시 세 번째 재현.
- 확장 당일 Airflow 스케줄러가 5분마다 OOM(체크 12개 병렬·640M) → 감시 공백 30분. 1G·병렬 8·두 물결.
- 운영 정리(kafka-ui·Prometheus 끔, MySQL 1.25G) → SLO 표 + 알럿 3층 + 주간 다이제스트("나에게·어디를·언제").
- 문서: docs/31·32·30. 블로그 13편 후보 — "감시가 먼저 죽는다: 확장 당일의 사고".
