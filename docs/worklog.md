# 작업 로그 (worklog) — CDC 파이프라인 호가 확장 · 유실/지연/중복 개선

> 목적: 모든 판단의 근거(실측 명령·시각·수치)를 시간순으로 남긴다. 시각은 UTC(호스트 시계) 기준, KST = +9h.
> 산출 문서: `docs/08-ingest-lag-incident.md`(적재 지연 사고), `docs/09-orderbook-phase1-execution.md`(1차 실행), `docs/10-phase2-flink-producer-upgrade.md`(2차 실행). 사전 검증 원문·프로브 스크립트·측정 JSON은 `~/cdc-orderbook-probe/`.

## 현재 상태 (최신화 2026-09-10 03:15 UTC / 12:15 KST)

- **7일 무변경 관찰 진행 중** (09-09 06:10 ~ 09-16 06:10 UTC, 태그 `obs-week1-start`). 예외 조치 1건 발생: 09-10 01:20 유니크 키 교체 + 백필(docs/13). 나머지 구조·설정 변경 없음.
- 운영: 체결 CDC 경로 287마켓(producer → MySQL → Debezium → Kafka → Flink → ClickHouse), 호가 직접 경로 287마켓, Flink 잡 3개(hashmap, TM 2g, 슬롯 4/4), ClickHouse 1.75G, health_check(잡 3·적재 지연 알림), n8n 알림은 Oracle만, 5분 지표 cron + 06:20 UTC 일일 요약.
- 데이터 상태: 체결 유실 재발 차단(01:20 이후 원장 대조 100%), 09-03 이후 7일치 복구 완료(210,469행), 09-03 이전 손실은 복구 불가(추정 5.3%). 중복 0(백필 실수분 53,012 삭제 완료). 백필 창(09-10 01:52~03:02) trade_aggregations 오염은 기록만.
- 관찰 뒤 순서(확정): (1) **reconcile_trades DAG** + dbt 유일성·커버리지·freshness 테스트 → (2) 관찰 분석(docs/14 예정)·튜닝(USDT LARGE_TRADE 임계, 파티션 키 market, Float64→Decimal, "하루" 정의 통일, 마켓 목록 갱신) → (3) 재생 증폭 실험(브로커 장애 시나리오) → (4) 브로커 3→1 + KRaft → (5) CDC 유의미화 A(가상 매매 원장)+B(이상탐지 케이스 관리), MySQL DROP PARTITION 전환, ReplacingMergeTree.
- 실험 도구: `~/pipeline-load-lab`(로컬 git) recorder/replayer/measure dry-run 검증 완료. 코퍼스는 관찰 종료 시 고정.
- 미커밋 잔여: dbt tests/assert_no_long_gaps.sql(이전 세션 diff), png 3개, 일일 요약 자동 추가분.
- 문서 인덱스: docs/07 중복 감사 · 08 적재 지연 사고 · 09 1차 · 10 2차(Flink) · 11 호가 · 12 관찰 계획 · **13 sequential_id 충돌 유실 사고** · worklog(결정 30건 + 시간순).

## 결정 사항 (확정, 재논의 불필요)

| 일시 | 결정 | 근거 |
|---|---|---|
| 09-09 00:10 UTC | 호가 수집 경로는 MySQL/Debezium 경유가 아닌 **수집기 → Kafka 직접 발행**(별도 토픽, retention 24h, RF2) | 호가는 체결의 14배 건수·48배 바이트(실측), Debezium 봉투 +34% |
| 09-09 00:10 | 호가 스펙 **count .15, 원본 TTL 7일 + 1분 파생지표 365일**(A안) | 사전 검증 §4 용량표 |
| 09-09 00:10 | 호가 착수 선행조건 = 디스크 정리 + Flink TM 산정·적용 | 1차·2차로 충족(02:16) |
| 09-09 00:10 | 부하 실험은 **녹화-재생 증폭**(실데이터 50→100→200건/s 계단), Faker 미사용 | 개선 전후 동일 입력 비교 |
| 09-09 00:10 | Spark 비교는 Oracle 임시 컨테이너(arm64, executor 1GB부터), 신규 인스턴스 금지 | A1 무료 한도 99.2% 실측 |
| 09-09 00:10 | 비CDC 컨테이너는 제거 대신 mem_limit 공존 | — |
| 09-09 00:10 | 서사 순서: 중복(완료) → 유실(완료: 지연으로 판정) → 지연(재생 실험) | JD "지연·유실·중복 직접 개선" |
| 09-09 01:20 | 2차 = Flink 상향+hashmap, best bid/ask Flink 통과, producer 상한 제거를 한 재제출 창에 묶음 | docs/09 결정 1·2·3 |
| 09-09 02:30 | **confluent-kafka** 의존성 승인(호가 수집기) | librdkafka, idempotent·zstd, 호스트에 2.13.2 존재 |
| 09-09 02:30 | **3브로커 유지**(단일 호스트, HA 아님을 문서화). 재생 실험에 브로커 장애 시나리오 포함해 비용 정당화 | 복제·ISR·페일오버 실측 가능 |
| 09-09 02:30 | **KRaft 전환은 재생 증폭 실험 뒤** 별도 유지보수 창(전후 실측 문서화) | 실험 기준선 보존, Kafka 3.6 마이그레이션 지원 |
| 09-09 04:05 | **브로커 3 → 실험 → 1**: 재생 증폭 실험까지 3브로커 유지(브로커 장애 시나리오 1회 실측·문서화) → KRaft 전환 창에서 **1브로커 combined 모드**로 축소(RF3 토픽 파티션 재배치 포함) | 단일 호스트라 HA 이득 없음. 실측 비용: 브로커 3개 RAM ~1.9GB+ZK 176MiB, 체결 RF3 쓰기 2GB/일, 호가 RF2 ~20GB/일, swap 4~6GB 사용 중 |
| 09-09 04:20 | ClickHouse 컨테이너 메모리 1.25G → 1.75G, JDBC 배치(500/2s) 유지, 호가 마트·패널은 실험 뒤 | 호가 인서트 후 ClickHouse 1.087/1.25GiB(87%) 실측. 재시작은 3잡 savepoint 정지로 유실·중복 0 |
| 09-09 04:28 | **7일 무변경 관찰** → 분석·튜닝 → 재생 증폭 실험 순. 관찰 지표는 cron 스크립트로 수집(Airflow DAG 대신) | 10분·30분 확인은 스모크 테스트일 뿐. 피크(KST 09/22~24시)·주말 사이클은 1주 필요. 재생 실험은 안정 기준선 필요 |
| 09-09 04:30 | 관찰 중 병행 작업: (1) cron 지표, (2) health_check 적재 지연 알림, (3) 재생 도구 준비(녹화기·재생기, 덤프 1회), (5) 포트폴리오 문서. 금지: RMT 전환·2월 백필, KRaft, 마트 재집계, 재생 본실행 | 프로덕션 부하를 주는 작업은 관찰 오염 |
| 09-09 04:32 | 별도 GitHub 리포: 시스템(compose·수집기·Flink·DDL·Airflow·docs)은 현 리포 유지, 실험 도구·관찰 데이터·분석·Spark 비교만 새 리포(권고, 미확정) | 배포 단위 기준 분리, 서사 단절 방지 |
| 09-09 04:33 | **전 코인(287마켓) 체결 확장을 7일 관찰 전에 실행**. 선행: MySQL EVENT 상향(승인), Kafka retention.bytes 상향(승인), producer 전 마켓 옵션, 1시간 dry-run(알림 폭주·buffer 확인) | 관찰이 "전 코인 규모에서 producer 개선이 버티는가"까지 검증. 피크 실측 53.6 msg/s vs 새 상한 2,500 rows/s |
| 09-09 04:34 | 관찰 전 정비 4건: health_check expected_jobs 2→3, Flink 재시작 전략 3×10s→20×30s(잡 코드, 다음 재제출), n8n 이중 실행 확인(→ 이중 확인됨, 처리 결정 대기), Debezium tombstones.on.delete=false(제안, 승인 대기) | ClickHouse 재시작 실측 24초 > 재시작 창 30초 아슬. delete+tombstone이 체결 토픽 트래픽 84% |
| 09-09 05:05 | Debezium 삭제 처리 순서: **지금 tombstones.on.delete=false만**(dry-run 후 적용) → 7일 관찰 중 "10분 40K 삭제 버스트" 비용 실측 → 관찰 뒤 MySQL 일 단위 파티션 + DROP PARTITION으로 청소 방식 전환(행 단위 삭제 이벤트 자체를 없앰). delete 필터(skipped.operations=d)는 보류 | 2월 46h 장애 = 50K 일괄 DELETE → delete+tombstone 10만 건 → 파서 NPE(docs/06). 체결 토픽 메시지 84%가 delete+tombstone(실측). 토픽은 compact가 아니라 tombstone 무용 |
| 09-09 05:10 | **CDC 정체성 정리**: 체결→MySQL→CDC 경로는 업무 필요가 아니라 CDC 운영 경험용(README 명시). 실무라면 체결은 호가처럼 직접 발행이 정답. 면접 프레임: "CDC 운영을 끝까지 겪고 비용(봉투 +34%, 쓰레기 84%, 46h 장애)을 실측했기에 호가는 직접 발행으로 갔다". 구조는 지금 안 바꿈 | 관찰 계획·지연 사고 서사 보존. "실무 관행과 같다"는 방향만 같고 구현은 단일 호스트 타협본임을 문서에 구분 표기 |
| 09-09 05:20 | **관찰 뒤 CDC 유의미화 방향(제안, 관찰 뒤 확정)**: A) 가상 매매 서비스 원장(orders/positions/balances, 체결 판정은 실시간 호가·체결 기준, "가상 주문" 명시) = 규모·원장 CDC·상태 upsert·스트림-상태 조인·실시간 손익. B) 이상탐지 케이스 관리 테이블(open→확인/오탐/종료를 규칙으로 갱신) → CDC → ClickHouse 정밀도 이력 → 임계값 재조정 순환. C) Airflow/n8n 메타 DB CDC는 보너스. **실주문(myOrder) 혼합은 제외** | 업비트 공개 API에 타인 주문·취소·청산 데이터 없음(문서 확인: 공개 WS ticker/trade/orderbook/candle, 인증 WS myOrder/myAsset 본인 한정, 선물·청산 없음). 실주문 반복은 허수성·취소 과다 감시 대상, 돈·키 관리 위험. 순서: 관찰 → 재생 실험 → A+B |
| 09-09 05:30 | **호가 데이터의 성격과 활용 근거**: 업비트 호가는 주문 단위(L3)가 아니라 가격대별 잔량 합계 스냅샷(L2). 취소는 잔량 감소로 반영되며 "누구의 주문"만 없음. 체결 스트림과 대조하면 "체결 없이 사라진 잔량 = 취소·정정"으로 추론 가능 → 2월(docs/06)에 "호가 없어 불가"로 남긴 감시 유형 2(허수성 매매)·3(취소·정정 과다) 탐지를 확장 항목으로 등록. 원본 7일 보관 이유 = 이 추론 계산의 입력 | 실측: 15단 전체 스냅샷 258 msg/s, 스냅샷 간 diff로 이벤트 복원 가능. L2는 스프레드·깊이·불균형 분석의 업계 표준 형태 |
| 09-09 05:30 | **실무자 관점 자체 평가(면접 대비)** — 강점: 200일+ 실운영, 사고 3건(46h 중단·37h 적재 지연·24분 알림 중단)의 원인·수치 문서화, 결정마다 실측 근거, 지연·유실·중복 각각 해결, 규모 하루 약 2,600만 이벤트(체결 400만+호가 2,200만, 초당 ~300). 약점(인정하고 답할 것): 단일 호스트 HA 아님(→3→1 결정), 체결 CDC 소스 인위적(→A+B), 싱크 at-least-once·멱등 upsert 미적용(→RMT 관찰 뒤), 스키마 계약·CI·IaC·SLO 없음, Iceberg 미구현. "왜 아직 안 했나" 답: 먼저 측정(중복 감사·7일 관찰)하고 게이트를 세운 뒤 전환하는 순서 | 약점을 먼저 말할 수 있는 것이 신뢰 요소 |
| 09-10 01:19 | **관찰 예외 조치**: MySQL 유니크 키 `(market, sequential_id)`로 교체(무중단) + REST 7일 원장 백필 210,469행(MySQL 경유 CDC) | 진행 중 유실(287마켓 2.94%, BTC 10%). 독립 수신기 실험으로 producer 원인 확정(docs/13) |
| 09-10 03:10 | **관찰 뒤 1순위 = `reconcile_trades` Airflow DAG**: 시간 파티션·catchup·멱등, 시간봉 대조 → 99% 미만 셀만 REST 원장 조회 → 누락 INSERT(MySQL 경유) → 재검증 → 품질 테이블 적재 → Slack 요약. REST pool 페이싱 ≤7/s, SLA "전날분 다음 날 정오까지". 이어서 dbt `unique_combination_of_columns(market, sequential_id)`·커버리지 테스트·source freshness(upbit_timestamp) | 원장 보존 7일 창 안에 자동 복구가 돌아야 유실이 영구화되지 않음. 오늘 수동 절차를 그대로 태스크화 |
| 09-11 12:40 | **백업 설계(제안, 관찰 뒤 확정)**: Oracle Always Free 블록 볼륨 150GB 추가(무료 한도 200GB 중 부트 47GB 사용) → ClickHouse 네이티브 증분 BACKUP 매일 04~05시 KST, WireGuard 경유. 영구: crypto_trades·orderbook_1m·anomaly_alerts·trade_aggregations, orderbook_raw 14일. MySQL(7일 버퍼)·Kafka 제외. 복구 리허설 1회 필수 | 현재 백업 0. 압축 후 총 약 10GB, 영구 보존분 증가 0.16GB/일(연 60GB 내외). "ClickHouse 압축·TTL"은 저장소 관리이지 백업이 아님. 같은 NVMe/같은 집은 장애 도메인 동일 |
| 09-15 23:50 | **관찰 뒤 저장 계층 결정 3건(확정)**: (1) Oracle Always Free 블록 볼륨 150GB 추가, (2) ClickHouse 영구 테이블(crypto_trades·orderbook_1m·anomaly_alerts·trade_aggregations) 증분 BACKUP + `orderbook_raw` 일 파티션을 Parquet로 내보내 같은 볼륨에 6개월 롤링 아카이브(삭제 대신 이동), (3) `orderbook_raw`에 `ttl_only_drop_parts=1`(파트 통째 삭제, 재작성 없음) — 09-16 첫 TTL 머지 실측 후 적용 | 실무 관행 = hot(빠른 디스크, 며칠) → cold(객체 저장소, Parquet, 사실상 영구). 무료 자원 한도(Oracle 150GB, 원본 0.8GB/일 → 약 6개월)로 롤링. 7일 TTL은 원본 삭제가 아니라 계층 이동으로 재해석. 백업 설계(09-11 제안)와 같은 볼륨으로 통합 |
| 09-16 06:40 | (3)번 `ttl_only_drop_parts=1` **철회**: 첫 TTL 머지 실측이 10,079,767행 재작성 12.8초·피크 65MiB로 비용 미미. 기본 설정 유지 | docs/14 H12 |
| 09-16 00:45 | **부하 실험 3계층 확정**: ① Upbit 자체 코퍼스 재생(50→100→200/s, 행동 충실도·전후 비교 기준) ② **Binance 공개 데이터 코퍼스 재생**(500→2,000/s, 규모) ③ `kafka-producer-perf-test`(브로커 단독 상한, 병목 분리). Binance 데이터는 load_test 전용, 분석 테이블 미혼입 | 실측: data.binance.vision은 정적 파일(S3/CloudFront, 서울 엣지 ICN80), API 키·rate limit 없음(문서·응답 헤더 모두 없음), MIT. BTCUSDT 일별 trades zip 24MB(09-14), 월별 660MB(08월), SHA256 CHECKSUM 제공. 컬럼 trade_id/price/qty/quote_qty/timestamp(µs)/isBuyerMaker/isBestMatch → 우리 이벤트 형식 어댑터 필요(ask_bid=isBuyerMaker 매핑, sequential_id=trade_id). 준비생 포트폴리오 대부분이 Faker·10만 건 수준이라 실트래픽 재생+포화 실측은 차별점 |
| 09-09 04:40 | n8n 이중 실행 해소: **미니PC 워크플로우만 비활성화**(컨테이너·자동 업데이트 cron 유지) | 실행 이력 대조로 이중 실행 실증. 워크플로우 비활성은 롤백이 가장 쉬움. Oracle이 6월 컷오버본 |
| 09-09 06:09 | 재생 실험 도구는 별도 리포(`pipeline-load-lab`, 로컬), 재생기는 `load_test.` 외 토픽 거부 | 프로덕션 토픽 오발행 방지 안전장치. 관찰 중엔 읽기·dry-run만 |
| 09-09 06:20 | README에 단일 호스트 HA 아님·체결 CDC 인위성·타협점을 명시 | 면접에서 약점을 먼저 말하는 것이 신뢰 요소(자체 평가 결정 참조) |
| 09-09 07:40 | 관찰 일일 요약을 cron(06:20 UTC)으로 자동화해 worklog에 자동 기록 | 세션이 없어도 docs/12 §2 "매일 확인"이 수행되도록 |

## 설정값 근거 표 ("왜 그 값인가")

| 설정 | 값 | 왜 | 근거(실측·문서) |
|---|---|---|---|
| Flink `state.backend` | hashmap (RocksDB에서 전환) | 키드 상태가 수십 KB인데 RocksDB 네이티브 풀 체크포인트가 MANIFEST(352MB)를 매번 복사 | TM 로컬 db 디렉터리 실측(SST 합 15KB, MANIFEST 352.7MB), canonical savepoint 20KB, 전환 후 17.8KB (docs/09 작업5, docs/10) |
| Flink TM `process.size` | 2g (1g→) | 1g에서 task heap 25.6MiB, Metaspace 230/256MB(90%) → 잡 추가 시 OOM 위험. 2g·managed 0.25에서 task heap 692M | Flink 1.18 메모리 모델 계산(`~/cdc-orderbook-probe/out/flink_memory_model.txt`) |
| Flink `jvm-metaspace.size` | 384m (256→) | 잡 3개 클래스로더. 전환 후 실측 81/384MB | REST 메트릭 |
| Flink `managed.fraction` | 0.25 (0.4→) | hashmap이라 RocksDB용 managed 메모리 불필요, heap으로 돌림 | 동일 |
| Flink 슬롯 | 4 (3→) | CDC 2 + circuit 1 + orderbook 1 | 슬롯 여유 0 실측(docs/09) |
| Flink 재시작 전략 | fixed-delay 20회×30초 (클러스터 기본 3×10s 대신, 잡 코드) | ClickHouse 재시작 실측 24초 > 3×10s 창이면 FAILED로 멈춤. 분 단위 장애를 넘기도록 | 04:23 ClickHouse 재시작 실측 |
| Flink Kafka 시작 오프셋 | committedOffsets(LATEST) (`latest()`→) | savepoint 없이 재시작하면 latest로 점프 → 유실 | 46h 장애 문서(docs/06 이슈2) |
| Flink checkpoint retention | RETAIN_ON_CANCELLATION | 취소 후 복구 가능성 확보(이전엔 삭제) | docs/09 작업5 |
| producer `BATCH_SIZE` | 100 (20→) | MySQL 왕복 횟수 감소. 100은 executemany 1회 부담이 작으면서 초당 수십 건 유입을 1~2회로 처리 | flush 단위테스트 5,000행 1.2ms |
| producer `MAX_BATCHES_PER_FLUSH` | 50 | 1회 flush 상한 5,000행 = 2초 창당 2,500 rows/s. 피크 실측 72.75 msg/s의 34배 여유, 수신 루프 응답성 보호 | docs/08 |
| producer `BUFFER_WARN_ROWS` | 5,000 | 정상 buffer 0~63(dry-run). 5,000이면 약 2분치 피크 적체 = 명백한 포화 신호 | dry-run 실측 |
| producer `MARKETS` | ALL_KRW (REST 조회) | 하드코딩 5개 → 상장 변화 자동 반영 | 사전 검증 287마켓 |
| MySQL EVENT | EVERY 10 MINUTE, LIMIT 40000 (매시 25K→) | 전 코인 유입 약 400만/일 = 시간당 16.7만 > 기존 정리 2.5만/시 → 무한 증가. 10분×40K = 시간당 24만(용량 1.4배). 1회 40K는 2월 사고(50K 일괄) 이하이며 파서 NPE는 수정됨 | dry-run: 2,634,793→2,563,267행 감소 확인, 예외 0 |
| Debezium `tombstones.on.delete` | false | 토픽이 compact가 아니라 tombstone 무용. 삭제 1건당 메시지 2→1 | 체결 토픽 84% delete+tombstone 실측; 적용 후 3,000건 중 tombstone 0 |
| Kafka trade `retention.bytes` | 4GB/파티션 (1GB→) | 전 코인 53 msg/s×658B ≈ 3GB/일, 72h 보존 유지 | 사전 검증 §4 |
| Kafka orderbook 토픽 | 6 파티션, RF2, 24h, 6GB/파티션, min.isr 1, segment 256MB | 파티션 6 = 슬롯 여유·향후 병렬도 2~3 대비. RF2 = 브로커 3에서 단일 장애 허용하되 RF3의 3배 쓰기 회피(호가는 재수집 가능한 스트림). 24h = A안(원본 7일은 ClickHouse가 담당). 6GB = 23GB/일 raw 추정의 파티션 몫 + 여유(zstd 실측 후 실제 ~3GB/일/파티션). min.isr 1 = RF2에서 브로커 1대 다운 시 쓰기 지속 | docs/11 |
| collector 프로듀서 | idempotent, acks=all, zstd, linger 50ms, batch 2000 | 중복 방지, 압축률(실측 2.5배), 50ms는 e2e 예산(2초 배치 창) 대비 무시 가능 | docs/11 |
| collector 재연결 백오프 | 1→30초 지수 | WS 연결 한도 5/s/IP(429 실측) 준수 | 사전 검증 §1-3 |
| Flink orderbook JDBC 배치 | 500건 / 2초 | 258 rows/s에서 파트 생성 분당 ~30개 유지. 1초로 줄이면 파트 2배(머지 부담) — e2e p95 1.9s의 대부분이 이 창, 사용자 결정으로 유지 | docs/11 §4 |
| Flink orderbook 워터마크 | out-of-orderness 5s, idleness 30s | 파티션 6개·키 해시로 항상 유입되나 저유동 파티션 정체 대비 | 코드 주석 |
| ClickHouse `orderbook_raw` | Array(Float64)×4, PARTITION BY toDate(ts), ORDER BY (market, ts), TTL 7일 | 배열이 열 압축에 유리(실측 가격 70배·잔량 15~18배), 일 파티션 = TTL 만료가 파트 드롭으로 끝남, 7일 = A안 | docs/11 |
| ClickHouse `orderbook_1m` 지표 | mid open/close/min/max, spread avg/bp/max, imbalance 1/5/15단, depth15, total, recv lag | 스프레드·깊이·불균형이 L2 분석 표준 지표. 1/5/15단은 count .15 구조 그대로. recv lag는 수집 지연 감시용 | docs/11 |
| ClickHouse 메모리 | 1.75G (1.25→) | 호가 인서트 후 87% 도달. max_server_memory_usage 자동 1.13→1.57GiB. 2G가 아닌 이유: 호스트 swap 4~6GB 사용 중이라 여유 최소화 | 04:19·04:55 실측 |
| ClickHouse system.* TTL | 14일 | 로그 15.9GiB(80%) 점유. 14일이면 사고 분석 창 확보하며 ~1GB 수준 | 정리 후 2.41GiB |
| health_check 적재 지연 임계 | p50 > 60초 (10분 창) | 정상 p50 1.2s, 8월 사고 초기(08-19 18:00)에 p50 3,565s. 60s면 포화 시작 수십 분 내 감지, 정상 변동(max 2.8s)의 20배로 오탐 여유 | docs/08 §2 |
| mem_limit 규칙 | 재기동 후 실사용의 1.5배, 최소 128M, Node 앱은 NODE_OPTIONS 병행 | 스왑 상태 RSS 기준 1.5배는 과소산정(n8n 24분 크래시) | docs/09 작업4 |
| 관찰 지표 수단 | cron + CSV (Prometheus 대신) | Prometheus는 Airflow StatsD만 수집 중. 7일 안에 exporter 구축은 범위 초과, 관찰 뒤 정식 지표계로 승격 | 사전 검증 §2-12 |
| 관찰 기간 | 7일 | 피크(KST 09/22~24시)·주말 저거래 1사이클. 10분 확인은 스모크 테스트 | docs/12 |
| 커밋 규칙 | 트레일러 없음, 비밀값 스캔 후 | 포트폴리오 리포(본인 작업으로 보여야), 공개 리포 | — |

## 사전 검증 요약 (09-08 22:25 ~ 09-09 00:20 UTC) — 상세 `~/cdc-orderbook-probe/REPORT.md`

- KRW 287마켓. 단일 WS 커넥션으로 전 마켓 trade+orderbook.15 동시 구독, 누락·드랍 0. 항상 전체 스냅샷(delta 없음).
- 한도 실측: WS 연결 5/s/IP(429 `too_many_requests`, Retry-After 없음), 구독 메시지 7/s 무에러, 동시 20커넥션 OK, REST orderbook 10/s/IP.
- orderbook.15 전 마켓: 아침(07:28 KST) 153.6 msg/s·158.7KB/s, 오전 피크(09:11 KST) **261.7 msg/s·272.8KB/s**. 와이어(permessage-deflate) 66.9→126KB/s. count별 크기(BTC) .5=432B / .15=1,051B / .30=1,998B.
- trade WS 신규 필드 bap/bas/bbp/bbs(최우선 호가) 발견 → 1·2차에서 ClickHouse까지 적재.
- 미니PC: 디스크 352G→(정리 후)366G 여유, crypto_trades 압축률 3.12, Flink 슬롯 0 여유·task heap 25.6MiB, CPU 제한 없는 컨테이너 전부.
- Oracle: PAYG 계정, A1 2OCPU/12GB = 무료 한도 99.2%, Budget 1 SGD 알림 있음.
- Oracle vs 미니PC 동시 프로브 2회: 건수·드랍·p95(35 vs 33ms) 동일 → 집 회선은 병목 아님.

## 1차·2차 실행 상세 로그 (09-08 22:58 ~ 09-09 02:16 UTC)

## 00. 준비
- 22:58 UTC (07:58 KST) backup/ 에 원본 보관: producer.py, cdc/n8n/icepush/circuit/fds compose, MySQL·ClickHouse DDL(crypto_trades + system.* 6개), docker inspect/images, crontab

## 작업 1. 야간 샘플 예약 (23:12 UTC)
- host crontab 1회성: `0 13 9 9 * /home/calme/cdc-orderbook-probe/night_sample.sh` (KST 22:00). 스크립트가 trade/orderbook.15 프로브 2개를 컨테이너로 띄우고 최대 12분 대기 후 잔여 강제 정리, 마지막에 crontab 자기 제거. 로그 `logs/night_sample.log`.
- 세션 내 후속 리마인더 13:16 UTC 등록(결과 반영용).

## 작업 2. 체결 best bid/ask 4필드 (23:17~23:23 UTC)
- 23:17 SIMPLE 키 실수신 확인: bap/bas/bbp/bbs (simple_keys.py)
- 23:18 producer.py 수정: INSERT 11컬럼, parse_trade에 `_opt()` NULL-safe 파싱 추가 (diff는 backup/producer.py.orig 대비)
- 23:18:15 MySQL `ALTER TABLE crypto_db.crypto_trades ADD COLUMN best_ask_price/best_ask_size/best_bid_price/best_bid_size DECIMAL(20,8) NULL, ALGORITHM=INSTANT` (1초, 2.79M rows)
- 23:18:16 Debezium: connector RUNNING 유지, `cdc` 토픽 offset 20에 ALTER DDL 이벤트 기록, `_schema-history`에 best_ask_price 2건, connect 로그 "Already applied 20 database changes"
- 23:19:28 ClickHouse `ALTER TABLE cdc_pipeline.crypto_trades ADD COLUMN ... Nullable(Float64)` ×4 (sequential_id 뒤). dbt/airflow에 `SELECT *`는 dbt tests 3개뿐(ref(int_ohlcv_1h) 대상, crypto_trades 아님) → 영향 없음
- Flink: CdcEventParser가 필드 화이트리스트 방식, ClickHouseSinks INSERT 컬럼 고정 → **신규 필드는 Kafka까지 도달하나 Flink→ClickHouse 미통과. 잡 재제출 필요 → 금지 범위, 2차 지시로 이관**
- 23:19:28 `docker compose build upbit-producer` (17초), 23:19:45 `up -d --no-deps upbit-producer` → 23:19:47 기동, WS 연결 완료. 재연결 공백: 마지막 STATS 23:19:21 → 신규 수신 23:19:47 (약 26초, 빌드 포함 시 재기동 자체는 2초)
- 23:22 검증: 재기동 후 564행 전부 non-NULL, XRP spread 1~2원(5~10bp), DOGE 1원(83bp). Kafka 이벤트에 best_* 4필드 포함(value 770B, 이전 637B → +133B)

## 작업 3. 8/29 반토막 판별 (23:18~23:30 UTC, 읽기 전용)
- REST `/v1/candles/days` 5코인 × 20일(8/18~9/6, UTC 일봉) 저장 `out/candles/days_5coins.json` (rate limit 헤더 group=candles sec=9)
- source_ts(binlog 시각) 기준 일별 비율은 8/19~8/31 41%~322%로 요동 → 그러나 **upbit_timestamp(거래소 체결시각) 기준으로 재집계하면 전 기간 전 코인 97.5~99.9% 일치** → 유실 아님
- 원인: producer→MySQL 적재 지연. source_ts−upbit_ts p50이 8/19 18:00 UTC 3,565s → 8/22 15:00 UTC **132,933s(36.9h)** 피크 → 8/30 00:00 UTC 정상(10s). 8/31 00~06 UTC 재발(7,265s). 9월에도 시간당 7~12 msg/s 구간에서 p50 60~1,475s 반복
- 메커니즘: producer.py flush()는 2초마다 1회, 1회에 BATCH_SIZE=20행만 INSERT → **최대 10 rows/s 상한**. 8/21~8/22 시장 급증(8/22 05:00 UTC 시간당 261,895건 = 72.75/s)으로 deque 백로그 누적, 이후 8일간 10/s로 배수
- 관련: 8/29 06:45 재연결과 무관(백로그 배수 중), "690K→364K"는 배수 완료 후 정상 수준 복귀
- 파일: out/candles/{ch_daily_5coins,ch_daily_by_upbit_ts,ratio_table,hourly_lag}.json

## 작업 4. 디스크·메모리 위생 (23:18~23:32 UTC)
- 23:18:46 `docker builder prune -f` → 7.016GB (df 95G→90G)
- 23:23:34 미사용 이미지 7개 삭제(목록 out/images_delete_list.txt, 보존 6개 사유 명기) → df 90G→86G. dangling 0
- ClickHouse system.*: 23:23 trace_log MODIFY TTL 14d + DROP 202602/202603 (권한 확인). 23:25~23:28 part_log/metric_log/query_log/asynchronous_metric_log DROP PARTITION 202602~202607 + MODIFY TTL 14d
  - **사고**: MODIFY TTL이 자동 실행한 MATERIALIZE TTL 뮤테이션이 메모리 한도(1.13GiB) 초과로 실패·재시도(metric_log 2.42GiB 요구, query_log 1.23GiB). ClickHouse CPU 345%, 메모리 1.16GiB/1.25GiB. query_log 202606 DROP도 실패(뮤테이션 잠금)
  - 23:31:25 `KILL MUTATION` (3건) → CPU 9.6%, 메모리 849MiB 회복. 202606·202608 파티션 추가 DROP(메타데이터 연산). 남은 데이터 202609만(5테이블 합 ~550MiB), TTL 14d는 메타데이터에 유지(향후 파트 병합 시 적용, ttl_only_drop_parts 아님)
  - 결과: system.* 15.91GiB → 2.41GiB (query_views_log 1.87GiB 미지시라 미처리). /var/lib/clickhouse 20G→8.8G. df 86G→76G
  - config.d/system-logs-ttl.xml 준비만(마운트·재시작 필요 → 보류)
- mem_limit compose 편집 완료(적용은 00:20 UTC 이후): n8n postgres 256M(shared_buffers 128MB라 규칙값 144M 대신 상향), n8n-redis 128M, n8n 416M, worker 432M, icepush-api 128M, circuit api 128M, fds-redis 128M, fds generator/consumer 512M(2월 이후 exited, 사용량 없음), cdc airflow-init 512M(exited, 1회성). my-postgres는 compose 없음(`docker run`) → `docker update` 예정 256M
- fds-generator/consumer: 2026-02-12 00:42 UTC 이후 exited(rc 137), compose `profiles: ["pipeline"]` 게이트 → 상시 부하 없음

## 작업 5. Flink (읽기 전용, 23:20~23:32 UTC)
- 체크포인트 크기 시계열 샘플러 25분 실행(logs/cp_sizes.log)
- 상태 596MB 정체 규명: TM 로컬 RocksDB 디렉터리 실측 — AnomalyDetector subtask1 `MANIFEST-000004` **352.7MB**, SST 15개 합 ~15KB, WAL 0B. subtask2 MANIFEST 176.9MB. Window 연산자 MANIFEST 55.7/39.6MB
  → 키드 상태(5마켓×5 ValueState)는 수십 KB. 체크포인트 크기 = RocksDB MANIFEST(플러시/컴팩션 버전 기록, 체크포인트당 ≈3.3KB 누적, 105,821회) → 정상 누적도 누수도 아닌 **네이티브 풀 체크포인트 방식의 메타데이터 비대**. 상한은 RocksDB max_manifest_file_size 1GB에서 롤오버

## 추가(사용자 승인, 23:44 UTC). Oracle vs 미니PC 수집 지연 동시 비교
- Oracle(ap-chuncheon-1, aarch64)에 임시 `python:3.11-slim`(arm64, 49MB) 컨테이너로 ws_probe.py 실행(websockets 16.0 pip 설치). 미니PC에서도 같은 시각 같은 조건(trade 전 마켓 / orderbook.15 전 마켓, 10분) 실행. 결과 out/oracle_*.json, out/minipc_par_*.json
- TCP/TLS 타이밍(curl, 3회): 미니PC connect 11~16ms / TLS 34~52ms / TTFB 53~71ms, Oracle connect 5.6~7.1ms / TLS 60~80ms / TTFB 72~100ms
- 23:38 docs/08-ingest-lag-incident.md 작성(작업 3 결과). 주의: 09-08 23:19 producer 재생성으로 이전 컨테이너 로그 소실 → 백로그를 로그 대신 ClickHouse(upbit_ts vs source_ts)로 재구성
- 23:50:04 UTC 작업 2 30분 검증 통과: producer WARNING/ERROR 0, STATS received 6,269 / inserted 5,799 / duplicates 470(INSERT IGNORE, 7.5%) / errors 0. MySQL 재기동 후 5,889행 NULL 0(0.000%). 스프레드 평균 BTC 1.48bp·ETH 4.19bp·XRP 5.54bp·SOL 7.21bp·DOGE 83.0bp, crossed(ask≤bid) 0건, 체결가가 BBO 밖인 행 BTC 8건(스냅샷 시점 차이로 추정, 미검증). Flink 체크포인트 105,848 완료/0 실패, health_check 23:10~23:40 4회 success. ClickHouse 5,987행 유입(best_* 컬럼은 전부 NULL — Flink 미통과, 예상대로). 컨테이너: producer 23MiB, mysql 703MiB, clickhouse 920MiB, flink-tm 348MiB
- 23:55 UTC Oracle vs 미니PC 동시 프로브 결과(08:44:52~08:54:54 KST, 10분): trade 10,280 vs 10,279건(17.13 msg/s 동일), orderbook.15 91,066 vs 91,043건(151.8 msg/s), 누락 마켓 0/0, 에러 0/0. 수신지연 p50(recv−tms) trade 27.6 vs 30.9ms, orderbook 24.0 vs 28.0ms. 미니PC NTP 오프셋 −3.8ms 보정 시 31.4/27.8 vs 30.9/28.0 → 차이 없음. 와이어 대역폭 66.6 vs 67.3KB/s. Oracle 임시 컨테이너·이미지·파일 정리 완료(probe-ora-* 0개, python:3.11-slim 삭제)

## 작업 4-4 mem_limit 적용 및 사고 (00:00~00:25 UTC)
- 00:00:59 UTC: 사용자 crontab `0 0 * * * /home/calme/n8n/update-n8n.sh`(n8n 자동 업데이트)가 23:29에 편집해 둔 compose를 먼저 적용해 n8n 4개 컨테이너를 재생성(이미지 digest 307d6065 동일 = 버전 변화 없음, 2.38.1)
- 00:01~00:23 UTC: **n8n-n8n-1 크래시 루프** — exit 134 "FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory", 약 23초 간격, 재시작 52회. 원인: cgroup 메모리 제한을 V8가 힙 상한으로 환산(416M→약 225MB, 576M 상향 후 실측 312MB)하는데 n8n 메인 프로세스 기동 시 힙 수요가 그 이상. 제한 없던 이전(호스트 15.7GB 기준)에는 발생하지 않음. 미니PC n8n은 CDC 이상거래 Slack/Gmail 알림 워크플로우를 돌리므로 **약 22분간 알림 기능 중단**
- 00:20:08 UTC 예약 스크립트: icepush-api·circuit-connect-api·fds-redis 재생성, my-postgres `docker update` 256M. cdc-* 미접촉
- 재기동 후 실사용이 기존 측정치(스왑 아웃 상태의 RSS)보다 큼: circuit-connect-api 22.8→119MiB(제한 128M의 93%), icepush-api 17→54MiB, n8n 270→380MiB. 00:21:37 circuit 256M·n8n 576M로 상향(docker update + compose)
- 00:23:3x UTC n8n-n8n-1 제한 **원복**(`docker update --memory 0`, compose에서 deploy 블록 제거). 교훈: 스왑이 많은 호스트에서 docker stats RSS 기반 1.5배 규칙은 과소산정; Node 앱은 cgroup 제한이 V8 힙 상한으로 이어지므로 별도 산정 필요
- `docker update --memory 0`은 반영되지 않음(inspect 576M 유지, 크래시 지속 → 재시작 57회까지). 00:25:23 UTC `docker compose up -d n8n`으로 제한 없이 재생성 → 00:25:26 기동, V8 heap_size_limit 4,192MB, 사용 330MiB, 105초간 재시작 0회, "n8n ready" 확인. 크래시 루프 총 지속 00:01~00:25 (약 24분)
- 최종 mem_limit 상태: n8n-worker 432M(사용 234MiB), n8n-postgres 256M(25MiB), n8n-redis 128M(3MiB), icepush-api 128M(54MiB), circuit-connect-api 256M(119MiB), fds-redis 128M(11MiB), my-postgres 256M(15MiB, docker update만 — compose 없음), fds-generator/consumer 512M·airflow-init 512M(compose만, exited), **n8n-n8n-1 제한 없음(원복)**
- 00:11:20~00:21:26 UTC(09:11~09:21 KST, 국내 오전 피크) p95 포함 재측정: trade 53.6 msg/s(아침 07시대 17.1의 3.1배), orderbook.15 261.7 msg/s(151.8의 1.7배), payload 272.8KB/s, 와이어 126KB/s. 수신지연 p95 미니PC 36.4/35.7ms vs Oracle 34.9/32.6ms, p99 126/59 vs 125/59ms(trade p99는 SNAPSHOT 287건 포함으로 오염). 결과 파일 out/*_all_2.json
- 00:55:19 UTC mem_limit 30분 관찰: 비CDC 컨테이너 8개 재시작 0·OOM 0. producer WARNING/ERROR 0(uptime 5,706s, received 28,856 / inserted 26,689 / duplicates 2,166), Flink 체크포인트 105,913 완료/0 실패, health_check 00:10~00:40 4회 success, ClickHouse 최근 10분 2,052행. free: used 6.7GB / swap 6.1GB(작업 시작 시 4.3GB → 00:20 6.9GB → 6.1GB). df 81G 사용(정리 직후 76G에서 +5G, 원인 미추적)

### 2차 실행 (사용자 승인 01:20 UTC: 1·2·3 묶음 — Flink 상향+hashmap, best bid/ask Flink 통과, producer 상한 제거)
- 01:36 미니PC 점검: 필수 정리 없음(df 366G 여유, available 8.0GB). 선택 후보: airflow/logs 3.1G, journal 1.6G(sudo), query_views_log 1.88GiB, MySQL 1.8G
- 01:40 backup/phase2/: flink/src 전체, flink-cdc-job-1.0.0.jar(02-23 빌드), producer.py(1차본), compose(1차본), jobs-before, offsets-before(flink-cdc-consumer 커밋 오프셋 p0 89,214,711/p1 89,181,681/p2 89,212,679, lag 79~96), ch-before
- 코드 변경: CryptoTradeEvent 4필드(Double, null 허용), CdcEventParser parseNullableDecimal, ClickHouseSinks INSERT 17컬럼+setNullableDouble, CdcPipelineJob setStartingOffsets → committedOffsets(LATEST)
- producer.py: flush()를 버퍼 소진까지 반복(MAX_BATCHES_PER_FLUSH=50), STATS에 buffer= 추가, 5,000행 초과 시 1분당 1회 WARNING. compose BATCH_SIZE 20→100
- compose Flink: JM/TM state.backend hashmap, externalized-checkpoint-retention RETAIN_ON_CANCELLATION, TM process.size 2g / jvm-metaspace 384m / managed.fraction 0.25 / slots 4, TM 메모리 제한 1280M→2304M
- 01:41 producer 이미지 재빌드(75.7MB). flush 단위테스트(test_flush.py, 가짜 커서) 4케이스 통과: 5,000행 1회 소진(50배치, 1.2ms), 12,000행은 5,000 상한 후 잔여 유지+경고, 3번째 배치 실패 시 해당 배치 버퍼 앞 복원·순서 보존, 이론 상한 2,500 rows/s(이전 10)
- 01:41 Flink JAR 빌드 시작(docker 멀티스테이지, 로그 logs/flink-build.log)
- 01:42:27 Flink JAR 빌드 완료(42MB, 1분, CryptoTradeEvent에 bestAskPrice 포함 확인)
- 01:42:48 health_check DAG pause. pre-stop: 커밋 오프셋 p0 89,215,036 / p1 89,181,993 / p2 89,212,975, ClickHouse 최근 max trade_id 95,997,125 (source_ts 01:42:49.039)
- 01:42:53 `flink stop --savepointPath` CDC → savepoint-dd0c41-0cb7e3ec0577 (**20KB, 파일 1개**; 네이티브 체크포인트 625MB 대비 → 상태 소량 확정). 01:42:58 circuit → savepoint-f27298-76d8cc7ff926 (116KB)
- 01:43:18 `docker compose up -d flink-jobmanager flink-taskmanager` → TM 9초 만에 등록. 실효 메모리: process 2048M = overhead 205 + metaspace 384 + fw heap 128 + fw offheap 128 + network 146 + managed 365 + **task heap 692M**. slots 4, state.backend hashmap, 컨테이너 제한 2304M
- 01:43:33 circuit 복원(JobID 55988aed…), 01:43:37 CDC 복원(신규 JAR, JobID 9ad0f8de…) → 둘 다 RUNNING, 슬롯 3/4 사용
- 01:44:24 producer 재기동(`up -d --no-deps`, 3초). 이전 컨테이너 최종 STATS 01:43:54 received 43,709 / inserted 40,557 / duplicates 3,152. 신규 STATS에 `buffer=0` 표기, 배치 크기 100
- 01:45:53 검증: CDC 잡 체크포인트 2/2 완료, **state 17.8KB, e2e 74~163ms**(이전 625MB, 1.8~12s), 예외 0. Kafka 오프셋 p0 89,215,159(> pre-stop 89,215,036, lag 16) 등 savepoint 지점부터 연속 소비. ClickHouse: 정지 후 첫 행 trade_id **95,997,126 = pre-stop max + 1 → 유실 0**. 정지 후 394행 중 388행 best_* non-NULL. TM 557MiB/2.25GiB, JM 337MiB
- 01:46:28 health_check unpause. 정지 구간(01:42:40~01:43:10) 69행 = uniqExact 69 → 중복 0. 정지 직후 6행(95,997,126~131)은 구 JAR가 barrier 전 처리해 best_* NULL(정상)
- 02:16:00 30분 검증: 체크포인트 32/32, state 17.8~18.0KB, e2e avg 51ms/max 163ms. Metaspace 81/384MB, Heap 138/822MB. producer 경고 0, buffer=1. health_check 4회 success. ClickHouse 30분 6,565행 best NULL 0, cdc_latency 5.3ms, ingest lag p50 1,670ms/max 4,025ms, 집계 25행. TM 962MiB/2.25GiB, swap 4.1GB
- 문서: docs/10-phase2-flink-producer-upgrade.md 작성

## 3차 — 호가(orderbook) 수집 착수 (09-09 02:30 UTC ~)
- 02:30 사용자 승인: confluent-kafka, 3브로커 유지, KRaft는 실험 뒤. 통합 worklog를 docs/worklog.md로 이전(이전 파일 ~/cdc-orderbook-probe/worklog-phase1.md는 보관)
- 설계: 수집기 `orderbook-collector`(별도 컨테이너, WS orderbook.15 287마켓 → Kafka `upbit.orderbook.v1`, key=market, zstd, idempotent) → Flink `OrderbookJob`(기존 JAR 내 별도 메인, 슬롯 1) → ClickHouse `orderbook_raw`(TTL 7d, Array(Float64) 4개) + `orderbook_1m`(TTL 365d)
- 03:43:45 Kafka 토픽 `upbit.orderbook.v1` 생성: 파티션 6, RF 2, retention.ms 86,400,000(24h), retention.bytes 6,442,450,944(6GB/파티션), min.insync.replicas 1, segment.bytes 256MB. 리더 브로커 2/3/1 분산 확인
- 03:44 ClickHouse `cdc_pipeline.orderbook_raw`(PARTITION BY toDate(ts), ORDER BY (market, ts), TTL 7일, Array(Float64)×4) · `orderbook_1m`(월 파티션, TTL 365일) 생성. DDL 파일 `clickhouse/orderbook.sql`
- 03:45:00 수집기 이미지 빌드(confluent-kafka 2.15.0, websockets 17.1) → 03:45:09 `cdc-orderbook-collector` 기동(compose 서비스 추가, 256M, cdc-network). 코드 `orderbook-collector/collector.py`: 단일 WS, REST로 KRW 마켓 조회, key=market, zstd, idempotent, 지수 백오프(1→30s), STATS 30초(recv/produced/queue/lag p50·p95)
- Flink `com.cdc.pipeline.orderbook.*` 6개 클래스 작성(Event/Parser/Minute/Aggregator/Sinks/Job). 같은 JAR, 별도 메인·컨슈머 그룹 `flink-orderbook-consumer`, 이벤트타임 1분 tumbling(out-of-orderness 5s, idleness 30s), committedOffsets(LATEST)
- 03:47:20 Flink JAR 재빌드(orderbook 클래스 9개 포함, 43.9MB). 03:47:34 `flink run -d -c com.cdc.pipeline.orderbook.OrderbookJob` → JobID 4e47e3d4…, 3잡 RUNNING, 슬롯 4/4
- 03:48:10 첫 검증(제출 36초 후): orderbook_raw 8,897행 / 268마켓 / 15단 배열, recv_lag(rts−tms) 평균 30ms, e2e(ts→flink_ts) 826ms. orderbook_1m 첫 창(03:47, 부분) 259행. BTC 03:47 창: 190 스냅샷, spread 1.73bp, imb5 +0.917. 컨슈머 lag 0, 예외 0. CDC 잡 체크포인트 124/124 무영향. TM 993MiB/2.25GiB, ClickHouse 1.04GiB, 수집기 25MiB
- 04:05 결정 기록: 브로커 3 → 실험 → 1 (결정 표 참조)
- 04:19:00 30분 관찰(03:49~04:19): 유입 257.7 msg/s(464,513건), 재연결 0·발행 실패 0. recv lag p50 30/p95 40ms, e2e(업비트→ClickHouse) p50 849/p95 1,892/max 2,203ms. **orderbook_raw 39.7 B/행 on-disk(압축 14.2배, JSON 대비 26배)** → 원본 0.89GB/일, 7일 6.2GB. Kafka zstd 415 B/msg → RF2 18.5GB/일. orderbook_1m 28창×286마켓 7,824행. Flink 체크포인트 31/31 state 61KB e2e 44ms. CDC 잡 155/155·producer buffer 2·health_check 3회 success 무영향. ClickHouse 메모리 1.087/1.25GiB(87%) 감시 필요. 문서 docs/11-orderbook-launch.md

## ClickHouse 메모리 상향 1.25G → 1.75G (04:22 ~ 04:26 UTC, 사용자 승인)
- 영향 분석: 데이터는 named volume(clickhouse_data) → 무관. 위험은 Flink JDBC 싱크 실패→재시작→at-least-once 중복 → 3잡 savepoint 정지로 회피. Kafka/MySQL/producer/수집기 무영향
- 04:22:50 health_check pause. 04:22:54~57 3잡 savepoint 정지(orderbook 4e47e3-77b40169cc28, circuit 55988a-c9c95b562c74, CDC 9ad0f8-e2b382f3b507). 정지 시 오프셋 backup/phase3/offsets-at-stop.txt
- compose: clickhouse memory 1280M→1792M, `./clickhouse/config.d/system-logs-ttl.xml` 단일 파일 마운트(디렉터리 마운트는 docker_related_config.xml을 가려 listen 설정이 깨지므로 회피)
- 04:23:22 `up -d --no-deps clickhouse` → 04:23:46 기동(24초). max_server_memory_usage **1.57GiB**(이전 1.13). 행 수 대조: 정지 전 대비 crypto_trades +257·orderbook_raw +7,668(정지 직전 싱크 flush분), 그 외 동일. `.inner_id.1453ac1e`(mv_latency_stats 내부) 295,877→295,875(−2, 머지에 의한 것으로 추정, 원본 아님)
- system.* 로그 테이블 rename 없음(config.d TTL과 ALTER TTL 일치 확인: `TTL event_date + toIntervalDay(14)`) → TTL 영속화 완료
- 기존 경고(IPv6 listen 실패)는 재시작 전과 동일한 무해 경고

## 전 코인 체결 확장 준비 + 관찰 전 정비 (04:35 UTC ~, 사용자 승인: MySQL EVENT·Kafka retention·health_check 수정·cron 지표)
- 04:35:31 MySQL `ALTER EVENT cleanup_old_trades`: EVERY 1 HOUR / LIMIT 25000 → **EVERY 10 MINUTE / LIMIT 40000**(처리 용량 60만→240만 행/일). 원본 backup/phase3/mysql-event-before.txt. 테이블 2,745,864행(9/1~)
- 04:35:31 Kafka `cdc.crypto_db.crypto_trades` retention.bytes 1GB → **4GB/파티션**(동적, 무재시작). 근거: 전 코인 53 msg/s×658B ≈ 3GB/일, 72h 유지
- 실측: 최근 24h anomaly_alerts 193건(LARGE_TRADE 38, VOLUME_SURGE 155) — 5코인 기준. 임계값: LARGE_TRADE BTC 5억/ETH 3억/기타 1억, PRICE_SPIKE BTC 2%/기타 3%, VOLUME_SURGE EMA×150, RAPID 비활성
- **발견: n8n 이중 실행** — 미니PC와 Oracle 양쪽 n8n 모두 "CDC Pipeline - Anomaly & Health Monitor" active=true (워크플로우 10개 목록 동일). 알림이 2중으로 나가는 상태. 어느 쪽을 끌지 결정 필요
- Flink 재시작 전략을 잡 코드에 명시(CdcPipelineJob·OrderbookJob: fixedDelay 20회×30초). 다음 재제출 시 적용
- 04:36 producer: `MARKETS=ALL_KRW` 환경변수 시 REST로 KRW 전 마켓 조회(재시도 10회) 후 구독. 이미지 재빌드·import 확인. 재기동은 04:55 관찰 확인 뒤(재시작 간격 분리)
- 04:37 health_check DAG: `expected_jobs=3`, `check_ingest_lag` 태스크 추가(최근 10분 source_ts−upbit_timestamp p50/max), evaluate에서 p50 > 60초면 "Ingest Lag" 알림. DagBag 파싱 오류 0, 태스크 7개
- 04:39 n8n 이중 실행 **실증**: 양쪽 execution_entity 최근 30분 "CDC Pipeline - Anomaly & Health Monitor" 30회/30회, 마지막 실행 04:39:27.026(미니PC) vs .029(Oracle), 성공 28/28. 사용자 결정: **미니PC 쪽 워크플로우만 비활성화**(컨테이너는 유지)
- 04:40:50 `docker exec n8n-n8n-1 n8n update:workflow --id=wSAmy0kCFOuxFCz2 --active=false` → DB active=false 확인(남은 active: My workflow 2, Circuit Connect Daily Log Export v2). CLI 안내대로 04:40:56 미니PC n8n 메인 컨테이너 restart(04:40:57 기동, "n8n ready")
- 04:44:35 검증: 04:41 이후 미니PC n8n CDC 워크플로우 실행 0건, Oracle 4건(매분). 이중 알림 해소. 미니PC n8n 메인 318MiB(제한 없음), worker 281/432MiB
- 04:55:04 ClickHouse 상향 30분 검증: 3잡 RUNNING, 체크포인트 CDC 30/30·orderbook 30/30, 예외 0. 정지 창(04:20~04:30) crypto_trades 2,630행 = uniqExact 2,630(중복 0), orderbook_raw 137,619 = uniqExact(market,ts) 137,619(중복 0), 분당 12.0~15.4K행 연속(04:23~24 재시작 분 포함, 유실 0). ClickHouse **930MiB/1.75GiB(52%)**(이전 87%), MemoryTracking 908MiB. health_check 04:20~04:40 3회 success(적재 지연 태스크 포함). producer buffer 5·errors 0, 수집기 215 msg/s·errors 0. 호스트 swap 3.9GB

## 전 코인 체결 확장 dry-run (04:55 UTC ~)
- 04:55:50 producer 재기동(`MARKETS=ALL_KRW`, BATCH_SIZE 100). 재기동 전 STATS 04:55:22 received 42,735 / inserted 40,696 / duplicates 2,036 / buffer 3, MySQL 2,631,402행(max trade_id 96,040,154)
- 04:55:52 기동: 마켓 **287개** 구독, WS 연결 완료 04:55:53. 첫 STATS(30초): received 585, inserted 547, duplicates 18, errors 0, buffer 20, **18.2 rows/s**(5코인 3.6/s의 5배)
- 1시간 dry-run 모니터 시작(10분 간격): producer buffer/rate, MySQL 증가·정리, Kafka 체결 토픽 유입, Flink CDC 체크포인트·백프레셔, ingest lag, anomaly_alerts 유형별 건수(알림 폭주 판정), ClickHouse·TM 메모리
- 04:58 관찰 지표 수집 스크립트 `scripts/observe/collect_metrics.sh` 작성·테스트(87컬럼, 실행 12초). 체결(5분 행수·마켓·ingest lag p50/p95/max·cdc_latency·flink lag·best NULL), 호가(행수·recv/e2e p50/p95/max·1m), alerts 유형별, ClickHouse 메모리·파트수·용량, producer/collector STATS, Flink 3잡 체크포인트·state·e2e, TM heap/metaspace, Kafka end-offset·lag·디스크, MySQL 행수, 컨테이너 9개 mem/cpu, 호스트 load/mem/swap/df. 출력 `~/pipeline-observation/metrics_5m.csv`. readonly_user가 system.*를 못 읽어 그 둘만 clickhouse-client 사용
- 05:00 crontab `*/5 * * * *` 등록(로그 ~/pipeline-observation/collect.log). **7일 관찰 창 시작 시각은 dry-run 종료·안정 확인 후 기록**
- 05:57:46 dry-run 1시간 종료 **통과**: received 96,545 / inserted 94,325 / duplicates 2,210(2.3%, INSERT IGNORE) / errors 0 / buffer 0~38 / 경고 0. 처리율 25.5~31.2 rows/s(5코인 3.6/s의 7~9배). Flink CDC 체크포인트 93/93, 백프레셔 0ms/s, e2e 27~49ms. ingest lag p50 1.1~1.2s / p95 2.1s / max ≤2.8s(10분 창 6회 모두). ClickHouse 10분당 11.0~19.7K행·239~254마켓. anomaly_alerts 1시간 20건(PRICE_SPIKE 6, LARGE_TRADE 5, VOLUME_SURGE 9; 상위 BTC 6·USDT 5·INJ 3) → 폭주 없음, 임계값 유지. MySQL 2,634,793 → 2,563,267행(10분×40K 정리가 유입을 따라감). health_check 04:50~05:40 6회 success. 메모리 producer 23MiB·mysql 744MiB·connect 734MiB·TM 1,000MiB·ClickHouse 737MiB
- 05:58:25 Debezium `tombstones.on.delete=false` PUT(원본 backup/phase3/connector-config-before.json). 커넥터·태스크 RUNNING 복귀 5초 내. Connect 로그 ERROR 1건 "Exception while closing JDBC connection"은 태스크 재시작 시 MySQL 커넥션 종료 예외(무해, 이후 정상)
- 05:58:53 health_check pause → 3잡 savepoint 정지(circuit a8c89c-d1a92a6c0bdf, orderbook 7d75f2-b78cca6fc6b4, CDC b8a55e-53eaa760d1e7) → 05:59:04 재시작 전략 포함 JAR로 재제출(CDC e1dac99d…, orderbook d9f4786e…, circuit 3e6ee790… 기존 JAR) → 3잡 RUNNING → 05:59:20 unpause. REST 확인: CDC·orderbook "fixed delay 30000ms, #20 attempts", circuit은 클러스터 기본(10s×3, 타 프로젝트 잡이라 미변경)
- 06:0x 커밋 **feb959b** "feat: orderbook pipeline, all-market trades, Flink hashmap/2g TM, producer drain loop, 7-day observation tooling" (26파일 +1,513/−45), 태그 `obs-week1-start`. 커밋 전 스캔: 비밀값·IP·이메일 없음(.env gitignore, 환경변수 참조만). 트레일러 없음(포트폴리오 리포 규칙). 첫 시도는 `flink/target` gitignore 규칙에 add가 막혀 실패·태그 오지정 → 태그 삭제 후 재커밋. 미커밋 잔여: dbt tests/assert_no_long_gaps.sql(기존 diff, 본 작업 아님), png 3개(기존 untracked)
- 06:07:09 재제출 검증: CDC·orderbook 체크포인트 8/8, 예외 0, state 58KB/56KB(287마켓 키). 연속성: 정지 전 max trade_id 96,137,856 → 정지 후 첫 행 96,137,857, 10,516행 = 고유 10,516(중복 0). orderbook_raw 68,143 = 고유 68,143, 분당 11.7~15.7K 연속(05:58~06:01). **tombstone 효과**: 06:05:31 정리 실행 후 p0 최근 3,000건 = create 2,097 / delete 903 / tombstone 0. health_check 05:40·05:50 success. producer buffer 10, errors 0. 지표 CSV 06:05 행: lag p50 1.35s / p95 2.18s, best NULL 0, 호가 e2e p95 1.9s 근방
- 06:10 `git push origin main` + 태그 push
- **06:10 UTC (15:10 KST) — 7일 무변경 관찰 시작.** 종료 예정 09-16 06:10 UTC. 기준 커밋 feb959b(obs-week1-start). 예외 조치는 시각·이유와 함께 이 파일에 기록

## 관찰 중 병행 작업 1: 재생 실험 도구 준비 (06:09 UTC ~, 실험 리포 `~/pipeline-load-lab`, 로컬 git)
- 원칙: 읽기·dry-run만. 프로덕션 토픽·테이블·잡 무접촉. 재생기는 목표 토픽이 `load_test.`로 시작하지 않으면 거부
- `replay/recorder.py`: 시각/오프셋 구간 → JSONL.gz(파티션·오프셋·Kafka ts·key·value 보존, `_meta` 라인). 임시 컨슈머 그룹·미커밋. 스모크: 체결 create 3,000건 0.3초(2.87MB raw), 호가 2,000건 0.4초. 잔여 컨슈머 그룹 0
- `replay/replayer.py`: 고정 속도 / 계단(예 50:300,100:300,200:300) / 원본 간격×배속, loop, 타임스탬프를 현재 기준으로 이동(원본은 `_orig_*` 보존), idempotent+zstd 프로듀서, `--dry-run`
- `replay/measure.py`: 5초 간격 토픽 end-offset·컨슈머 lag·Flink 백프레셔/busy/체크포인트·ClickHouse 적재율·e2e p50/p95/max·컨테이너 자원 → CSV
- `docs/experiment-plan.md`(초안): 격리(load_test.* 토픽·테이블, 실험 전용 TM, CdcPipelineJob 파라미터화 필요), 시나리오 S1 계단/S2 호가/S3 브로커 장애/S4 개선 후, 판정 기준, 코퍼스는 관찰 종료 시 고정
- 리포 커밋 356605c(로컬). GitHub 생성은 미정

## 관찰 중 병행 작업 2: README 갱신 (06:20 UTC ~)
- 갱신 범위: 데이터 소스(287마켓 체결·호가 실측치), 파이프라인 흐름(호가 직접 발행 경로), "2026-09 확장" 절(호가 경로 다이어그램 + 전/후 표), 리소스 배분(30컨테이너, 제한 합 14.3GB·실사용 7.5GB, 브로커·ZK에 3→1·KRaft 예정 명시), 기술 스택(collector·관찰 지표), Phase 9 체크리스트, 이슈 5(적재 지연 36.9h)·이슈 6(n8n 크래시 루프)·이슈 1 후속, 성능 지표(적재 지연·호가 e2e·처리량·체크포인트·200일+·91.97M), 프로젝트 구조(orderbook-collector, orderbook 패키지, orderbook.sql, config.d, observe, docs 07~12·worklog), 감시 유형 2·3 "호가로 확장 예정", 예상 질문 Q2·Q3·Q5·Q6 수정 + Q12(hashmap)·Q13(호가 경로)·Q14(7일 관찰) 추가, 서버 환경
- 원칙: 이 세션에서 실측·문서화된 수치만 사용. 단일 호스트 HA 아님·CDC 소스 인위성은 README에도 명시(면접 답변 프레임과 일치)
- 09-09 07:39 UTC 관찰 일일 요약(자동): 샘플 33, 위반 없음, 체결 lag p95 max 2.18s, 호가 e2e p95 max 5916.0ms, buffer max 63.0, 알림 44, ClickHouse max 0.93GiB → /home/calme/pipeline-observation/digest_2026-09-09.md
- 13:00:01~13:10:04 UTC 야간 프로브(cron, KST 22:00) 완료: 체결 21.3 msg/s(p95 44), 호가 .15 **247.9 msg/s**(p95 367, payload 251 KB/s, 와이어 114 KB/s, recv lag p50 21/p95 27.5 ms). 아침 대비 체결 ×1.95·호가 ×1.61, 오전 피크 대비 호가 ×0.95. 프로브 컨테이너 잔존 0, crontab 항목 자기 제거 확인. 체결 recv lag p95 217ms(1회, 판정 보류)
- 13:16 가동 후 9.5h 실측: orderbook_raw 8,010,381행 평균 234.5 rows/s(KST 시간별 221~246), 39 B/행(14.4배), Kafka 브로커1 2.41GB/536만 msg = 449 B/msg. 용량표 재계산(REPORT.md §4-B, docs/11 §3-B): ClickHouse 원본 7일 5.5GB, Kafka 24h 18GB, A안 상시 ≈ 25GB. docs/08은 사고 문서라 야간 값과 무관 → 미수정
- 09-10 00:25 UTC 관찰 19.5h 중간 점검(수동, 무변경): 234샘플 3잡 RUNNING 234/234, 체크포인트 실패 0, producer 경고 0, best NULL 0, 수집기 발행실패 0·재연결 0. KST 09시 피크에서 체결 69.4 rows/s·buffer max 104·lag p95 2.1s(H1·H2 정상). 체결 lag p50 1.2s 전 시간대 일정, p95 max 2.56s. 호가 e2e p95 중앙값 1.94s, 3s 초과 1샘플(05:59 재제출 창). 알림 273건/19.5h(≈14/h): BTC 80·**USDT 60**·XRP 30 — USDT는 LARGE_TRADE 기본 임계(1억)에 스테이블코인 대량 체결이 걸리는 것으로 추정 → 관찰 뒤 임계값 재조정 후보 1순위(판정은 7일 데이터로). 오늘 체결 1,702,107행 = uniqExact(source_ts,trade_id) → 중복 0
- **H11 해석 보정(계획 변경 아님)**: MySQL 행수 2.63M→3.88M 증가는 정리가 7일 전(5코인 시절, 일 36만 행) 데이터만 지울 수 있어서 생기는 과도기. 287마켓 행이 7일 경과하는 09-16부터 정리(24만/h) > 유입(~8.5만/h)이 되어 정상상태 약 1,400만 행에서 수렴 예상. 09-16 이후에도 증가하면 위반
- 디스크: df 83.5→102.7GB(+19GB/19.5h). Kafka 브로커1 4.6→10.6GB(호가 RF2 + 전 코인 체결). 호가 24h·체결 72h 보존이 차면 정상상태(호가 ≈18GB + 체결 RF3 ≈27GB) → 09-12경 플래토 예상, 여유 366GB 대비 문제 없음

## 관찰 중 분석 1: 유실·중복·병목 직접 지표 (09-10 00:30 ~ 01:10 UTC, 읽기 전용)
- 구간별 지연 분해(체결, 최근 12h, ms): s1 producer→MySQL p50 1,150~1,300 / p95 2,090~2,300 (2초 배치 창) · s2 binlog→Debezium p95 7~10 · s3 Kafka→Flink→싱크 flush p50 1,400~1,800 / p95 3,000~3,180 (JDBC 3초 배치 창; flink_ts는 flush 시각) · s4 ClickHouse insert p95 13~17 · **e2e p50 2.6~3.0s / p95 4.4~4.9s / p99 4.9~5.5s**. 결론: 지연 예산은 배치 창 2개(2s+3s)가 결정하며 자원 병목 아님(producer 활용률 최대 13.8% = 345 rows/s ÷ 2,500)
- 호가(최근 6h): recv p95 33~43ms · Kafka→Flink→flush p95 1,640~1,940 · insert p95 13~15 · **e2e p95 1.7~2.0s / p99 2.0~2.1s**. 워터마크 5s 초과 지연 도착 0%, crossed 0, 15단 미만 0
- 중복: 체결 6h 561,909행 = uniqExact(sequential_id) = uniqExact(source_ts,trade_id) → 0. 호가 3h 2,782,661 = uniqExact(market,ts) → 0
- 커버리지(최근 1h): 체결 285/287마켓(2개는 무거래로 추정), 호가 287/287. ClickHouse 파트 crypto_trades 34·orderbook_raw 13, 머지 0
- **유실 대조(신규 `scripts/observe/reconcile_trades.py`, 업비트 시간봉 vs ClickHouse 체결시각 기준, 287마켓×12h=3,394셀)**: 거래량 가중 비율 **94.74%**(5코인 시절 99.3~99.9%), 95% 미만 셀 531, 시간대별 89.7%(15h·19h UTC)~99.8%. 마켓별 최소 비율 <90인 마켓 134개
- **위치 특정 1**: MySQL vs ClickHouse(09-09 12~24h UTC, upbit_timestamp 기준) 951,279 = 951,279, 3,392셀 불일치 0 → CDC 이후 유실 0
- **위치 특정 2**: REST `/v1/trades/ticks`(거래소 원장) vs ClickHouse sequential_id — BTC 19h: 4,111건 중 CH 3,846, **누락 265(6.4%, 거래량 3.3%)**, 누락 체결의 해당 초 체결수 중앙값 121·최대 218, 166/265가 같은 ms 형제 체결. KERNEL 15h: 50 중 19 누락(거래량 54%), 해당 초 18건 버스트. JTO 20h: 100 중 4. AWE 20h: 2 중 1 누락(단독 체결, 거래량 99.5%). extra(CH에만 있는 것) 0
- producer received = inserted + duplicates(누락 없음), 재연결 0, MySQL sequential_id distinct = count → INSERT IGNORE 탈락 아님. **결론: 업비트 WebSocket `trade` 스트림 자체가 버스트(초당 수십~수백 건) 구간에서 일부 체결을 전달하지 않음(+드물게 단독 체결 누락).** docs/06의 "RAPID_TRADES 100건/10초 천장" 관찰과 같은 현상의 다른 얼굴. 파이프라인 유실이 아니라 **소스 전달 갭**
- 시사점: (1) 5코인 일 단위 대조(99%+)로는 안 보였고 287마켓·시간 단위·원장 대조로 드러남 → 정합성 검증은 소스 원장 기준이어야 함. (2) 해법은 REST ticks 기반 **정합성 보정 배치**(시간별 비율 <99% 셀만 원장 조회 → 누락 sid를 MySQL에 INSERT → CDC로 흘러 ClickHouse까지 자연 보정). 이 보정이 CDC 경로의 실제 쓰임이 되기도 함. 관찰 뒤 후보 목록에 추가(우선순위 높음). (3) H5(유실 없음) 가설은 "MySQL 이후 유실 없음"으로 판정, 소스 갭은 별도 지표(원장 대조 비율)로 관리

## 관찰 중 분석 2: "소스 전달 갭" 결론의 더블체크 (09-10 00:48 UTC ~, 사용자 지적)
- 지적: "MySQL 이후 무결 + producer 앱 카운트 일치"만으로는 앱 밑(websockets 라이브러리 큐, TCP, 서버의 slow-consumer 처리)에서 빠졌을 가능성을 배제 못 함. 특히 producer는 flush(동기 executemany) 동안 이벤트 루프가 멈추므로 "클라이언트가 느려서 서버가 건너뛴" 경우가 남음
- 실험 설계(판별 실험): DB 없이 수신만 하는 **독립 WS 클라이언트**(`~/cdc-orderbook-probe/trade_sid_probe.py`, KRW 287마켓 trade, isOnlyRealtime, max_queue 4096)를 producer와 동시에 20분 붙여 모든 (market, sequential_id, ttms, recv_ms)를 기록 → 같은 창(가장자리 1분 제외)에서 REST `/v1/trades/ticks`(거래소 원장) vs 독립 클라이언트 vs 파이프라인(ClickHouse) 세 방향 sequential_id 집합 대조
- 판정 기준(실험 전에 고정): REST−독립>0 & 독립−파이프라인=0 → 소스 전달 갭(파이프라인 무결). 독립−파이프라인>0 → 우리 producer 유실(앞선 결론 철회, 수신/쓰기 분리 개선 항목화). 둘 다 0 → 버스트 없는 창, 재실험
- 00:48:21 UTC 독립 클라이언트 기동(컨테이너 probe-sid, 프로덕션 무접촉). 종료 후 120초 대기(파이프라인 캐치업) → 대조 스크립트 자동 실행. 결과는 아래에 그대로 기록(결론이 안 나더라도 실험 설계·판정 기준·결과를 남기는 것 자체가 목적)
- 09-10 01:00 UTC 확인 사항(마켓 목록 변화): WS는 명시 코드 목록만 구독. producer·collector는 기동 시 1회 `/v1/market/all` 조회 후 갱신 없음 → 신규 상장은 재기동 전까지 미수집, 상장폐지는 무에러로 0건. 업비트 신호: ticker WS `market_state/is_trading_suspended/delisting_date/market_warning`, `/v1/market/all?is_details=true` market_event. **관찰 뒤 항목 추가**: (1) 수집기 5분 주기 목록 갱신 + 같은 연결에서 재구독(업비트는 최신 구독 메시지로 교체), (2) `markets` 상태 테이블(상장일·폐지일·경보) = 마켓 마스터 CDC 소스, 하류 자동 제외·신규 학습기간, (3) 품질 지표 "목록에 있으나 N시간 0건 마켓". 관찰 종료 시 목록 재조회로 287개 대비 변화 기록
- 01:10:22 UTC 세 방향 대조 결과(창 00:49:21~01:07:21, 독립 클라이언트 60,988건/282마켓): **REST = 독립 클라이언트 (8개 마켓 전부 100% 일치: BTC 2,984=2,984, XRP 2,187, VTHO 7,952, IOST 3,524, KAT 3,627, WAVES 3,003, BLAST 2,534)**, 파이프라인(ClickHouse=MySQL)은 BTC 2,678(**−306, 10.3%**), XRP −100, VTHO −151, IOST −58, KAT −29, WAVES −68, BLAST −58. 판정 기준 2번 해당 → **우리 producer 유실. "소스 전달 갭" 결론(분석 1) 철회**
- producer STATS 창 내 received 58,303 ≈ 독립 클라이언트 54,891(창 경계 차이) → 메시지는 다 받았고 **INSERT 단계에서 버려짐**(duplicates 1,991 증가)
- **근본 원인**: 업비트 `sequential_id`는 전역 유일이 아니라 **마켓별** 유일(= 체결 ms×10,000 + ms 내 순번). 서로 다른 마켓이 같은 ms에 체결하면 sid가 같음. 독립 클라이언트 데이터에서 60,988건 중 sid 중복 2,032건(3.3%) 전부 서로 다른 마켓. MySQL `UNIQUE KEY uk_seq_id (sequential_id)` + `INSERT IGNORE`가 뒤에 온 마켓의 체결을 "중복"으로 버림. 실증: BTC 누락 sid 17890014091030000/1/2가 MySQL에 **KRW-XRP** 행(00:50:10.626)으로 존재. 누락이 버스트 초(중앙값 104 체결/s)에 몰리는 이유 = 같은 ms 충돌 확률이 높아서
- 왜 지금까지 안 보였나: 5코인 시절 ms 충돌 확률이 낮아 거래량 기준 99%+로 가려짐(그래도 producer lifetime "duplicates" 5.3% 중 상당수가 이 충돌이었을 가능성). 287마켓에서 충돌이 급증해 건수 3.3%·BTC 10%로 드러남. 1차 producer STATS "duplicates 2.3%"를 INSERT IGNORE 정상 동작으로 기록한 것은 오판
- 교훈(기록): "소스 문제"로 결론내기 전에 **독립 수신기로 소스를 재현**해야 했음. 사용자의 더블체크 요구가 없었다면 유실을 소스 탓으로 문서화할 뻔함. 유일키 가정(sequential_id 전역 유일)은 문서로 확인된 적 없는 가정이었음
- **판단 실수 기록(사용자 지적으로 확인)**: (1) 1차(09-08 23:50) producer STATS "duplicates 2.3%"를 INSERT IGNORE 정상 동작으로 기록 — 실제로는 마켓 간 sequential_id 충돌로 버려진 체결. (2) 09-10 00:30 원장 대조에서 누락을 보고 독립 수신기로 소스를 재현하지 않은 채 "업비트 WS 미전달"로 결론(분석 1) — 09-10 01:10 세 방향 대조로 철회. 근본 가정 오류: 외부 ID(sequential_id)의 전역 유일성을 문서 확인 없이 스키마(UNIQUE)에 박음
- 01:19:47~01:20:19 UTC **인덱스 교체 실행(승인)**: `ALTER TABLE crypto_db.crypto_trades DROP INDEX uk_seq_id, ADD UNIQUE KEY uk_market_seq (market, sequential_id), ALGORITHM=INPLACE, LOCK=NONE` — 4,044,089행, 32초, 무중단(producer STATS errors 0·재연결 0). Debezium 스키마 변경 토픽에 DDL 기록, 커넥터 RUNNING. 원본 DDL backup/phase3/mysql-crypto_trades-ddl.pre-index-swap.txt. **관찰 기간 예외 조치 1건**(진행 중 유실 3%·BTC 10%)
- 백필 결정(승인): MySQL 경유(REST `/v1/trades/ticks` → 누락 (market, sid) INSERT → CDC → ClickHouse). 부작용 사전 명시: Flink 5분 집계·이상탐지는 processing-time이라 백필 행이 "현재" 창에 섞임 → trade_aggregations 오염(해당 창), 오래된 가격과 현재가 비교로 **PRICE_SPIKE 오탐 가능**(alert의 trade_id로 사후 식별 가능). 적재 지연 알림(p50>60s)은 백필 중 발동하므로 실행 동안 health_check pause. dbt OHLCV는 upbit_timestamp 기준이라 백필 행이 올바른 시간에 귀속됨
- 09-10 01:30 UTC 사용자 질문 "유실·중복·지연 외 DE가 놓쳤을 것" → 코드·설정 재점검으로 7건 등록(관찰 뒤 튜닝 목록, 근거 포함): (1) Kafka 파티션 키가 PK 해시라 마켓 내 순서 미보장 → AnomalyDetector 도착순 비교 오탐 가능 → 키를 market으로; (2) CdcEventParser가 누락 필드를 0.0/"UNKNOWN"으로 채움 → 조용한 손상 경로 → 필수 필드 누락은 실패/격리; (3) ClickHouse 가격·수량 Float64(MySQL은 DECIMAL) → 합계·VWAP 오차, 저가 코인 정밀도 → Decimal 전환; (4) "하루" 정의 불일치(dbt=upbit_ts KST, 품질·dedup 게이트=source_ts UTC, 파티션=source_ts) → 이벤트 시각·KST 통일; (5) 원장 대조가 수동 → REST 7일 창 안에 일일 자동 대조+자동 백필 필요(우선순위 1); (6) Debezium snapshot.mode=initial + 오프셋 유실 시 전체 재스냅샷 → 대량 중복 유입 리스크; (7) 무시된 행 지표 부재(오늘 사고 원인)·최소 권한(producer root, CH default, Kafka 무인증)
- 01:23:14~01:41:03 UTC 백필 dry-run(287마켓, 09-09 05:00 → 09-10 01:20 UTC, 읽기 전용, 1,069초): 원장 1,929,281건 vs MySQL 1,872,608건 → **누락 56,700건(2.94%)**. 마켓별: ETH 6,742(13.2%), BTC 6,432(5.8%), XRP 3,869(3.8%), USDT 2,824(5.3%), SOL 1,301(5.5%), TRUMP 1,175(3.9%). 고거래 마켓일수록 같은 ms 충돌이 잦아 손실률 높음. 결과 CSV out/backfill/backfill_20260910T012357Z_dry.csv
- 교훈 추가: 같은 REST 한도 그룹(`trades/ticks`, 10/s/IP)을 쓰는 작업 2개를 동시에 돌려 429 발생(검증 대조 실패) → 대조는 dry-run 종료 후 429 재시도 포함해 재실행. 원장 API 사용 작업은 직렬화·중앙 페이싱 필요
- 01:52:16 UTC **수정 검증 통과**: 인덱스 교체 후 독립 클라이언트 10분(01:23:01~01:33:01, 31,392건/275마켓) 세 방향 대조 — BTC 807=807=807, XRP 887, VTHO 2,474, BIRB 2,341, UP2 1,496, SOPH 1,865, BLAST 1,379 전부 일치, 초당 최대 105건 버스트 포함 **파이프라인 누락 0**. (첫 시도는 dry-run과의 REST 429 충돌, 두 번째는 대기 루프의 pgrep 자기 매칭으로 지연 — 절차 오류, 결과 무관)

## 유실 복구 백필 (09-10 01:52 UTC ~, MySQL 경유 CDC, 사용자 승인)
- 01:52:53 health_check pause. 기준선: anomaly_alerts 43,846(PRICE_SPIKE 93), MySQL 4,138,185행(max trade_id 98,080,670)
- 01:52:58~02:17:05 **287마켓 구간(09-09 05:00 → 09-10 01:20 UTC) 백필**: 원장 1,929,281 vs MySQL 1,872,608 → 누락 56,700 **전부 INSERT(56,700/56,700)**, 1,447초. 진행 중 producer errors 0·buffer 30, 02:01 시점 ClickHouse에 21,719건 이미 전파(CDC 경로 정상)
- 02:17:49~02:37:07 **5코인 구간(09-03 → 09-09 05:00 UTC) dry-run**: 원장 2,293,250 vs MySQL 2,146,849 → 누락 **146,413(6.38%)**. 5코인만으로도 BTC·XRP·ETH가 같은 ms에 체결하는 빈도가 높아 손실률이 287마켓 구간(2.94%)보다 오히려 큼 → "5코인 시절엔 가려졌다"는 건 거래량 기준 대조가 건수 손실을 숨긴 것이지 손실이 적었던 게 아님. 09-03 이전(2~8월)의 동일 손실은 원장 7일 창 밖이라 복구 불가 — 규모는 producer lifetime duplicates 3.99M/75.9M(5.3%)에서 추정
- 02:37:07 5코인 구간 INSERT 시작(146,413건, 500건 배치·0.5초 간격)
- 02:37:07~02:59:56 5코인 구간 백필: 실제 실행 시 누락 153,769(dry-run 146,413보다 +7,356: 그 사이 MySQL 7일 정리가 09-03 초반 행을 삭제) 전부 INSERT. 03:00 ClickHouse에 백필 행 210,469 = 56,700+153,769 전부 전파. 3잡 RUNNING, producer errors 0. health_check unpause 03:01
- **백필 실수 1건**: 5코인 창 하한을 09-03 00:00으로 잡아 MySQL이 이미 정리한(ClickHouse엔 있는) 09-03 00:00~02:55 행 53,012건을 재삽입 → ClickHouse 중복(trade_id만 다름). 03:03:37~59 lightweight DELETE로 백필 사본 제거 → 09-03 485,030 = uniqExact 485,030. 교훈: 백필 하한은 타깃 기준·소스 보존 경계 고려
- 백필 부작용(예고대로): 01:52~03:02 알림 1,687(PRICE_SPIKE 1,473) vs 전날 7 → 백필 행 연결 1,646건 DELETE(03:04), 잔여 123. trade_aggregations 해당 창 오염은 기록만. Oracle n8n이 그 시간 알림 발송
- 03:05 원장 재대조(09-09 15:00~09-10 03:00, 287마켓 3,393셀): **시간별 전부 100.0%, 95% 미만 0** → 수정+백필 완료 확인
- 문서 docs/13-sequential-id-collision-incident.md 작성. 스크립트 scripts/observe/{backfill_trades,trade_sid_probe}.py 리포에 복사
- 09-10 03:25 UTC README 이슈 7(sequential_id 충돌 유실 사고: 현상·위치 특정·오판과 정정·근본 원인·해결·복구·부작용·교훈) 추가, Phase 9 체크리스트에 예외 조치·관찰 뒤 1순위(reconcile DAG + dbt 테스트) 반영, docs 인덱스에 13번 추가
- 09-10 03:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 80.98% | cells 6813 | <99% 559 | <95% 557 | worst hour 2026-09-09T03 0.0% | fetch_fail 0 | /home/calme/pipeline-observation/reconcile_2026-09-10_03Z.csv
- 09-10 03:35 UTC 일일 원장 대조 cron 등록(`35 6 * * *`, `scripts/observe/daily_reconcile.sh`, 읽기 전용·백필 없음, 06:20 일일 요약과 직렬). 첫 수동 실행(24h 창 09-09 03:00~09-10 03:00) 가중 80.98%는 **확장 전 2시간(03~04 UTC, 5코인만 수집)이 창에 포함된 기간 효과** — 확장 이후 시간대만 보면 아래 검증값. 09-11부터는 창 전체가 287마켓 구간
  - 검증값: 확장 전 2시간은 5코인 셀만 100.0%(나머지 마켓은 수집 전이라 0), 확장 후(09-09 05:00~09-10 02:00) 6,245셀 가중 **100.0%**, 99% 미만 2셀·95% 미만 0
- 09-10 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H1 체결 lag p95≥5s 3연속', 'H10 알림>2000/일', 'H11 MySQL 행수 증가'], 체결 lag p95 max 614116.92s, 호가 e2e p95 max 1984.0ms, buffer max 104.0, 알림 2084, ClickHouse max 0.71GiB → /home/calme/pipeline-observation/digest_2026-09-10.md
- 09-10 06:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 100.0% | cells 6816 | <99% 2 | <95% 0 | worst hour 2026-09-09T06 100.0% | fetch_fail 0 | /home/calme/pipeline-observation/reconcile_2026-09-10_06Z.csv
- 09-11 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H11 MySQL 행수 증가'], 체결 lag p95 max 3.15s, 호가 e2e p95 max 1992.0ms, buffer max 397.0, 알림 465, ClickHouse max 0.62GiB → /home/calme/pipeline-observation/digest_2026-09-11.md
- 09-11 06:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 100.0% | cells 6756 | <99% 0 | <95% 0 | worst hour 2026-09-10T06 100.0% | fetch_fail 0 | /home/calme/pipeline-observation/reconcile_2026-09-11_06Z.csv
- 09-11 12:20 UTC 상태 점검(관찰 2.3일): 3잡 RUNNING·체크포인트 3,257/0 실패, producer errors 0·duplicates 정지(56,806), 수집기 재연결 1회(09-10 19:17, 공백 없음), 일일 원장 대조 09-10 100.0%(99% 미만 2셀)·09-11 100.0%(0셀), Kafka 합 40.9GB(체결 파티션 1.92GB < 4GB cap, 최고 세그먼트 09-08 19:00 → 72h 보존 유지), df 108G
- **추가로 놓친 것 3건(사용자 "냉철하게" 요청)**: (8) **백업 없음** — MySQL(버퍼)·ClickHouse(92M행·200일 이력) 모두 스냅샷/덤프 없음. 단일 NVMe 장애 = 전량 유실. 관찰 뒤 ClickHouse `BACKUP` 또는 파티션 단위 export 정례화 필요(우선순위 상). (9) **알림 채널 포화** — 287마켓 후 알림 473건/일, 하루 1,440분 중 270분(19%)에 n8n이 Slack/Gmail 발송 → 사실상 노이즈, 임계값 재조정 전까지 알림의 신호 가치 없음(USDT 등). (10) **컨테이너 로그 로테이션 미확인** — json-file 드라이버, daemon.json log-opts 확인 불가(sudo). 200일 컨테이너 로그가 무제한이면 디스크 잠식 가능. sudo로 확인 필요
- 09-12 06:20 UTC 관찰 일일 요약(자동): 샘플 288, 위반 ['H11 MySQL 행수 증가'], 체결 lag p95 max 3.06s, 호가 e2e p95 max 1999.0ms, buffer max 493.0, 알림 394, ClickHouse max 0.6GiB → /home/calme/pipeline-observation/digest_2026-09-12.md
- 09-12 06:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 100.0% | cells 6779 | <99% 0 | <95% 0 | worst hour 2026-09-11T06 100.0% | fetch_fail 0 | /home/calme/pipeline-observation/reconcile_2026-09-12_06Z.csv
- 09-13 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H11 MySQL 행수 증가'], 체결 lag p95 max 3.78s, 호가 e2e p95 max 2005.0ms, buffer max 188.0, 알림 333, ClickHouse max 0.61GiB → /home/calme/pipeline-observation/digest_2026-09-13.md
- 09-13 06:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 100.0% | cells 6733 | <99% 0 | <95% 0 | worst hour 2026-09-12T06 100.0% | fetch_fail 0 | /home/calme/pipeline-observation/reconcile_2026-09-13_06Z.csv
- 09-14 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H11 MySQL 행수 증가'], 체결 lag p95 max 3.1s, 호가 e2e p95 max 1991.0ms, buffer max 243.0, 알림 1433, ClickHouse max 0.71GiB → /home/calme/pipeline-observation/digest_2026-09-14.md
- 09-14 06:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 100.0% | cells 6789 | <99% 0 | <95% 0 | worst hour 2026-09-13T06 100.0% | fetch_fail 1 | /home/calme/pipeline-observation/reconcile_2026-09-14_06Z.csv
- 09-15 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H11 MySQL 행수 증가'], 체결 lag p95 max 2.74s, 호가 e2e p95 max 2004.0ms, buffer max 179.0, 알림 470, ClickHouse max 0.73GiB → /home/calme/pipeline-observation/digest_2026-09-15.md
- 09-15 06:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 100.0% | cells 6813 | <99% 0 | <95% 0 | worst hour 2026-09-14T06 100.0% | fetch_fail 0 | /home/calme/pipeline-observation/reconcile_2026-09-15_06Z.csv
- 09-15 11:23 UTC 상태 점검(관찰 6.2일): 3잡 RUNNING, 체크포인트 8,965/8,964 완료·실패 0, producer errors 0·duplicates 56,806 정지(6일간 +0), 수집기 198 msg/s·발행 실패 0·재연결 누적 2, health_check success, 일일 원장 대조 09-12~09-15 **4일 연속 100.0%(99% 미만 0셀)**, 일일 요약 위반은 H11(MySQL 증가)만 — MySQL 14,896,141행·min created_at 09-08 11:15 = 7일 정리가 287마켓 행에 도달해 예측한 약 1,400만에서 수렴 중. ClickHouse crypto_trades 3.45GiB·orderbook_raw 4.21GiB(52파트, 09-09 03:47~)·orderbook_1m 210MiB. Kafka 합 43.7GB(플래토), df 108G(09-11과 동일 → 플래토 확인). 관찰 종료 예정 09-16 06:10 UTC
- 09-15 23:47 UTC (09-16 08:47 KST) 절차 오류 1건: 시각 대기 루프를 `HHMM` 문자열 비교로 짜서 자정 넘김(2347 < 0500 판정 실패)에 즉시 실행됨 — TTL 머지 관찰과 코퍼스 녹화 잡 둘 다. TTL 관찰은 epoch 비교로 재기동(05:00 UTC 대기). 코퍼스 녹화는 이미 실행 중이라 완료시켜 "09-15 하루치(23:47 기준)" 코퍼스로 보관하고, 관찰 종료 시점(06:12 UTC) 녹화를 별도 파일명으로 다시 예약. 교훈: 자정을 넘기는 대기는 epoch 기준으로
- 관찰 종료까지 남은 시간 계산 실수: 09-15 11:23 스냅샷 기준으로 "18.5시간"이라 답했으나 실제 시각은 09-15 23:47 UTC → 잔여 6.4시간(사용자 지적 정확). 06:10 UTC(15:10 KST) 종료 후 docs/14 분석·코퍼스 고정 예정
- 09-16 00:05 UTC 블로그 원고 대비 기록 점검: docs/08에 "교훈" 절 추가(현상·원인·판정·해결·검증·교훈 완비), README 이슈 1~7 순서 정렬, `docs/journey-index.md` 신설(에피소드 12건 × 한 줄·핵심 수치·실수·근거 문서, 3편 제안, 인용 주의). 사고 문서 2건(08·13) 모두 실수·철회 기록 유지
- 09-15 23:47~09-16 00:03 UTC 재생 코퍼스 1차 녹화(조기 실행분, 읽기 전용): 체결 create 24h(09-14 23:47~09-15 23:47) **2,461,293건**(delete 356,865건 제외, raw 2.36GB → gz 146MB, 192초), 호가 6h(09-15 17:47~23:47) **5,602,029건**(raw 7.0GB → gz 839MB, 751초). SHA256 기록(trades 203a28e0…, orderbook a4d557f1…). 임시 컨슈머 그룹 잔존 0. 관찰 종료 시점(06:12 UTC) 2차 녹화는 별도 예약 유지 → 둘 중 피크가 뚜렷한 쪽을 실험 코퍼스로 고정
- 09-16 00:20 UTC 일일 처리량 실측(관찰 4일 완결일, UTC 일 기준): 체결 09-12(토) 1,557,393 · 09-13(일) 3,199,277 · 09-14(월) 2,507,143 · 09-15(화) 2,459,444(287마켓). 호가 스냅샷 14.3M(166/s) · 17.6M(204/s) · 18.6M(215/s) · 19.7M(229/s). ClickHouse orderbook_raw 일 파티션 630~729MiB, 체결 Kafka 토픽 논리 2.53GB/일(RF3 디스크 7.6GB/일, create+delete 포함). 총 이벤트 약 1,700만~2,300만/일, 평균 200~270/s
- 09-16 00:35 UTC 부하 실험 방식 조사(웹): 실무 표준은 (a) 프로덕션 트래픽 녹화-재생(결정적 출력 비교), (b) 부하 생성기(Gatling Kafka 플러그인, NBomber, kafka-producer-perf-test, OpenMessaging Benchmark)로 브로커 상한 측정, (c) Nexmark(Flink SQL 벤치마크, 우리 잡엔 부적합). 준비생 포트폴리오는 대개 Faker 생성기·공개 데이터셋(택시 등)이며 실트래픽 재생·포화 실측은 드묾. **규모 보강안(제안, 미확정)**: Binance 공개 데이터(data.binance.vision, 심볼별 일/월 trades zip, 무료)로 실체결 코퍼스를 10~50배 규모로 확보해 load_test 토픽에서 재생 → "가짜"가 아닌 실데이터로 초당 수천 건 실험 가능. 분석 테이블에는 넣지 않고 부하 실험 전용. 3계층 계획: ① 자체 Upbit 코퍼스 재생(행동 충실도) ② Binance 코퍼스 재생(규모) ③ kafka-producer-perf-test(브로커 단독 상한, 병목 분리)
- 09-16 01:00 UTC `docs/15-load-experiment-design.md` 작성(준확정): 재생 근거(실측), 3계층(Upbit 코퍼스/Binance 코퍼스/브로커 단독)+S3 장애+S4 전후, Binance 사양(정적 파일·키 없음·크기·컬럼·어댑터 매핑·심볼 선택·격리), 자원 격리, 측정·판정·성공 기준 4개, 순서. README Phase 9 체크리스트·docs 인덱스 반영
- 09-16 01:59 UTC 세션 백그라운드 대기 잡 2개(TTL 관찰·코퍼스 2차 녹화)가 에이전트 하네스의 메모리 보호로 종료됨(호스트 실측은 used 7.9GB·avail 7.8GB로 여유, 파이프라인 무영향). 시각 대기는 세션 대신 **호스트 cron 1회성**(05:05 TTL 관찰, 06:12 코퍼스 녹화, 실행 후 자기 제거)으로 재등록. 교훈: 수 시간 대기는 세션 프로세스가 아니라 cron/at로
- 09-16 02:20 UTC 블로그 초안 2편 작성(미커밋, 사용자 다듬기 전): `blog-post-6.md` [1편] 사전 검증→8월 적재 지연 사고(3,870자), `blog-post-7.md` [2편] 확장 실행→sid 충돌 유실→백필(4,315자). 스타일 메모리(차분한 ~습니다체, 볼드 강조, 직전 맥락 도입, 소제목 6~7개, 표 사용) 적용. 수치는 docs/08·09·10·11·13 기록값만 사용. 3편은 docs/14 이후
- 09-16 02:35 UTC 블로그 초안 추가: `blog-post-dedup-audit.md` [중복 적재 감사] (docs/07 기록값만 사용: 91,061,227행·중복 1,641,600(1.80%)·2월 99.98%·샘플 trade_id 5285572 8중복 타임라인·3월 이후 373건 sink 재시도·감사 당일 13건·trade_id 재사용·Phase 2/3-A/3-B·검증 2회). 시리즈상 [1편] 앞에 위치
- 09-16 02:50 UTC 블로그 [4편] 초안 `blog-post-9.md`(결정 편: 단일 호스트 3브로커·CDC 정체성·백업 부재·관찰 원칙·재생 실험). 시리즈 재편: [중복 감사]·[1]·[2]·[3 관찰 결과, docs/14 후]·[4 결정]·[5 실험 결과]. 초안은 gitignore로 리포 미포함
- 09-16 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H2 producer buffer>1000 또는 WARNING', 'H11 MySQL 행수 증가'], 체결 lag p95 max 2.84s, 호가 e2e p95 max 1984.0ms, buffer max 253.0, 알림 588, ClickHouse max 0.66GiB → /home/calme/pipeline-observation/digest_2026-09-16.md
- 09-16 06:35 UTC 일일 원장 대조(자동, 백필 없음): weighted 99.72% | cells 6819 | <99% 8 | <95% 4 | worst hour 2026-09-15T20 97.04% | fetch_fail 0 | /home/calme/pipeline-observation/reconcile_2026-09-16_06Z.csv

## 7일 관찰 종료 (09-16 06:10 UTC) → docs/14-observation-week1.md
- 2,016샘플 결측 0, 3잡 100% RUNNING, 체크포인트 실패 0. 가설 12개: 통과 10 / 조건부 1(H5, 수정 전 유실 → 수정 후 6일 100%) / 위반 1(H10 알림 편중, 상위 5마켓 62%, 스테이블코인 26%)
- 백필 창(09-10 01:50~03:10 UTC) 지표는 왜곡(백필 행 체결시각 최대 7일 전 → lag 61만 초, 알림 1,687)이라 지연 통계에서 제외하고 그 사실을 문서에 명시
- 핵심 사실: 자원 포화 없음(버퍼 ≤493, 백프레셔 0, CH ≤0.73GiB), 지연은 배치 창이 결정(09시 피크 p95 2.05s < 새벽 2.4s), 호가 e2e p95 1.95~2.0s 일정. TTL 머지 1회 10.08M행 12.8초·65MiB → `ttl_only_drop_parts` 불필요로 결정 수정. MySQL 과도기(삭제 백로그 3,205행). df +35.7GB 중 Kafka 보존 +30GB
- 튜닝 순위 제안 8개(1순위 이상탐지 임계·알림 요약, 2순위 대조 DAG·dbt 계약, 3순위 백업, 4순위 파티션 키 market …). 적용은 사용자 결정 전 없음
- 코퍼스 2세트 SHA256 고정. obs_end 호가(KST 09:12~15:12, 09시 피크 포함)를 실험 기본 후보로
- 09-16 07:30 **정정**: docs/14 §5의 EURC 설명("호가 단위") 오류. 실측은 얇은 호가창의 bid/ask 왕복(스프레드 p90 3.09%, max 24.3%). 직전 체결 대비 규칙 자체의 비교 기준 문제. 7일 오프라인 시뮬레이션: PRICE_SPIKE 3% 146/일 → 스프레드<1% 조건 시 20/일(69→37마켓); mid-price 기준은 371/일로 오히려 증가(얇은 창에서 체결가가 mid에서 멀어짐). LARGE_TRADE 1억 고정 107/일·23마켓, 마켓별 p99.99는 233/일. VOLUME_SURGE BTC 알림 중앙값 1,230만 원 체결(수량 EMA 기준의 한계). 판단 오류 기록: 분포를 보기 전에 원인을 적었음
- 09-16 08:10 **이상탐지 기준 재검토(사실 확인, 결정 전)**: ① 업비트 이상거래 감시정책 PDF(static.upbit.com/guide/market_surveillance_policy.pdf, 7p)는 이상거래를 "가격이나 거래량이 비정상적으로 변동하는 경우" 등으로 정의하고 모니터링 7유형(가장·통정성매매, 허수성매매, 취소·정정주문과다, 특정종목매매집중주문, 체결관여과다, 주문관여과다, 시세관여과다)을 열거. 7유형 모두 계정·주문 단위 관여율 기반 → **공개 체결·호가 데이터로는 계산 불가**. 현 AnomalyDetector 주석의 "시세관여·체결관여·매매집중 대응"은 근거 없음. ② 업비트 시장경보제도(주의 종목) 5유형: 가격 급등락(24시간 전 종가 대비, 2026-05-18 기준 변경), 거래량 급등, 입금량 급등, 가격 차이(글로벌 시세, CoinMarketCap 기준), 소수 계정 집중. 수치 임계는 비공개(고객센터 페이지는 Cloudflare 차단, 공지에는 수치 없음). ③ REST `GET /v1/market/all?is_details=true`가 `market_event.warning`과 5개 `caution` 불리언을 마켓별로 반환 → 거래소 지정 결과가 **라벨(ground truth)**로 수집 가능. 09-16 08:00 UTC 시점 KRW 287마켓 중 28개 플래그(warning: RVN·MANTRA·SAND·INJ 등, caution: 거래량/입금량/글로벌가격차 조합). 이력 API 없음 → 지금부터 폴링해야 라벨이 쌓임
- 09-16 09:45 **라벨 폴러 가동(사용자 "일단해봐" 승인)**: `scripts/labels/poll_market_events.py` cron 1분, `cdc_pipeline.upbit_market_events`(DDL `clickhouse/market_events.sql`). 최초 스냅샷 37플래그(TRADING_VOLUME_SOARING 16, DEPOSIT 13, WARNING 6, GLOBAL_PRICE 1, CONCENTRATION 1). 관찰 종료 후 첫 변경이며 파이프라인 구성요소는 건드리지 않음(외부 읽기 폴링 + 신규 테이블)
- 09-16 09:50 **기존 데이터에 라벨이 있었나**: 호가 프로브(09-08 22:26 UTC)가 `is_details=true` 스냅샷 1장을 저장해 둠(`~/cdc-orderbook-probe/out/market_all.json`). 현재와 diff: 지정 ≥30 / 해제 ≥36 (7.5일, 창 안에서 켜졌다 꺼진 건 안 보이므로 하한). 유형별 신규 지정: TRADING_VOLUME_SOARING 16, DEPOSIT 12, GLOBAL_PRICE 1, CONCENTRATION 1, PRICE_FLUCTUATIONS 0
- 09-16 09:55 **첫 백테스트(마켓 단위, 시각 없음)**: 피처 = UTC 일 거래대금 max/median(09-10~15, 6일). 지정 16마켓 p50 8.4 vs 나머지 270 p50 2.2. 임계 4: TP 15 / FP 71 / FN 1. FP가 큰 이유 두 가지 후보: ① 09-15에 시장 전반 거래량 급증(16개 중 14개 피크가 09-15) ② 창 안에서 지정→해제된 마켓이 음성으로 잡힘(라벨 노이즈). → 분 단위 전이 라벨이 필요한 이유가 실측으로 확인됨
- 09-16 10:00 **기간 단축 수단**: 업비트 공지 API(`api-manager.upbit.com/api/v1/announcements`, 20건/페이지)에서 1,500건(2025-02-27~) 스캔 → "거래 유의 종목 지정/해제/연장" 공지 53건(WARNING 라벨, 일 단위 시각). REST 일봉 200일이면 287마켓 피처를 소급 계산 가능(287콜). 라이브 1분 라벨은 선행 시간 측정에만 필요
- 09-16 10:05 **정정**: 위 "거래 유의 종목" 공지 53건은 라벨로 **못 쓴다**. 본문 5건(INJ·RVN·STORJ·ZIL·SAND) 확인 결과 지정 사유가 전부 해킹 등 보안사고(시행령 제17조 제1호 마목) 또는 공시·사업 실재성 미진이며 가격·거래량과 무관. 우리 규칙의 정답은 자동 지정되는 caution 5종뿐이고 그 이력은 09-08 스냅샷 1장 + 09-16 09:45부터의 폴링이 전부. 폴러 첫 전이 09:46 관측(1건)
- 09-16 10:40 **철회·발견**: "주의 경보 이력은 어디에도 없다"(09-16 10:05)는 **틀렸다**. 사용자가 "지난번 업비트 탓도 결국 우리 문제였다"며 재확인을 요구 → 탐색 범위를 넓힘. 결과: ① Wayback(45회 캡처)은 전부 `is_details` 없이 저장돼 무용, 텔레그램 공지봇·깃허브 무용 ② 업비트 웹 번들에서 `/service_center/market_warning_history`(시장경보 현황) 페이지와 그 데이터 API **`crix-api-cdn.upbit.com/v1/crix/market-event-records`**(params: quoteCurrencyCode, start, end ≤30일, page, size ≤200, warningLevel, eventType) 발견 ③ 이력 2026-03-20~ 6개월, KRW **15,588 에피소드**(지정·해제 시각 초 단위, 유형, 등급 1~3). 유형별: GLOBAL_PRICE 10,546(중앙값 3분 지속, 분 단위 재평가) / TRADING_VOLUME 2,189 / PRICE_FLUCTUATIONS 1,538(중앙값 4분) / DEPOSIT 1,272 / CONCENTRATION 43. 거래량·입금·소수계정은 매일 01:00 UTC(10:00 KST) 일괄 지정·24h 지속 ④ 공식 기준 문서(Zendesk API로 취득 `upbitcs.zendesk.com/api/v2/help_center/ko/articles/900005994766.json`, 2026-09-01 갱신): 자동 지정, 가격 급등락은 24시간 전 종가 대비(05-18~), 글로벌 가격 차이 ±3~10%는 표시만·지정 안 함(→ 지정은 ±10% 이상으로 추정), 신규 상장 96h 제외, 예외 조건 비공개 ⑤ 관찰 주간만 PRICE_FLUCTUATIONS 276(16마켓)·TRADING_VOLUME 128(106마켓) → **라벨 대기 2~4주 불필요**, 즉시 백테스트. 판단 오류 기록: 부재를 단정하기 전에 웹 클라이언트가 쓰는 API를 확인하지 않았음
- 09-16 10:45 적재: `clickhouse/market_event_records.sql`(ReplacingMergeTree, 에피소드당 1행), `scripts/labels/fetch_market_event_records.py`(30일 창·200건 페이지·0.3초 간격, 증분은 최근 2일) cron 매시 07분. 1분 폴러는 교차검증용으로 1주 유지 후 정리 판단
- 09-16 11:30 **백테스트(docs/16)**: ① PRICE_FLUCTUATIONS 관찰 주간 226 에피소드의 지정 시각 |24h 변동|: 주의 min 46.2%·p50 51.2%, 경고 min 97.4%·p50 100.9% → 임계 ±50%/±100%. 역검증: ≥50%인 분 3,221개 중 3,220개가 업비트 에피소드 안 ② TRADING_VOLUME_SOARING 6개월 1,981 라벨: 7일 평균 대비 ×4 + 하한 10억 → 정밀도 0.79 / 재현율 0.80 (30일 평균은 0.57/0.60) ③ 입금·소수계정 계산 불가, 글로벌은 Binance 참조 후. 일봉 200일 `upbit_daily_candles`(`scripts/analysis/fetch_daily_candles.py`, 287콜). 제안 규칙 3개(PRICE_24H, VOLUME_24H, EXCHANGE_FLAG) + 기존 3규칙 삭제 — 적용은 사용자 결정 대기
- 09-16 11:40 **교훈(사용자 확인)**: "제대로 찾으니까 나오잖아". 두 번의 같은 실수(09-10 소스 탓, 09-16 이력 없음) → 원칙 추가: 부재·외부 귀책 결론은 클라이언트 번들의 API 경로, 우회 API(Zendesk 등), Wayback CDX, 공지 API까지 본 뒤에만. 발견 경로는 docs/16 §3에 기록. journey-index 에피소드 후보
- 09-16 12:05 **백테스트 자체 감사**(사용자: "정말 제대로 한 게 맞나", "가변 기준일 수도"): ① 관찰 주간 226건은 14마켓·19마켓-일 → 독립 표본 부족 인정 ② 6개월 (마켓,일,등급)당 1건 167건을 업비트 1분봉으로 독립 재계산: 주의 p50 51.7%(45~55% 104/145), 경고 p50 101.2%, 위험 196.9% ③ 월별 p50 51.5~52.7% 일정 = 정적 기준. 05-18 이전만 45% 미만 7/37, 이후 1/108 → 공식 문서의 참조 변경(전일 종가→24h 전)과 일치 ④ 거래량: 라벨 날짜를 ±1일 틀리게 붙이면 정밀도 0.06~0.13으로 붕괴 → 정렬이 맞음. 수량 기준 0.73/0.79 ≈ 금액 기준, 중앙값 기준은 나쁨 ⑤ 재현율: 주의 에피소드 분의 91.1%가 ≥50%, 99.0%가 ≥46%. docs/16 §4 갱신
- 09-16 12:35 **최종 재검사 + 부록**(사용자: "어떤 코드·쿼리로 결론을 얻었는지까지 적어라"): 결론에 쓴 쿼리 10개를 `scripts/analysis/rule_basis_check.sh`로 모아 처음부터 재실행 → docs/16 숫자와 전부 일치(GLOBAL 3건 증분 제외). 출력 원문 `~/pipeline-observation/rule_basis_check_20260916.txt`, 사고 과정 순서·입력 정의·쿼리 원문·결과를 `docs/16-appendix-queries.md`에 수록. `verify_price_threshold.py --report` 모드 추가(저장 JSON 재집계, REST 재호출 없음)
- 09-16 11:50 **reconcile_trades DAG + dbt 계약 착수**(사용자 승인: "1층을 더 뻗어나가서 dbt랑 airflow 먼저", 기준은 "현업 관행이거나 왜인지 한 문장으로 답할 수 있을 것"). 설계 결정 12건은 docs/17 결정표. 핵심: Airflow=EL·순서·알림 / dbt=계산·판정, 참조값(시간봉)을 테이블로 보관, 판정은 dbt 싱귤러 테스트, REST 는 Airflow pool 1슬롯으로 직렬화, Slack 은 실패 시에만
- 09-16 11:52 **DAG 첫 실행이 실제 유실 2건을 잡음**(대상일 09-15 + 09-09~14 백필). ① **KRW-BFC 6일 무수집**: 09-10 상장, producer·수집기는 기동 시 마켓 목록을 1회만 조회 → 신규 상장 미구독. 체결·호가 0건. 관찰 기간 cron 대조는 마켓 목록을 ClickHouse 에서 가져와 **구조적으로 탐지 불가**했음(= "6일 연속 100.0%"는 이 마켓 제외 값). 거래소 목록을 기준으로 삼는 커버리지 테스트가 잡음 ② **재연결 5초 구멍**: 09-15 20:47:00 WS 끊김 → 20:47:05 재연결, 그 사이 체결 누락. TIA 20시 셀 50.6%, REST 틱 대조로 20:47:02 체결 2건(2,280+20 TIA) 확인. 같은 시각 8마켓 89~97%. docs/14 의 미조사 WARNING 1건의 정체
- 09-16 11:55 부수 수정: daily_pipeline 의 '하루' 정의를 KST 체결시각으로 통일(종전 `toDate(source_ts)` = UTC 적재시각 → KST 리포트가 9시간 어긋난 하루를 집계), `dbt source freshness` 선행 태스크 추가, `schedule_interval`→`schedule`. DAG 구조 테스트 2건이 실제 DAG 와 달라 실패 상태였음(check_ingest_lag 누락, slack 의존성 오기) → 수정, 컨테이너에서 10/10 통과. Dockerfile 에 pytest 추가(다음 빌드 반영). cron `daily_reconcile.sh` 비활성화(DAG 와 같은 06:35, REST 충돌)
- 09-16 11:56 **미결(사용자 결정 필요)**: BFC 백필은 REST 7일 창이라 **09-17 까지**가 기한. 20:47 구간 백필. producer·수집기의 마켓 목록 주기 갱신 + 재연결 gap-fill(코드 변경·재기동)
- 09-16 11:07 **BFC 백필 완료**(사용자 승인 "백필하면 저장된다는거지? 거기까지 일단 진행해"): REST 원장 261,713건 = 누락 100%(MySQL 0건), MySQL INSERT → CDC 경유로 ClickHouse 261,713건 전부 도착(11:06:57 확인). 소요 462초. 실시간 지연 영향 없음(백필 중 다른 마켓 p50 1.2초). 상장 시각 09-10 04:00 UTC, REST 7일 창 안이라 전 구간 복구
- 09-16 11:10 백필 부산물: BFC 알림 138건(PRICE_SPIKE 134·LARGE_TRADE 3·VOLUME_SURGE 1) 발생. **09-10 백필과 성격이 다름** — 그때는 7일 전 가격과 현재가를 비교한 무효 비교였고, 이번은 단일 마켓을 시간순 삽입해 연속 체결 비교 자체는 유효. 다만 detected_at 이 오늘이라 mart_alert_rate 의 날짜 축을 오염시킴 → `~/pipeline-observation/removed_alerts_bfc_backfill_20260916.csv` 로 보존 후 삭제(138건, 되돌릴 수 있게 CSV 우선)
- 09-16 11:21 **마켓 목록 주기 갱신 구현·배포**(사용자: "신규상장코인도 받아올수있게 구성한다고했던것같은데"). 09-10 worklog 에 "관찰 뒤 항목"으로 적어둔 (1) 5분 주기 갱신·재구독을 구현. **선행 검증**: 같은 WS 연결에서 구독 메시지를 재전송하면 구독이 교체되는지 프로브로 실측 — 1마켓 → 3마켓(확장) → 1마켓(축소) 모두 반영, 연결 유지, 수신 공백 없음. 기록에만 있던 주장을 처음으로 실측함
  - producer/collector 공통: `MARKET_REFRESH_SEC=300`, 같은 연결에서 재구독, 변경 시 추가·제외 마켓을 WARNING 으로 로그. 안전장치 = 조회 실패나 100개 미만 응답이면 기존 목록 유지(장애 시 구독 축소 방지), 명시 `MARKETS` 지정 시 갱신하지 않음(사용자 의도 존중). REST 부담 5분당 1회
  - 배포 공백: producer 11:20:49.1→11:20:51(2초), collector 11:21:00.2→11:21:01.6(1.4초). 체결 공백분은 백필 대상에 포함
  - 검증: 11:22:46 BFC 실시간 체결이 sequential_id 17895577664810000 으로 REST 원장과 일치, 호가도 11:21:06 부터 수신. 6일 만에 정상화
- 09-16 11:23 **재연결 구멍 백필 실행**: 09-15 20:46:45~20:47:30 창, 287마켓. dry-run 결과 REST 996 / MySQL 836 / 누락 **160건(16.06%)** — 5초 재연결이 45초 창의 16%를 날린 것이 수치로 확인됨
- 09-16 11:40 **재연결 구멍 백필 완료**: 160건 전량 INSERT. 대조 재실행 결과 09-15 dbt 테스트 7개 전부 PASS(직전에는 유실·커버리지 2개 FAIL). 주간 재집계: 09-10~15 가중 비율 **100.000%**, 99% 미만 셀 0, 0건 셀 0 (09-09 67%는 유니크키 수정 이전이라 예상된 값, docs/13)
- 09-16 11:45 **새 발견 — 백필이 Flink 스트림 상태를 오염시킨다**(docs/17 §4-4). 20:47 백필 직후 알림 10건을 체결 시각으로 가르니 정확히 반반: 백필된 옛 체결이 현재가와 비교된 것 5건 + 직후 실시간 체결이 백필된 옛 가격과 비교돼 되튄 것 5건. 원인은 AnomalyDetector 의 마켓별 `lastPrice` keyed state 가 백필 행으로 덮이는 것. **한 번의 백필이 두 번 오탐**. 09-10 백필의 1,687건도 같은 기전이었으나 당시엔 특정하지 못했음. BFC 백필에서 안 보인 이유는 그 마켓이 애초에 미구독이라 끼어들 실시간 스트림이 없었기 때문 → 구독 중 마켓 백필에서만 발생. 10건 CSV 보존 후 삭제
- 09-16 11:46 위 발견의 귀결: 재연결 gap-fill 을 그대로 자동화하면 메울 때마다 알림이 두 배로 튄다. 선택지 (a) is_backfill 플래그 (b) 이벤트 시각이 N분 이상 늦으면 알림 생략 (c) 별도 토픽·잡 분리. (b) 권장 — 가장 단순하고 지연 유입 일반에 적용됨. 사용자 결정 대기
- 09-16 11:47 **재배포 보류 결정**: 로그 축약(미용 목적) 커밋은 빌드만 해두고 재기동하지 않음. 근거 = 재기동 1회가 2초 공백 = 287마켓 약 50건 손실이고, 같은 날 "5초 공백 = 45초 창의 16%"를 실측한 직후다. 다음 기능 변경(gap-fill 등) 배포 시 함께 반영. 현재 배포본과 저장소 차이는 로그 포맷 한 줄뿐
- 09-16 12:00 **세션 총괄 기록**(사용자: "지금 상황 싹다 기록해, 발견한 모든 걸"). `docs/18-findings-and-lessons.md` 신설 — ① 데이터로 뒤집힌 결론 3건(지표 100%인데 데이터 틀림 / 백필의 스트림 상태 오염 / 이상탐지 기준 무근거) ② 자기 감사 6항목(표본 독립성·1분봉 독립 검증·가변 기준 가설·라벨 정렬 민감도·재현율·재실행) ③ 부재 단정 실수 2회와 명문화한 원칙 ④ 관찰이 알려 준 것 5가지 ⑤ 판단 정정 4건 ⑥ 산출물 ⑦ **DE 관점 냉정한 평가(강한 신호 5 / 약한 부분 3)** ⑧ 결정 대기 4건. README 에 이슈 8 추가·문서 색인 갱신, journey-index 에 블로그 에피소드 14~16 추가(5~7편 후보)
- 09-16 12:20 **재기동 공백 백필 완료**: 09-16 11:20:40~11:21:10(30초) 287마켓, REST 525 / 보유 503 / 누락 **22건(4.19%)** 전량 복구. 2초 공백이라 20:47 건(5초, 16.06%)보다 작음 — 공백 길이와 손실률이 함께 커지는 것 확인
- 09-16 12:20 **상태 오염 기전 3회째 재현**: 이번 백필 산물은 KRW-LSK 2건(백필 체결 11:20:50 "급락 529→498" + 직후 실시간 체결 11:46:24 "급등 498→530")으로 정확히 한 쌍. 나머지 알림 9건은 실시간 정상 탐지(LARGE_TRADE·VOLUME_SURGE). 누적 관측: 09-10 1,687건 / 20:47 백필 10건(5쌍) / 재기동 백필 2건(1쌍) → **백필 1건이 오탐 2건**이라는 규칙성이 세 번 확인됨. 2건 CSV 추가 보존 후 삭제
- 09-16 12:50 **docs/19 신설 — 유실 방지 구조 현황표를 작업 기준으로 승격**(사용자: "우리의 작업사항으로 따라가야 하니 저장"). 14항목: 있음 5 / 절반 2 / 없음 7. 없음 7개 중 5개(수신 공백·늦은 이벤트·실시간 감지·배포 공백·수리 계보)가 배포 1 하나로 해결, 나머지 2개(체결 Kafka 선기록·백업)는 별도. 다음 단계 8순서 확정: 배포 1 → 1주 대조 → 백업 → 이상탐지 섀도 → 부하 실험 → 브로커 축소 → CDC 재검토 → RMT. 갱신 규칙: "있음"은 실측 근거 있을 때만. 실무 논의 요지 기록: REST 백필은 이 소스의 혜택이지 역량이 아님. 어필할 것은 내구 경계 설계·멱등 소비자·측정 가능한 완전성·늦은 이벤트 처리·수리 계보
- 09-16 22:24~22:50 **배포 1 실행**(사용자 승인 "진행해, 왜에 납득할 기준으로"). 상세 docs/20.
  - **설계 변경 1(실측 근거)**: 늦은 이벤트 가드를 "단조 순서"에서 "적재 지연 > 60초"로. 구현 전 측정: 같은 마켓 체결의 5.87%가 이벤트 시각 역순 도착(p99 2.3초, 최대 4.8초, 3파티션 병렬 소비). 단조 가드는 실시간 6%를 탐지에서 뺀다 → 폐기. 60초 = 정상 최대 7.75초의 8배 = health_check 의 기존 "실시간 아님" 기준
  - **설계 변경 2**: 겹침 배포 → 기동 시 gap-fill(30분 상한). compose container_name 제약, 같은 결과
  - Flink: 가드 + 메트릭 + 하네스 테스트 3개(실제 LIT 오염 사례 재생), 빌드에서 -DskipTests 제거 → 테스트가 JAR 게이트. savepoint 재제출: 정지 전 max 113,774,470 → 첫 행 113,774,471(+1), 3초, 체크포인트 정상
  - producer: 재연결·기동 gap-fill(REST, insert_direct 로 삽입 수 정확), `ingest_repairs` 창 단위 계보(수동 도구도 공용). 배포 재기동 22:29:37.6 → 22:29:39 연결. 기동 gap-fill 5.1초 창 원장 101 / 삽입 30 / 65.6초
  - **가드 실전 증명**: 그 30행(지연 65.6~66.7초) → `lateEventsSkipped` 19+11 = 30, 되튐 알림 0. 첫 조회 0 은 메트릭 수집 주기 탓(70초 뒤 30) — "0 = 실패"로 단정할 뻔함
  - **독립 검증**: 별도 도구 dry-run 으로 재기동 창 20초 재대조 → 원장 609 / 보유 609 / 누락 0. 멱등: 백필된 20:47 창 재실행 삽입 0
  - health_check 커버리지: 첫 실행 KRW-G 263초 오탐(체결 1초 뒤 체크가 돌아 미적재 직전 체결과 비교) → 조건을 "거래소가 60초 넘게 전에 본 체결 부재"로 수정 → 0건. DAG 테스트 10/10
  - dbt: 내가 만든 reconcile/ 폴더가 계층 규칙 위반 → intermediate/ 로 이동, parse OK. `ingest_repairs` 소스 등록
  - CI 신설(.github/workflows/ci.yml): Flink 테스트·DAG 테스트·dbt parse. 첫 실행 Flink 통과, Airflow(sqlite URI 오타 "sqlite:////:memory:"·메타DB 미마이그레이션)·dbt(protobuf 5 비호환) 실패 → 수정 푸시
  - **보안 점검 발견**: ClickHouse 8123 호스트 0.0.0.0 공개 + default 무인증 → docs/19 #15 추가, 백업과 묶어 처리
  - docs/19 현황: 있음 10 / 절반 2 / 없음 2 / 형식만 1 / 대기 1(#15)
- 09-16 23:13~23:37 **장애 주입 테스트**(사용자 승인 "주입 테스트 승인, 진행하고 기록"). 상세 docs/20 §5
  - 네트워크 단절 5초 → WS 끊김 없음, 유실 0(BTC 120/120). 45초 → 끊김 없음, 수신 56초 정지 후 2,551건 일괄, 유실 0(BTC 124/124, XRP 200/200). 90초 → 끊김 없음, STATS 113초 공백, 유실 0(BTC 223/223, XRP 219/219), 1,674행이 60~113초 늦게 적재 → 가드 제외(+1,674), 되튐 0
  - **원인 특정**: 동기식 MySQL executemany 가 단절 중 이벤트 루프를 막아 ping 도 정지 → 서버가 끊을 계기 없음 → TCP 버퍼가 복구 후 일괄 전달. "유실 0"은 막힌 루프가 우연히 연결을 지킨 결과. docs/19 #1(Kafka 선기록) 근거 추가. 09-15 실제 손실은 서버측 close 라 우리 쪽에서 재현 불가
  - **SIGUSR1 강제 재연결 훅 추가·배포**(c9d9711). 배포 재기동 gap-fill: 5.0초 창 원장 62/삽입 12. 신호 → 1012 종료 → 5초 후 재연결 → gap-fill [reconnect] 9.2초 창 원장 137/삽입 75/64.5초, 계보 기록. 가드: lateEventsSkipped 1,716→1,791(+75 정확히), 되튐 0
  - 판단 정정: 메트릭을 처리 직후 읽고 0 이라 단정할 뻔한 것 2회(수집 주기). 이후 60초 뒤 재조회를 규칙으로
  - "1주 잔여 손실 측정" 삭제 → 주입 테스트로 대체. 다음 = 내일 06:35 대조 확인 → 백업 + 접근 통제
- 09-16 23:54 **재연결 gap-fill 독립 검증 완료**: 별도 도구 dry-run 으로 강제 재연결 창(23:35:40~23:36:00, 20초, 287마켓) 재대조 → 원장 272 / 보유 272 / **누락 0**. 배포 1의 두 gap-fill 경로(기동·재연결) 모두 "자체 집계 + 독립 도구" 이중으로 검증 종료. 오늘 커밋 32개. 다음: 09-17 06:35 대조 DAG 결과 확인 → 백업 + 접근 통제(#15). 결정 대기: dbt 품질 층·구 이상탐지 마트 정리를 규칙 교체와 묶을지 먼저 할지
- 09-17 00:25~01:05 **2번 착수: 백업 + 접근 통제**(사용자 승인 "2,3 진행해"). 상세 docs/21
  - 백업: `backups.allowed_path` 가 재시작 없이 hot-reload 됨(실측) → 전체 백업 3.90GiB/20초(orderbook_raw 제외) → 호스트 추출 25초 → rsync Oracle 4.19GB/71초. **Oracle 블록 볼륨 150GB 신규 생성·연결·마운트**(/mnt/backup, 부트 47 + 150 = 197 ≤ 무료 200). **복원 리허설 성공**: Oracle 임시 ClickHouse(arm64) RESTORE 79초, crypto_trades 109,829,325행, 실제 집계 쿼리 OK. 첫 시도는 rsync 소유자(uid 1000) 때문에 권한 실패 → chmod → 성공 (리허설 없이는 몰랐을 실패)
  - 접근 통제: 영향 범위 점검(query_log 로 "누가 default 로 붙나")에서 **타 프로젝트 2개**(circuit Flink 잡, icepush-api·icepush dbt DAG) 발견 → 전용 사용자 icepush 생성, env 주입(icepush 저장소 2파일 커밋 1699e50), circuit 은 제출 시 자기 DB URL env. 사용자 4종(pipeline / n8n_reader / icepush / default 비밀번호). docker-exec 스크립트는 컨테이너 클라이언트 설정 파일(호스트 600) 마운트로 무수정
  - **컷오버 런북**(scripts/ops/clickhouse-auth-cutover.sh) 1회 정지 창: 3잡 savepoint → 7서비스 재생성 → 무인증 403 → 3잡 복원(trade_id 연속 +1) → producer 기동 gap-fill 714/565 → 가드 565 정확. Grafana·dbt·Airflow·icepush-api·circuit 전부 정상
  - **판단 오류 2건(docs/21 §2)**: ① "Docker 프록시가 출발지를 127.0.0.1 로 가린다" → 틀림, Access 로그는 10.88.0.1. 내 로컬 curl 을 n8n 으로 오독. IP 제한 풀었다 되살림 ② n8n 초안(workflow_entity.nodes)을 3번 고쳤으나 **실행은 workflow_history 의 발행 버전** → 전부 무효, 재시작 5회 낭비. 발행 버전에 적용하자 즉시 성공. 교훈: 설정을 읽는 위치부터 확인. 최종: n8n 자격증명(httpBasicAuth, 암호화) + n8n_reader HOST IP 10.88.0.1 + readonly=2, DB 평문 잔존 0
- 09-17 01:04~01:25 **backup_daily DAG**(docs/21 §4): 권한 설계(바인드 마운트 chown 101 via 컨테이너 root, Airflow group_add 101, ssh 키 사본 uid 50000·호스트키 핑거프린트 대조 고정). 실측: 호가 하루 Parquet lz4 977MB → **zstd 726MB**(19.7M행, 70초, 피크 136MiB), 09-16 647MB. 첫 증분 80MB. 용량 산정 → **180일 불가, 120일 확정**(≈85GB + 백업 ≈20GB < 147GB). 첫 실행(dags test 2026-09-16): 백업·Parquet·rsync·보존 성공, 검증 실패 → 원인 = 수동 테스트의 실행일 역산으로 incr_20260915 가 최신 전체보다 오래된 증분으로 분류되어 원격에서 삭제(정책 정상 동작) → 산물 제거 후 검증 0건 통과. DAG unpause, 첫 스케줄 09-18 01:20 UTC
- 09-17 01:30~01:50 **3번: 이상탐지 v2 + dbt 품질 층**(docs/22). 사용자 질문("이상탐지 초점 → 파이프라인 상태 초점에 맞게 구성돼 있나")에 대한 실행
  - Flink: 구 AnomalyDetector·AnomalyAlert 삭제, `MarketAlertDetector`(PRICE_24H 등급 전이, 분 종가 링 1,500·forward-fill·늦은 이벤트 가드) → `market_alerts`(rule_version v2-shadow). 하네스 테스트 5개 — 첫 실행 1건 실패는 테스트 T0 가 분 경계 미정렬(+30초가 다음 분)이라서, 정렬 후 통과. `git rm` 으로 테스트 디렉터리가 사라져 첫 빌드가 테스트 없이 돈 것도 기록(재빌드). 배포: savepoint 574e51 → `--allowNonRestoredState`(구 연산자 상태 의도적 폐기) → 18febb20 RUNNING, trade_id 114,196,670 → +1, 메트릭 lateEventsSkipped·levelTransitions 노출. 첫 24h 는 참조 없어 출력 0(설계상, docs/22 §2)
  - dbt: `quality/` 층(dq_reconcile_daily·dq_repairs_daily·dq_ingest_daily·**dq_rule_eval_daily**), `int_volume_surge_daily`(VOLUME_24H, post-hook 으로 market_alerts 기록·멱등) 13/13 PASS. 구 마트 mart_volume_spike·mart_alert_rate 삭제, exposures 갱신. **정정**: dq_ingest_daily 첫 판은 수리 행이 섞여 09-16 p95 511,766초 → 실시간(≤60s)만 분위수, 늦은 행 별도 카운트
  - 실전 평가: VOLUME_24H 09-16 정밀도 0.833/재현율 1.0, 09-15 0.684/0.867, 09-14 0.75/0.938 (백테스트 0.79/0.80 과 같은 자리)
  - Airflow: `rules_daily`(01:05 UTC) 신설, daily 리포트 첫 블록 = Pipeline Quality(대조 %·99% 미만 셀·수리·지연 p95·늦은 행·섀도 전이). DAG 테스트 11/11
  - n8n FDS: 본문을 `upbit_market_events` 전이로 교체(발행 버전+초안, psql 변수는 -c 에서 안 되고 -f 에서만 됨 — 1회 실패 후 수정), success·ClickHouse 도착 확인
  - Grafana: 메인 패널 2개 → market_alerts, 품질 대시보드(dq_* 6패널) 프로비저닝 확인
  - README: 구 규칙을 업비트 감시 유형에 대응시킨 절(근거 없음)을 v2 절로 교체 — 공개 문서에 틀린 주장이 남아 있으면 안 됨
  - 승격 기준 docs/22 §4. 다음: 섀도 7일 → 승격 판단
- 09-17 02:05 UTC docs/22 §4 정정: 사용자 "왜 섀도가 7일?" → 7일은 관행이었음을 인정. PRICE_24H 는 하루 0.9건이라 7일 표본 6건으로 0.9 판정 불가 + 임계 근거는 이미 6개월 역검증 → 기준을 "전이 10건 이상 1분봉 재계산과 전부 일치(구현 동등성)"로. VOLUME_24H 는 하루 5건·요일 주기 → 7일 유지. 승격은 부하 실험과 독립(격리 토픽·테이블)이라 대기 없이 실험 착수
- 09-17 02:10~02:30 UTC **부하 실험 준비(docs/15 §7)**. Flink 잡 env 파라미터화(토픽·그룹·테이블 접두·탐지기 토글·잡 이름, 기본값 = 프로덕션), compose `lab` 프로필 TM(1.5g/슬롯 2), `load_test.*` 토픽 2개·`load_test_*` 테이블 4개(TTL 1일; `GRANT ... load_test_*` 와일드카드는 24.1 미지원 → pipeline 의 DB 등급 grant 가 이미 덮어 불필요). 실험 잡 b29dd061 lab TM 배치 확인. 스모크 3,000건: 적재 3,000/uniq 3,000, 프로덕션 오염 0. **발견**: 재생기 rate 모드의 시각 이동이 원본 간격 기준이라 e2e p50 −14.4s → "보내는 순간"으로 수정, 재스모크 p50 2.99s/p95 4.75s. Binance 5심볼 09-15 다운로드·SHA256 일치·변환. `prod_guard.py`(프로덕션 e2e p95 > 30s ×3 → 재생기 kill) 스모크 4.68/4.93s. 단일 호스트 한계 답: 두 번째 동일 사양 호스트 없이는 해결 없음 → 창(UTC 17~21)·가드·기록으로 축소, cpuset 은 4코어라 무의미. 빌드는 Docker 멀티스테이지(호스트 mvn 없음)
- 09-17 02:25~02:35 UTC Binance 코퍼스 확정: 5심볼 × 2026-09-15 = **10,106,013행**(BTC 3,419,940 / ETH 3,412,911 / XRP 1,718,373 / SOL 953,367 / DOGE 601,422 — zip 행 수와 정확히 일치), gz 218MB, SHA256 df1e124a… 고정. 파싱 스모크 200/s × 15초: 발송 2,997 = 적재 2,997(uniq 2,997), 5마켓 가격·ask_bid·e2e p95 2.3~2.9s 정상, 프로덕션 `crypto_trades` 의 BIN- 행 0. 절차 오류 1건: 변환 완료 대기 루프의 `pgrep -f binance_adapter` 가 대기 셸 자신에 걸려 6분간 "실행 중"으로 오판(로그엔 02:25 exit=0). 교훈: 완료 판정은 프로세스 이름이 아니라 산출물(로그 exit 코드·파일)로
- 09-17 02:45~03:15 UTC **부하 실험 ① 체결 계단(Upbit obs_end 코퍼스, 50→100→200/s 각 10분)**, 실험 잡 b29dd061(lab TM). 발송 209,999 / 적재 209,999 / uniq(market,seq) 209,999 / uniq(trade_id) 209,999 → **유실 0·중복 0**. 계단별: 백프레셔 max 0ms/s, 소스 busy p50 5→15→26ms/s, ClickHouse 적재 60→120→241 rows/s(입력과 일치), e2e p50/p95 2.95/5.03 → 2.89/5.00 → 2.34/4.39s(배치 창이 결정, 유입 많을수록 낮음 = docs/14 관찰과 동일), 체크포인트 e2e p95 66~81ms, ClickHouse CPU 중앙값 16~17%(변화 없음), Kafka 브로커 CPU 19→22→27%, load1 max 6.4→6.8→8.9(4코어). **임계점 미도달**(2~8배). 프로덕션 영향: KRW 체결 e2e p95 10분 창 4.68~4.80(전) → 4.79/5.02/4.85/4.69(중), max 5.6~8.3 → 8.3~10.5s, 가드 max 8.01s(임계 30s 미발동). 측정 주의 2건: (a) 컨슈머 lag 은 커밋 오프셋 기준이라 Flink 체크포인트 60초 톱니(max ≈ rate×60: 2,953/5,931/11,980) — 적체 판정은 60초 창 최솟값 추세(190→186, 849→836, 1,779→1,613 = 평탄)로; (b) measure.py 의 TM 자원 열이 프로덕션 TM 을 찍고 있었음 → `--tm-container` 인자 추가(lab TM 742MiB 확인), ① 의 tm 열은 프로덕션 TM 값. 절차 오류: `pkill -f prod_guard.py` 가 자기 셸에 맞아 검증 셸이 죽음(exit 144) → 앵커 패턴 `^python3 replay/...` 로
- 09-17 03:17~03:25 UTC **③ 브로커 단독 상한**(`kafka-producer-perf-test`, 토픽 load_test.perf 3p/RF3/minISR2 = 프로덕션 체결 토픽과 동일 내구성, 레코드 660B = 체결 봉투 크기, acks=all). Connect 기본형(linger 0, batch 16KB, 무압축): 목표 10k/s → 9,920/s p50 12ms·p99 550ms(따라감), 목표 30k → **19,133/s(12.0MB/s)** p50 2.26s·p99 4.8s(포화), 100k·무제한 → 19,187/19,271/s(동일 상한). 배치형(zstd, linger 20ms, batch 1MB): **64,956/s(40.9MB/s)** p50 37ms·p99 1.1s. 결론: 브로커 상한은 무배치 acks=all 기준 약 19k/s, 배치 시 65k/s — 파이프라인 계단(200/s)의 100~300배라 **Kafka 는 병목 후보에서 제외**. 한계: 프로듀서 클라이언트가 cdc-kafka-1 컨테이너 안에서 돌아 브로커별 CPU 귀속 불가(137~147% 표본은 클라이언트 포함), 4코어 호스트에서 클라이언트·브로커 3개가 CPU 를 나눠 "브로커 상한"이 아니라 "이 호스트 상한". load1 5.5→12.0. 프로덕션 영향: 2분 창 p95 4.5~4.8s 유지, 배치형 65k/s 구간(03:24)만 p95 6.2s·max 13.9s, 가드 max 9.19s(45표본, 미발동). 토픽 삭제(6GB 복제 로그 정리)
- 09-17 03:30 UTC S3(브로커 정지)·②(규모 계단) 자동 실행이 세션 권한 분류기에 막힘(프로덕션 브로커 `docker stop`, cron 등록). 우회하지 않고 스크립트로 남김: `pipeline-load-lab/replay/scenario_broker_stop.sh`, `scenario_scale.sh`(둘 다 가드·측정·검증 포함, 실행 명령은 파일 머리). ②는 UTC 17~21 창. docs/23 결과 문서 시작(①·③ 확정, 나머지 대기)
- 09-17 03:33~03:46 UTC **S3 브로커 정지**(사용자 터미널 실행, `scenario_broker_stop.sh`). ① 100/s 재생 중 kafka-2 5분 정지. 프로덕션: 파티션 1 리더 2→1, ISR {1,3}, 클러스터 under-replicated 104 파티션, 분당 적재 연속(빈 분 0), 정지 순간 분만 e2e p95 7.73s(max 10.9s) 나머지 4.6~4.8s, Connect WARN 5(재시도, 에러 0), 프로덕션 Flink 체크포인트 실패 0·재시작 0, 재기동 후 ISR 회복 ≤15s·리더 복귀. **무손실 실측**: trade_id 구간 12,849 = MySQL 12,849 = ClickHouse 12,849. 실험 72,000/72,000, lab 잡 lag 평탄·CP 실패 0. 가드 max 7.74s 미발동. 발견: lag 60초-최솟값이 체크포인트 정렬 때문에 오르는 것처럼 보일 수 있음 → 적체 지표를 "end-offset − 적재 건수"로 바꿀 것. 부수: circuit-connect 컨슈머 DisconnectException 265건(무영향). docs/23 §3
- 09-17 03:50 UTC 사용자 "왜 KST 02~06? 지금 하면 안 되나" → 창 규칙은 근거 없는 보수였음을 인정(①·③·S3 에서 프로덕션 p95 최악 6.2s, 가드 미발동, 낮·새벽 유입 차 2배는 2,000/s 실험 대비 잡음). docs/15 §7·docs/23 정정. ② 03:53 UTC 사용자 터미널에서 시작(s2_scale_20260917T0353)
- 09-17 03:55~03:59 UTC ② 500/s 계단 중 사용자 "kafka-1 CPU 147%, 과부하 아닌가?" → 분리 측정: 브로커 JVM(PID 1) 10초간 232틱 = **23%**, docker stats 20~28%(3브로커 동일 수준). 147% 는 measure.py 가 5초마다 `docker exec cdc-kafka-1 kafka-get-offsets/consumer-groups` 로 띄우는 **CLI JVM 2개**(수명 2초, 각 116%·89%) — 브로커가 아니라 측정 도구가 4코어 호스트의 ~1코어를 주기적으로 소모. 수정: confluent_kafka 클라이언트로 end-offset·committed 읽기(`--bootstrap`), CLI 대비 교차검증(end 415,612 vs 4초 뒤 CLI 417,863 = 500/s 진행분). 실행 중인 구 측정기를 죽이고 새 측정기로 교체(CSV `_b`, 03:57:29 이후), 재생기·가드는 유지. 교체 후 브로커 CPU 20~34%, 순간 스파이크(kafka-3 147% 1표본)는 브로커 자체의 버스트. ①·③ 의 k1_cpu 열도 같은 오염 — docs/23 에 한계로 명시
- 09-17 04:23~04:30 UTC **② 결과**(docs/23 §4): 2,099,994 발송 = 적재 = uniq → 유실·중복 0. 2,000/s(피크 34배)까지 백프레셔 0, 계단 끝 적체 1.6~1.8초분(싱크 배치 창)으로 일정, 선행 지표 후보 CH CPU 21→34→40%·소스 busy 48→76→126ms/s(선형). 프로덕션 p95 4.69~5.01s, 가드 max 6.07. 재생기 음수 sleep 버그 수정(10k 드라이런 9,999/s 유지). **②-확장 5,000→10,000/s 04:28 UTC 시작**(s2b_scale, 스크립트 STEPS/RUN env 화). 임계점 없이는 개선·S4·N배 문장을 쓸 수 없어서 — docs/15 "필요 시 5,000/s"의 연장
- 09-17 04:48~05:00 UTC **②-확장 결과(docs/23 §5)**: 8,999,983 발송=적재=uniq. 5,000/s 실시간 유지(p95 0.13s, CH 메모리 1.65/1.75G), **10,000/s 임계**: 소스 busy max 1,000ms/s, 정체 5회(적재율 387/s까지), e2e p95 26.5s·max 36s, 적체 49,508 은 5초에 소진. 병목 = 소스 체인의 동기 JDBC 싱크(inserts/s × insert 지연 → 서브태스크 0 이 파티션 2개 담당해 1.1s/s) + 머지 CPU(NewPart 29,933·MergeParts 7,440 = 0.87코어). CH 예외 0·메모리 한도 로그 0. 선행 지표: 소스 busy(선형), CH 메모리(5k 에서 94%), insert 평균 지연(9→24ms). N배 = **5,000/s = 피크 85배**. 동거 분리(사용자 질문): MySQL 정리 이벤트(10분, DELETE 4만)가 04:45 정체와 겹침(MySQL 51%), 브로커 스파이크는 관찰 주간에도 37% 표본에 존재, 타 프로젝트 영향 없음(API 0.2~1.3%, 프로덕션 TM 6%). S4 개선: `CLICKHOUSE_BATCH_SIZE` env(기본 200, 실험 1,000) → 빌드 중
- 09-17 04:53~05:05 UTC **S4 전후표**(docs/23 §5-2): `CLICKHOUSE_BATCH_SIZE=1000` 실험 잡(8eaeaf76)으로 10k/s 재실행. 5,999,966 발송=적재=uniq, 정체 5→**0**, e2e p95 26.5→**0.28s**, 소스 busy 651→200, insert 50→10/s, 파트 29,933→6,222, 머지 519→98s, CH CPU 103→28%, CH 메모리 1.735→0.99G, load1 11.1→6.7. 프로덕션 p95 4.5~4.8, 가드 max 7.54. N배 확정: 현 설정 5,000/s(피크 85배), 배치 1,000 ≥10,000/s(170배). 프로덕션 배치는 200 유지(피크 59 rows/s 라 3초 안에 200 도 안 참 → 효과 0, 정지 창만 소요) — 지속 2,000/s 넘는 날 적용
- 09-17 05:05~05:12 UTC **선행 지표 알림 승격**: `health_check` 에 `check_source_busy`(Flink REST, 프로덕션 CDC 소스 서브태스크 busy max > 500 ms/s)·`check_insert_latency`(query_log, crypto_trades insert 평균 > 30 ms/10분) 추가. 기준선 실측: busy 3 ms/s, insert 시간별 평균 14~18 ms(실험 시간 04시만 21). `airflow tasks test` 3.0 / 14.8 ms, DAG 테스트 11/11(기대 목록 갱신). 백프레셔는 소스가 체인 머리라 10k/s 에서도 0 → 지표로 부적합
- 09-17 05:12 UTC 실험 정리: 실험 잡 취소, lab TM 정지 → TM 1/슬롯 4, 프로덕션 3잡 RUNNING, 가용 6.9GB. load_test 토픽(6h)·테이블(TTL 1일) 자연 소멸. 하네스 교훈: 백그라운드 대기가 "메모리 부족"으로 2회 종료(호스트 가용 5.4GB — 하네스 자체 보호) → Monitor 도구로 전환
- 09-17 05:20 UTC 사용자 "문제가 일어난 곳이 어디고 실무에서도 흔한가" → docs/23 §5-3 표(6지점: 동기 JDBC 싱크 체인, 파티션/병렬도 불일치, CH 소량 insert, MySQL DELETE+CDC, 브로커 정지, 단일 호스트). 측정 교훈 5건 docs/18 이관
- 09-17 05:15~05:45 UTC **PRICE_24H 동등성 모델**(docs/22 §4-1): `int_alert_transitions_recomputed`(SQL 재계산, 2일 4.2M 체결 6~7초) + `dq_alert_parity_daily`(마켓·등급쌍·±60s 매칭), rules_daily 편입, 기본 창(워밍업) 빈 표·강제 창 통과, dbt 테스트 6/6. 실패 4건 기록(스칼라 서브쿼리 Nullable→run_query 상수화, 배열 조인 1.84GiB→행으로 풀기, CTE 재계산 1.12GiB→별도 모델, `p.day` 열 이름→별칭). **발견(docs/22 §4-2)**: 체결 단위 판정은 임계 근처에서 플래핑 — LSK 하루 0→1 전이 86회 vs 거래소 지정 9회, 첫 전이 7시간 빠른 것은 선행이 아니라 정의 차이. 거래소 지정 지속 중앙값 6분·연속 간격 중앙값 10분 → 거래소는 분 단위 평가. docs/16 검증도 1분봉 기준 → 판정 단위를 분 종가로 바꾸는 v2.1 제안, 사용자 결정 대기
- 09-17 05:45~05:53 UTC **PRICE_24H v2.1 = 분 종가 판정**(docs/22 §2·§4-3). 사용자 "그 기준의 이유는?" → 근거 3(정답 해상도 분, 임계 근거 1분봉, 참조 대칭), 히스테리시스 기각 근거(거래소 지속 중앙값 6분·N 근거 없음). SQL 재계산 분 종가로 변경 → 강제 창에서 LSK 9/CAP 3/DRV 1 = 거래소 9/3/1, 첫 지정 시각까지 일치(18:49/01:47/22:21). Flink: 처리 시간 타이머(분 끝+10s)로 분 닫기, 빈 분 타이머 체인(24h), 상태 2개 추가(savepoint 호환), 테스트 7개 통과(Docker 빌드 게이트). 배포 savepoint-18febb-1cadd3640808 → ce93ca68, trade_id +1(114,474,507), 상태 3.5MB 복원, minutesEvaluated 371/2분, rule_version v2.1-shadow. README 규칙 표 갱신
- 09-17 05:55 UTC 브로커 3→1 선행 실측 스크립트 `pipeline-load-lab/replay/scenario_all_brokers_stop.sh`(사용자 실행): 3브로커 3분 정지 중 15초마다 Connect 태스크 상태·호가 수집기 buf_err/deliv_err/queue·Flink 상태·MySQL 60초 적재를 기록, 재기동 후 ISR 회복·Connect 자동 복구 여부(60초 뒤 FAILED 면 태스크 재시작 API 호출·시간 기록)·정지 구간 MySQL 행 수 = ClickHouse 행 수 따라붙기·호가 분당 적재로 유실 창 측정
- 09-17 06:00~06:12 UTC 사용자 통찰 "호가가 되돌릴 수 없는 데이터에 더 가깝다" → 맞음: 체결은 MySQL 원장+거래소 REST 이중 안전망, 호가는 둘 다 없음. **`dq_orderbook_gaps_daily`** 신설: 수집 시각(recv_ts) 초당 스냅샷 0 인 구간(3초 이상)을 gap 창으로, 추정 유실 = 그날 초당 중앙값 × 빈 초. 근거: 09-16 초당 p01 109·min 43·0 인 초 0 → "한 초 0 건"은 정상 변동이 아님. 마켓별로 안 세는 이유: 뜸한 마켓 분당 2건, 장애는 전 마켓 동시. 30일 전수: **09-12 06:20:31 +6초(추정 942건)** 1건 발견 — 수집기 재연결·전송 오류·큐 증가 없음 → 거래소 측 침묵 가능성, 원인 불명으로 기록. 첫날(09-10)은 수집 시작 04:44 전이 gap 으로 잡혀 min(recv_ts) 이전을 제외. ClickHouse 집계 안 집계(median 안 countIf) 오류 1건 → CTE 분리. 24초/4MiB
- 09-17 06:12~06:16 UTC `health_check` Connect 자동 복구: 태스크 FAILED → `POST /connectors/{name}/tasks/{id}/restart`, 커넥터 FAILED → `/restart`, 알림에 "auto-restarted" 병기. 근거: Connect 는 FAILED 태스크를 스스로 안 살리고, 재시작하면 Debezium 이 binlog 오프셋(보존 무기한)에서 이어 읽음. DAG import OK, `tasks test` RUNNING/restarted [], 테스트 11/11. 실제 FAILED 경로는 브로커 전체 정지 실측에서 처음 밟는다(스크립트가 +60s 에 먼저 재시작하므로 health_check 경로는 별도 확인 필요). docs/23 §6 갱신(호가 = 되돌릴 수 없는 데이터, 수정 3건 중 2건 완료)
- 09-17 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H2 producer buffer>1000 또는 WARNING'], 체결 lag p95 max 540085.56s, 호가 e2e p95 max 58699.0ms, buffer max 246.0, 알림 481, ClickHouse max 1.37GiB → /home/calme/pipeline-observation/digest_2026-09-17.md
- 09-17 06:38~06:47 UTC **S5 브로커 전체 정지 3분**(사용자 실행, docs/23 §7). 체결: MySQL 계속 적재(분당 660~1,194), Connect RUNNING 유지(FAILED 없음), 재기동 +15s 에 정지 구간 3,307=3,307 → 무손실·지연만(e2e p95 216s). 호가: 큐 적체 → +134~155s 부터 `Message timed out`(120s) → 재기동 순간 06:42:25 librdkafka "partition count 6→0" 메타데이터 블립으로 큐 잔여 폐기(`Broker handle destroyed`) + produce `_UNKNOWN_TOPIC` 을 WS 에러로 오인해 재연결. 최종 유실 35,838(recv−produced 35,840) = 3분 20초치 전부. `dq_orderbook_gaps_daily` 가 06:38:50 +216s·추정 38,664 로 기록. **수정**: 수집기 `message.timeout.ms` 600s, `topic.metadata.propagation.max.ms` 300s, produce KafkaException 을 전송 오류로 처리(재연결 안 함). 06:46:26 재빌드·재기동(약 5초 공백, 내일 gap 표에 남을 것). 검증: `OUTAGE_S=60 replay/scenario_all_brokers_stop.sh` 재실행 필요(사용자). 3→1 축소의 핵심 위험 = 호가의 메타데이터 블립(1브로커는 재시작마다 발생)
- 09-17 07:06~07:16 UTC **검증 재실험 브로커 전체 정지 60초**(사용자 실행). 호가: deliv_err 0, 큐 7,261 → 재기동 후 소진, 유실 창 없음 → 수집기 수정 통과. 체결: 리밸런스 후 커넥터 UNASSIGNED/태스크 RUNNING 유령 상태로 **7분 발행 0**(1차는 RUNNING 유지·즉시 복구 — 같은 정지에 두 결과) → 수동 restart?includeTasks=true 20초 만에 binlog 재개, 정지 창 1,413=1,413 무손실, 재전송 중복 27건(at-least-once, RMT 근거). health_check 자동 복구 확장: 커넥터 ≠ RUNNING → 태스크 포함 재시작, ClickHouse 1분 적재 0 → CDC 커넥터 재시작. 테스트 11/11. 교훈: 같은 장애를 두 번 일으켜야 두 번째 형태가 나온다
- 09-17 07:20~07:35 UTC **docs/24 브로커 3→1 축소 설계**(결정 대기). 사용자 "KRaft 안 쓰는 게 나은가?" → KRaft 가 표준(ZK 4.0 제거)이 맞고 문제는 방법: A(ZK 유지·파티션 재할당 RF3→RF1, 오프셋·클라이언트 무변경, 되돌리기 가능) 지금, B(새 KRaft 클러스터 컷오버: Connect 오프셋 PATCH 이식·Flink earliest·타 세입자 circuit-connect/game-events 오프셋까지) 계획, C(제자리 KIP-866)는 cp-kafka 7.5.3=Kafka 3.5 라 early access → 이미지 업그레이드 뒤. 확인 사실: 이 클러스터에 타 프로젝트 토픽 4개·컨슈머 그룹 5개 동거, 브로커당 13~14GB, Connect offsets REST 동작(3.6+), Flink 소스 committedOffsets/LATEST. 절차 6단계·검증·롤백·잔여 위험 기술
- 09-17 07:45 UTC 사용자 승인 → 브로커 3→1 A안 착수. 확인: 브로커 정적 설정 KAFKA_MIN_INSYNC_REPLICAS=2·DEFAULT_REPLICATION_FACTOR=3·OFFSETS_TOPIC_RF=3, 동적 minISR=1 은 orderbook·game-events·load_test 만 → 체결 토픽·내부 토픽·circuit 토픽은 RF1 이 되는 순간 acks=all 이 막히므로 **클러스터 기본(동적) + 전 토픽 minISR=1 을 재할당보다 먼저**. RF3 토픽은 브로커 1 이 전체 복제본을 이미 가져 재할당이 메타데이터뿐, 복사는 RF1/RF2 토픽의 브로커 1 에 없는 파티션만. 런북 `scripts/ops/kafka-reduce-to-one.sh`(사전 점검·load_test 토픽 삭제·minISR·재할당 JSON·롤백 배치 저장·실행/검증·잔여 replica 0 확인). 세션 분류기가 실행을 막아 사용자 터미널 실행. 정적 default.replication.factor=3 은 compose 정리(단계 5) 때 kafka-1 재기동과 함께 바꿈 — 그 전까지 자동 생성 토픽은 실패(예상 없음)
- 09-17 07:55 UTC 사용자 "정적 minISR=2 면 1대는 안 되는 것 아닌가 / 실무는 Kafka 를 원본으로 쓰나" → docs/24 §5-1: minISR 의미와 동적 변경(재할당 전), 실무 표준 짝 RF3/minISR2, "다른 복사본이 있느냐"로 원본 여부가 갈림(체결=전달 계층, 호가=원본급), 축소 후 호가의 단일 복사본 구간을 위험으로 명시
- 09-17 08:17~08:19 UTC **브로커 3→1 재할당 완료**(사용자 터미널, docs/24 §4-1): load_test 토픽 삭제(114→105 파티션), minISR 1(클러스터 기본+전 토픽), 105 파티션 → replicas [1] 약 2분. 독립 재검증: 105/105 Replicas 1/Isr 1, under-replicated 0, 브로커 2·3 잔여 0(디스크 36KB, ~27GB 회수), 컨트롤러 브로커 1, 적재 지속(체결 703/60s·호가 9,940/60s), 수집기 오류 0, Connect RUNNING. 롤백 JSON 저장. 다음: 브로커 2·3 정지 → 24h 관찰
- 09-17 08:21~08:23 UTC 브로커 2·3 정지(사용자, exit 143). 19초 뒤: 브로커 kafka-1 단독, 컨트롤러 1, under-replicated 0, 적재 지속(체결 937·호가 9,780/60s), 클라이언트 4종 오류 0, Connect·Flink 정상, 호스트 가용 메모리 6.9→8.4GB. **3→1 축소 완료(정지 0초)**. 24h 관찰 시작 — 내일 06:35 대조 DAG·유실 창 표·health_check 로 판정 후 compose 정리
- 09-17 08:30 UTC 기록 점검(사용자 요청): 누락 3곳 보완 — docs/19 현황표(#11 중복 27 근거·#12 1브로커 축소·#16 호가 유실 창 신설, §2 부하 실험 완료), README Phase 체크리스트(부하 실험·v2.1·호가·축소 완료 항목), journey-index 블로그 5~8편 제안(에피 13~18 → 4편, 편별 뼈대·숫자·출처). 오늘 worklog 31항목·커밋 37개
- 09-17 08:35 UTC 사용자 "24h 기다리는 이유?" → 규칙이 아니라 대조 도구가 일배치라서. 즉시 검증으로 대체: 1브로커 이후 11분(08:22~08:33)을 거래소 1분봉과 마켓·분 단위 거래량 대조 → **1,402셀 일치, 누락 0**(REST 289콜 46초). backfill dry-run 은 MySQL 조회가 600초 타임아웃(10분 창 × 287 마켓 + 정리 DELETE 겹침)으로 실패 → 1분봉 대조로 전환. 24h 관찰 대신 오늘 compose 정리·1브로커 재기동 실측으로 진행, 내일 06:35 DAG 는 정식 기록
- 09-17 08:45 UTC compose 정리: kafka-2·3 서비스 제거, kafka-1 정적 설정 minISR 1·default RF 1·offsets RF 1, bootstrap 목록 kafka-1 단독(각 서비스의 다음 재생성 때 반영). **실수 1건**: depends_on 이 맵 형태(`kafka-2:` + condition)라 목록형 정규식으로 제거가 안 돼 `config` 가 invalid 한 채 커밋(61f1c8d) → 즉시 수정(7cebb27). 교훈: compose 편집 뒤 `docker compose config -q` 를 커밋 전에. 이전 파일은 ~/kafka-reassign/docker-compose.before-reduction.yml. 재기동 실측 런북 `scripts/ops/kafka-single-restart-check.sh`(사용자 실행: kafka-2·3 컨테이너 제거, kafka-1 재생성, 응답까지 초, Connect·수집기·따라붙기·호가 분당·정적 설정 확인)
- 09-17 08:50 UTC 사용자 "12시간 측정은?" → 동의하되 재기동은 09-18 06:35 대조 DAG 결과 확인 후(정식 판정 + 사람이 있는 시간). 그때까지 1브로커 상시 운전 관찰(health_check 10분, 내일 아침 유실 창·지연 표). docs/24 갱신
- 09-17 08:55 UTC **하루 마감**. 오늘: 백업/접근통제(21) → 규칙 v2·품질층(22) → 부하 실험 5시나리오·S4·선행 지표 알림(23) → PRICE_24H v2.1 분 종가 + 동등성 모델 → 브로커 전체 정지 실측 2회·호가 유실 창 모델·수집기 수정·Connect 자동 복구 → 브로커 3→1 재할당·정지·compose 정리(24). 커밋 40+. **내일 재개 체크리스트**: ① 06:35 UTC 대조 DAG(1브로커 첫 정식 판정) ② `dq_orderbook_gaps_daily`·`dq_ingest_daily` 09-17 행 ③ 밤사이 health_check 알림 ④ 이상 없으면 `scripts/ops/kafka-single-restart-check.sh`(사용자) → docs/24 완결 ⑤ 다음 작업: ReplacingMergeTree(중복 27), 섀도 동등성 첫 결과는 09-19 01:05
- 09-18 사용자 결정: 관찰을 브로커 정지 시점(09-17 08:22 UTC)부터 꽉 찬 24h 로. 재기동 실측은 **09-18 08:22 UTC(17:22 KST) 이후**, 06:35 대조 결과 확인 뒤. "06:35 직후" 안보다 약 1시간 40분 늦어짐
- 09-18 04:30~04:45 UTC 1브로커 20h 관찰(docs/24 §4-2): 이상 없음(복제 부족 0, 시간별 적재 연속, 09-17 p95 2.09s, 유실 창 실험 1건). health_check 알림 2회 = insert 평균 착시(p95 28~32 정상, 7.5s 단발 2~4건이 평균 42~66ms, dbt·머지 시각) → 임계 p95 > 60ms + 단발 max > 5s 분리, `tasks test`·DAG 테스트 11/11. 교훈: 평균으로 알림 걸지 말 것 — 실험에서 정체 신호는 분포 전체의 상승이었다
- 09-18 05:00 UTC 포트폴리오 정리 문서 작성(사용자 요청, 지금까지 여정만, 9절·숫자마다 조건·출처). 사용자 판단으로 **공개 레포에서 제외**(로컬 보관, gitignore `docs/portfolio-*.md`) — 한 번 푸시된 커밋 2e11f1e 는 히스토리에 남음
- 09-18 06:20 UTC 관찰 일일 요약(자동): 샘플 287, 위반 ['H9 Kafka 브로커1 로그>20GB'], 체결 lag p95 max 2.83s, 호가 e2e p95 max 81272.0ms, buffer max 170.0, 알림 0, ClickHouse max 0.68GiB → /home/calme/pipeline-observation/digest_2026-09-18.md
- 09-18 10:50~10:55 UTC 상태 점검: 1브로커 28h 정상, 06:35 대조 DAG 09-17 6,803셀 100%(정지 실험 시간대 포함), health_check 41회 정상. 어제 06:35 실행(09-16분) FAILED 원인 규명 = KRW-BFC 09-16 11:00 셀 43.7% — 09-16 BFC 백필 창이 그 시간을 덜 덮어 306행 부족(대조 DAG 가 잡아 준 두 번째 유실) → `backfill_trades.py` 306행 삽입, int_reconcile_hourly 09-16 재계산 100%. 24h 조건 충족, 재기동 실측 대기(docs/24 §4-3)
- 09-18 10:56~11:05 UTC **1브로커 재기동 실측(docs/24 §4-4, 축소 완결)**: 브로커 2·3 제거, kafka-1 재생성. 응답 14초. Connect +15s UNASSIGNED → +30s 스스로 RUNNING(유령 상태 미재현, binlog 재접속 +5s). 체결 정지 창 MySQL 1,820 = CH 고유 1,820, 중복 239(재전송, 10:57:14~17 도착), e2e p95 18.8s → 4.67s. 호가 deliv_err 0, 초당 0 인 초 0. 정적 설정 3개 반영 확인. 스크립트 한계: count() 비교라 중복 때문에 상한까지 돎 → uniqExact 로 수정. docs/19 #12 완료, README 갱신. 회수 메모리 1.5GB·디스크 27GB
- 09-18 11:05 UTC **ReplacingMergeTree 전환 착수**(사용자 승인, docs/19 #11). 근거: Connect 재시작 재전송 중복 27(09-17)+239(09-18). 사전 확인: crypto_trades 113,062,905행·3.74GiB·8 월 파티션(TTL 실제 365일 — init.sql 의 90일과 다름, 함께 정정), 의존: MV `mv_latency_stats`(inner, 이름 바인딩)·dbt 뷰 stg_trades, 사용자 권한은 전부 DB 단위(교체 영향 없음), 전체 uniqExact 는 메모리 한도(1.57GiB) 초과 → 파티션별 검증. 방법: `crypto_trades_rmt`(ReplacingMergeTree(flink_ts), 같은 정렬 키·파티션·TTL) → 닫힌 파티션 7개 복사(월당 7~12초, 프로덕션 p95 4.43s 영향 없음; 202602 는 10.68M→9.04M 로 초기 중복 1.64M 이 머지에서 바로 제거됨) → 현재 달 복사 → MV DETACH → 차이분 복사 → EXCHANGE TABLES → 잔여분 복사 → MV ATTACH(이름으로 재바인딩) → 검증
- 09-18 11:05~11:15 UTC **RMT 전환 완료(docs/25)**: 닫힌 파티션 7개 복사(원본 uniq = 새 FINAL, 202602 +3 은 다른 source_ts 키), 현재 달 일괄 INSERT 가 서버 메모리 한도 초과(1.84>1.57GiB) → 일 슬라이스·스레드 1 로 43초, 실패 잔여 파트 2M 행은 OPTIMIZE PARTITION FINAL 17초로 정리. MV DETACH → 차이분 → EXCHANGE → 잔여 0 → ATTACH. 검증: Flink 적재 계속(60s 2,367), 중복 239 → 0, 202609 FINAL 24,058,224 = 옛 uniq, p95 4.67s. init.sql DDL(RMT, TTL 365 실제값) 정정. 옛 테이블 7일 보관. 교훈: 대량 INSERT SELECT 는 서버 총 메모리 기준으로 슬라이스, 실패한 INSERT 는 파트를 남길 수 있다(RMT 라 머지로 흡수)
- 09-18 11:20 UTC 사용자 "실제 DE 는? 누구나 받을 수 없는 플래그는?" → 답: 플래그 발명은 DE 일이 아니라 남에게 없는 데이터를 믿을 수 있게 쌓는 것. 우리만의 데이터 = 호가 이력(거래소 미제공) → 체결×호가 분 단위 결합 마트를 2층 전에 두기로. docs/19 §2-1 순서 갱신
- 09-18 11:25 UTC 실수 1건: 재기동 스크립트가 따라붙기 루프를 도는 동안 같은 파일을 편집(uniqExact) → bash 는 스크립트를 순차로 읽으므로 바뀐 줄에서 `syntax error near 'done'` 으로 종료(post 출력 유실, 수치는 별도 조회로 확보). 교훈: 실행 중인 셸 스크립트 편집 금지, 수정은 종료 후. 또 `pgrep -f` 자기 셸 매칭에 한 번 더 걸림(앵커 패턴 재확인)
- 09-18 11:15~11:20 UTC **docs/26 CDC 구간 재검토**(결정 대기). 실측: 24h 체결 토픽 4,109,009 메시지 중 적재 2,235,132 → **삭제 이벤트 46%**(Flink 가 버림, binlog_row_image FULL), 정리 DELETE 실행 계획 풀스캔(created_at 단독 인덱스 없음, 59초·CPU 51%), binlog 15파일 1.38GB 무기한, 원장 16.1M행 2.2+1.7GB. 제안: Debezium skipped.operations=d, created_at 인덱스(온라인), binlog 30일 — 정지 없음. 파티션 DROP 기각(유니크 키에 created_at 강제 → gap-fill 중복). #1 Kafka 선기록 철회(정지 실험 4/4 원장이 지킴, docs/19 갱신). dim_markets 설계(일봉 첫 봉 = 상장일 근사, 287 중 55 창 안)
- 09-18 11:26 UTC 사용자 "실무는 어떻게? 삭제 이벤트는 실무에서도 불필요? 스펙 때문? ClickHouse 는?" → docs/26 §1-1: 거울 vs 보관소로 갈림(의미의 문제, 스펙은 아픔의 크기), 업무 삭제/보존 삭제 구분 3방식, OLTP 삭제 불문율 3, binlog 보존 기준, ClickHouse 는 삭제를 본 적 없고 보존은 이미 정석(TTL 파트 만료)
- 09-18 11:28 UTC 사용자 "실무라면 어떻게 놓겠나" → docs/26 §6: 시세=스트리밍+대조, 원장=CDC(2층이 CDC 를 제자리에), 호가=내구 우선·계층 보존, SLO 는 소비자, 환경 분리·스키마 계약, 측정 문화는 그대로
- 09-18 11:30 UTC 사용자 "설계 부채면 고치는 게 낫지 않나" → 정정: 파티션 DROP 기각 근거는 created_at 기준일 때만 성립. upbit_timestamp(체결 시각) 파티션이면 유니크 키가 gap-fill 재삽입도 막아 근본 해결이 맞음. 원장 교체 절차 규모·2층 원장 설계와의 중복 때문에 2층과 묶어 실행, 지금은 B·C·D 응급 처치. docs/26 §3 정정
- 09-18 11:33~11:40 UTC **응급 처치 3건 적용**(사용자 승인, docs/26 §3): C. `ALTER TABLE ADD INDEX idx_created_at` 온라인 82초, 적재 지속(60s 1,318행), EXPLAIN DELETE 풀스캔 → key=idx_created_at rows 1,173. B. Debezium `skipped.operations=d` PUT 200, 커넥터 자체 재시작 후 RUNNING, 로그 오류 0(42건은 설정 출력의 'errors.*' 키). D. **정정**: binlog 는 무기한이 아니라 compose `--expire-logs-days=3` 로 3일이었음(`binlog_expire_logs_seconds=0` 만 보고 오판, SET PERSIST 가 오류 4113 으로 알려 줌) → expire_logs_days 0 + binlog_expire_logs_seconds 30일 SET PERSIST(mysqld-auto.cnf 확인), compose 옵션도 교체. 검증 대기: 11:45 정리 실행에서 토픽 메시지 ≈ 적재 행·DELETE 소요·삭제 행 수. 또 worklog 의 앞선 4항목 시각이 추정치였음(12:05 등) → 커밋 시각으로 정정
- 09-18 11:42 UTC 실수: compose mysql `command` 옵션 줄에 인라인 주석을 넣어 config 가 깨진 채 커밋(bda0884) → 주석 제거·검증 후 수정 커밋. 어제(depends_on)와 같은 종류 — **compose 편집 뒤 `docker compose config -q` 를 커밋 전에 반드시**(두 번째 위반)
- 09-18 11:47 UTC **응급 처치 검증**(docs/26 §3-1): 11:45 정리 실행 낀 150초 창에서 토픽 +3,237 ≈ 적재 3,297(삭제 이벤트 0; 전 같으면 +11,535), MySQL 삭제 11,535행, DELETE processlist 1회·최대 1초(전 59초). 세 조치 모두 효과 확인. 24h 확인 항목 등록
- 09-18 11:50~12:00 UTC **dim_markets 완료**(docs/26 §4-1): upbit_market_master 테이블 + reconcile_trades `fetch_market_master`(289마켓, 첫 실행 429 로 32 unknown → 재실행 해소 → 재시도 로직), 상장일 근사 57 창 안/232 창 이전, dim_markets(테스트 4/4) coverage_gap_days BFC 6·JPYC 0·PYUSD 0, dq_rule_eval 96h 제외. 부수 발견: rules_daily 01:05 가 라벨 fetch :07 보다 앞서 같은 날 지정을 못 봄(09-17 exchange 0 → 재빌드 0.9/0.9) → 01:15 로. DAG 테스트 11/11. 편집 실수 3건(한 줄 구문 안 인라인 주석: 런북 루프·compose command·테스트 집합) → 규칙화
- 09-18 12:10~12:30 UTC **체결×호가 분 결합 마트 완료(docs/27)**: mart_trade_orderbook_1m(증분 일 단위, FINAL 체결 + orderbook_1m), 10일 하루씩 적재 3,779,219행·290마켓(하루 17~27s, 480MiB), 테스트 2/2. 걸린 것 3(YAML 콜론, LowCardinality(UInt8) 금지, FULL JOIN 기본값 1970 → coalesce 오류; 검증 조회가 잡음). **첫 답**: EURC 되튐은 스프레드(단조 아님)가 아니라 체결/깊이 — 변동 ≥300bp 분의 vod 중앙값 1.003 vs <10bp 0.002(500배), EURC 09-13 15:31 vod 5.4·변동 7,633bp. 유동성 플래그 후보 vod ≥ 1(검증은 2층 라벨 대조). docs/19 §2-1 3번 완료
- 09-18 12:35 UTC **하루 마감**. 오늘: 24h 판정·재기동 실측(24 완결) → RMT(25) → CDC 재검토·응급 처치 3건 검증·dim_markets(26) → 결합 마트·EURC 원인 정정(27). **내일 재개 체크리스트**: ① 01:15 UTC(10:15 KST) rules_daily → `dq_alert_parity_daily` 첫 실제 값(PRICE_24H 참조 09-18 01:46 이후분)·`dq_rule_eval_daily` 09-18 행(01:15 로 옮긴 뒤 exchange 0 문제 해소 확인) ② 24h 토픽 메시지 ≈ 적재 행(삭제 이벤트 0 유지, 관찰 CSV) ③ 06:35 대조 DAG(fetch_market_master 첫 스케줄 실행 포함) ④ 09-25 옛 테이블 crypto_trades_rmt DROP ⑤ 다음 작업: 2층 설계(가상 원장 + 체결 시각 파티션 재설계). 모든 시각은 UTC(KST = +9)
- 09-18 12:32 UTC 섀도 현황(참고): v2.1 PRICE_24H 전이 23건(02:17~12:31, 마켓 3 — KRW-G 1↔2 등급 전이, KRW-JPYC −50% 경계 분 단위 반복: 09-17 상장이라 96h 제외 대상). 첫 동등성 판정은 09-19 01:15 UTC(10:15 KST) rules_daily — 창이 하루 단위(09-18 01:46 ~ 09-19 00:00)이고 라벨이 매시 :07 에 오므로 일배치가 기준
- 09-18 12:50 UTC 사용자 "이후 작업은 DAE/DA 인계용 다듬기인가?" → 두 갈래 병행으로 정리(docs/19 §2-2): DE 본업(원장 CDC + 체결 시각 파티션 재설계, 재처리 실증, 스키마 계약, KRaft, 용량 계획) / DAE 인계(dim·마트·케이스·승격·품질 계약). 포트폴리오 로컬 문서 §10 에 같은 목록. 재생·증폭은 포트폴리오 §4 에 이미 반영
- 09-19 01:16~01:40 UTC **첫 동등성 판정(docs/22 §4-4)**: 70/68/66, 미매칭 6 = KRW-G ±100% 경계 1분 뒤집힘 3쌍. 참조 동일·늦은 도착 0 → 원인 = 분 종가 정의(도착 순 vs 이벤트 순, 재정렬 5.87%). v2.1.1: sequential_id 가 큰 체결로만 종가 갱신(현재·직전 열린 분), 테스트 2개, 빌드 중. 규칙 평가 PRICE 09-18 정밀도 1.0·재현율 0.283 — 거래소 재지정 습관(G 39건/일) 때문에 전이 기준이 부적절, 상태 기준 49/60=0.817(창 이전 GEOD 제외 0.89). VOLUME 0.929/0.812. 밤사이: 토픽 vs 적재 차 0.3%(삭제 이벤트 0 유지, 전 46%), 복제 부족 0, health 알림 3건 = 단발 insert 스톨 10~22s(12:10·12:20 마트 적재/RMT 머지, 16:00 dbt) — p95 정상
- 09-19 01:45 UTC v2.1.1 배포: savepoint-ce93ca-9d3db2d0ce07 → 49d1fdae, trade_id 119,125,047 → +1, 복원 확인, 3잡 RUNNING. rule_version v2.1.1-shadow. 다음 판정 09-20 01:15(창 09-19 하루). 06:35 대조 DAG 는 fetch_market_master 첫 스케줄 실행 포함 — 확인 항목
- 09-19 01:50 UTC dq_rule_eval_daily 에 state_recall(지정 시각 +10분 안 우리 등급 > 0 비율) 추가 — 09-18 PRICE 0.817. 승격 재현율 기준을 이 열로(docs/22 §4-4)
- 09-19 01:55 UTC 실수 2건: state_recall 수정을 빌드 확인 전에 커밋(4414ea8: USING 두 번 → ClickHouse 오류), 고친 뒤엔 p.day 열 이름 문제(어제 동등성 모델과 동일) → 별칭. 규칙 재확인: 모델 편집 뒤 build 통과를 보고 커밋, 조인 결과 열은 항상 별칭
- 09-19 02:00~02:15 UTC **docs/28 2층 설계**(결정 대기). A 체결 테이블 재설계: upbit_timestamp 일 파티션 + PK(trade_id, upbit_timestamp) + UNIQUE(market, sequential_id, upbit_timestamp)(sequential_id = ts×10,000+n 실측), DELETE→DROP PARTITION, Debezium message.key.columns=trade_id 고정, 무정지 교체 절차(드라이런→sql_log_bin=0 복사→RENAME→차이분→검증), 위험 = Debezium 의 RENAME 이어 받기(더미로 선실험). B 가상 원장: orders(mirror)/fills(append)/positions, 규칙 기반 paper trading(Faker 없음), Kafka 엔진+MV 소비. C 케이스 테이블·내부 신호 4후보. 순서 A→B→C, A-0 드라이런부터
- 09-19 02:20 UTC 사용자 "실무는 시각이 특히 중요, 지연도 거기서 나옴, 다시 안 바꾸게 철저하게" → docs/28 A-2-1 스키마 검토표: 시각 5개(거래소 upbit_timestamp / 수신 recv_ms(신설) / MySQL created_at / binlog source_ts / flink_ts)로 e2e 를 4구간으로 분해, 행 출처 ingest_source(신설)·stream_type(신설), 키·파티션·시간대 원칙, 추가 컬럼은 DEFAULT 로 4시스템 동시 변경 회피
- 09-19 01:35~01:45 UTC **A-0 드라이런 완료(docs/28 A-4)**: 파티션 경계 5일 오차 발견·수정(코드 계산), 09-18 2,676,324행 75초 복사(sql_log_bin=0, CDC 누출 없음, 프로덕션 p95 불변), INSERT IGNORE 재삽입 0, DROP PARTITION 2.68M 행 0초, Debezium RUNNING. **RENAME 이어 받기 실험**: 더미 캡처 테이블 스왑 → WARN 2줄 남기고 계속 캡처(스왑 후 INSERT 도착) → A-3 실행 가능. 사용자 "검토 점 전부 포함" → A-5 ClickHouse 재생성(체결 시각 파티션·(market, upbit_timestamp, sequential_id) 키) 설계 추가. 정리: include.list 복원·더미 삭제. 다음: A-1 실행 결정
- 09-19 01:50~02:00 UTC 사용자 "Kafka 사용 방식이 적절한가, 냉정하게" → docs/29: 브로커는 문제 아님, 메시지 설계 셋이 실측 피해 있음 — ① 체결 키 trade_id(마켓 내 순서 깨짐 = 재정렬 5.87%·종가 버그의 원인) → market 키 ② 체결 집계가 처리 시간 창(호가는 이벤트 시간) ③ 압축 없음·bytes 보존(800B/건 9.97GB, 재처리용 시간 보존 없음). 그 외 파싱 실패 DLQ 없음, 스키마 계약 없음, JMX exporter 없음. #1·#3 은 A-1 창에 포함
- 09-19 02:05 UTC 사용자 "실무 관점 부족 많다, 개선 방법과 이유" → docs/29 §4 개선 계획(창 3개, 항목마다 왜·어떻게·검증): 창1 = A-1 과 함께 market 키·zstd·7일 보존, 창2 = Flink 파싱 DLQ·집계 창 폐기/이벤트 시간, 창3 = 스키마 계약·JMX exporter. 쿼터·ACL·TLS 는 근거 약해 안 함
- 09-19 02:10 UTC 사용자 "격리·보안 안 하는 근거가 합당한가" → 아니었음. 위협 모델로 재판단(docs/29 §5): TLS 는 근거 있게 안 함, **노출은 실측으로 문제** — 9092·2181·8083·8081 이 0.0.0.0, LAN IP 에서 무인증 토픽 조회 성공, Connect·Flink REST 는 LAN 에서 조작 가능. 조치: `scripts/ops/lan-firewall.sh`(DOCKER-USER 차단, 사용자 sudo 실행) → 다음 재기동 창에 127.0.0.1 바인딩. "비용"을 근거로 쓴 문장을 정정
- 09-19 02:15 UTC 사용자 "KRaft 로 바꾸면 다시 해야 하나" → docs/29 §6: 방화벽·바인딩·Debezium 키·DLQ·계약은 유지, 토픽 설정(압축·보존)만 새 클러스터에 재적용 → 토픽 정의를 코드(`kafka-topics.sh`)로 두어 컷오버 때 재실행. 하루 기록 점검: docs/22 §4-4·docs/28 A-4·A-5·docs/29 §1~§6·worklog 전부 커밋됨
- 09-19 01:50~01:58 UTC **A-1 착수**: 실행 직전 냉정 점검에서 4건 추가(trade_id AUTO_INCREMENT 충돌·옛 EVENT 비활성·p_max 자가 치유·롤백 되돌림 복사, docs/28 A-3-1) → 런북 `mysql-trades-partition-swap.sh`(prepare/copy/verify/swap/finalize/rollback). prepare: 새 테이블 생성 OK, 프로시저는 `mysql -e` 가 본문 세미콜론에서 끊겨 실패 → DELIMITER 파일(`manage_trade_partitions.sql`)로. **실수**: 프로시저 테스트 사본을 **복사 중인 새 테이블**에 호출해 p20260911 DROP → 직전에 복사된 09-10 159,976행 유실(INSERT IGNORE 라 재복사로 복구). 교훈: 진행 중인 대상에 테스트를 걸지 말 것 → 스왑 전 일별 건수 대조(verify 단계) 강제
- 09-19 02:00 UTC Kafka 창1 일부 적용: `scripts/ops/kafka-topics.sh`(토픽 정의 코드화) → 체결 토픽 zstd·retention.ms 7일(+bytes 8GB 안전장치), DLQ 토픽 `cdc.dlq.crypto_trades` 생성(create 인자 조립 버그 1회 수정). 프로듀서 오류 0, 적재 정상. market 키 변경은 A-1 스왑 뒤(한 창에 한 변경). 복사 로그: 09-10·09-11 이 내 테스트 DROP 에 지워짐(dst 0) → verify 에서 잡히면 재복사
- 09-19 02:03 UTC 사용자 "A 뒤 3단계 끝나면 끝인가, KRaft 는 그때?" → 아님. 순서: A-5 ClickHouse 재생성 → Kafka 창2(파싱 DLQ·집계 창 폐기) → 도구 정리 → **B 가상 원장(1일) → C 케이스·신호(1일) → 섀도 승격(판정 누적) → 스키마 계약·JMX(반나절씩) → KRaft 컷오버(이미지 7.6+·타 세입자 오프셋 이식·127.0.0.1 바인딩을 한 정지 창에)**. docs/19 §2-2·docs/24 §6 과 일치
- 09-19 02:05 UTC 사용자 지시: 복사 완료 + verify 결과까지만 보고하고 정지. 스왑 이후는 사용자 승인 뒤 진행(복사가 잘됐는지 먼저 확인)
- 09-19 02:05~02:15 UTC A-1 copy 완료(10일 16.86M행, 하루 38~95초, 프로덕션 p95 불변). verify: 09-10·09-11 은 내 테스트 DROP 탓 dst 0 → 재복사로 일치. 남은 불일치 2건은 정상 — 09-12 는 옛 테이블이 보존 DELETE 로 줄어 dst ≥ src(23,480), 09-19 는 복사 뒤 유입으로 src ≥ dst(차이분은 swap 이 옮김) → verify 판정 규칙에 반영. p_max 0. **스왑은 사용자 승인 대기**
- 09-19 06:32~06:37 UTC **A-1 스왑 완료**(사용자 승인, docs/28 A-6): EVENT disable → AUTO_INCREMENT 120,040,919 → RENAME 06:32:51 → 차이분 672k 18초. 검증: 새 첫 id 120,040,919, 스왑 전 오늘 행 old=new 1,419,753, Debezium WARN 2줄·RUNNING·새 컬럼 3개 실림, CH 연속성 +1, 적재 3,311/60s p95 4.54. finalize: 유지보수 EVENT 매일 00:05, cleanup_old_trades 삭제. Kafka 창1 마무리: message.key.columns=market 적용, 키 {"market":…} 확인, 적재 지속. 남은 확인: 09-20 00:05 첫 자동 DROP/ADD, 재정렬률, 09-26 옛 테이블 DROP
- 09-19 06:45 UTC 사용자 "A-5 는 뭘 어떻게 바꾸나, 이유·실무" → docs/28 A-5 보강(파티션=체결 시각 월, 키=(market, upbit_timestamp, sequential_id), 컬럼 3 추가, 바꾸지 않는 것, 실무 원칙 대비). 승인 대기
- 09-19 06:39~06:57 UTC **A-5 ClickHouse 재생성 완료**(사용자 승인, docs/28 A-7, `scripts/ops/clickhouse-trades-v2-cutover.sh`): 체결 시각 월 파티션 + (market, upbit_timestamp, sequential_id) 키 + 새 컬럼 3. 일 슬라이스 218일 114.15M행 9분(p95 불변) → verify 218일 uniq 불일치 0, 2월 −4행은 옛 키가 못 거른 재스냅샷 중복을 새 키가 정리한 것 → EXCHANGE 06:56:34 정지 없음, 차이분 58,988·잔여 63. 프루닝 실측: 1시간 조회 39/39 파트 1.0s → 5/56 파트 0.10s. 옛 표 crypto_trades_v2 09-26 DROP
- 09-19 06:50 UTC 실수: verify 첫 판을 월별 uniqExact 한 쿼리로 짜서 메모리 한도 초과(1.12GiB) → 일 단위 분할로 수정. 어제 24M INSERT 와 같은 종류(큰 집계는 나눈다)
- 09-19 07:10~07:25 UTC 사용자 "Kafka 를 분산 처리의 의의에 맞게 쓰고 있나 / 없이도 됐나 / 끝까지 가도 같나 / 분산 의의를 살리는 게 낫지 않나": 정직한 답 = 분산 성질(복제·병렬·확장)은 안 쓰고 로그 성질(오프셋 재개·생산자/소비자 분리·다중 소비자·키 순서)을 쓴다. Kafka 없이 Flink CDC 직결로도 됐고 처음 고른 이유는 역량 증명(README 표). B(원장 4테이블·소비자 3종·상태 전이의 키 순서)와 창2(DLQ·계약)에서 로그 성질의 필요가 커진다. 단일 호스트(코어 4·NVMe 1)라 장애 격리·확장은 끝까지 불가, docs/24 결론 유지
- 09-19 07:25 UTC 내 제안 "컨슈머 리밸런스 실측" 을 냉정 점검 후 **철회**: Flink KafkaSource 는 파티션을 자체 할당(정적)하고 group.id 는 오프셋 커밋용이라 TM 을 죽여도 Kafka 리밸런스가 아니라 Flink 페일오버(이미 여러 번 실측)가 일어난다. 진짜 리밸런스는 ClickHouse Kafka 엔진(kafka_num_consumers=2, 게임 이벤트 큐)에만 있고 우리 파이프라인의 관찰된 문제와 무관 → 만들어 낸 시나리오라 원칙("실측은 우리가 가진 질문에 답한다")에 어긋남. README 3-broker 표기 3곳(그림·스택 표·Q6)은 축소 뒤 미갱신 → 다음 커밋에서 수정
- 09-19 07:05~07:25 UTC **Kafka 창2 완료**(docs/29 §7, `scripts/ops/flink-cdc-redeploy.sh`): 파서 ProcessFunction 화 + 파싱 실패 사이드 아웃풋 → `cdc.dlq.crypto_trades`(원문+사유) + 카운터 parseFailures/skipped, 새 컬럼 3 통과(recv_ms 는 producer 미구현이라 NULL), 5분 처리 시간 집계 폐기(Grafana 패널 2개 → 원본 체결 시각 5분 버킷), uid 명시. 테스트 14/14. 재배포 savepoint → 696c9170, 정지 51초, 재시작 창 MySQL 2,665 = CH 2,665. DLQ 실증: 깨진 JSON 주입 → DLQ 1건·카운터 1·적재 지속. health_check `check_parse_failures`(증가분 알림) + DAG 테스트 11/11
- 09-19 07:22 UTC 실수 2건: `docker exec` 에 `-i` 없이 stdin 파이프 → producer 가 조용히 아무것도 안 보냄(첫 주입 "실패"의 원인은 파이프라인이 아니라 내 명령). Flink 연산자 메트릭 id 는 공백→`_`(`CDC_Event_Parser.parseFailures`)이고 합산은 `/subtasks/metrics` — "n/a" 를 "안 됨"으로 단정 않고 id 목록으로 확인
- 09-19 07:25 UTC README 정정: 3-broker 표기 4곳(그림·스택 표·체크리스트·Q6) → 1-broker 축소 반영, 5분 집계 폐기 반영. dbt sources 의 trade_aggregations 는 이력 전용으로 표기
- 09-19 07:25~07:32 UTC **도구 정리 완료**(docs/28 A-8): producer 가 recv_ms·ingest_source('ws'/'gapfill')·stream_type 을 채움, backfill 도구는 'backfill'. 재기동 17초 빌드 + 기동 gap-fill 23행(전부 gapfill 표시). **시각 6개 첫 실측**: 거래소→수신 82ms, 수신→INSERT 1,083ms, MySQL→Flink 1,697ms(ws 1,788행 평균). 정정: reconcile 은 MySQL 을 안 읽고 backfill 은 이미 upbit_timestamp 창이라 "도구가 created_at 창" 메모는 과장이었음
- 09-19 07:35 UTC 사용자 "냉정하게 B 가 나은가 / 캡처 기능을 살리는 게 B 인가": 그렇다 — 지금 CDC 는 INSERT 만 잡아 큐와 같고, UPDATE·DELETE 를 키 순서로 따라가 최종 상태를 재구성하는 것이 캡처의 의의. B 주문 테이블이 첫 사례. 규모 확장(Binance 실시간)은 같은 범주의 양이라 B 뒤 별도 결정
- 09-19 07:35~07:45 UTC 사용자 "B 들어가기 전에 Upbit·Binance 로 UPDATE/DELETE 를 정말 못 받는지 다른 루트로도 확인해라" → **문서 + 실제 소켓 구독**으로 재검증(docs/28 B-0). 결과: Upbit 체결은 불변(맞음)이나 candle(같은 캔들 8회 갱신)·ticker(market_state)·orderbook 은 키 단위 갱신 스트림이고, 주문 생애주기(myOrder)는 실돈 필요·테스트넷 없음·"주문 테스트 API"는 생성 안 함. **Binance 는 틀렸음**: depth diff 6초 1,632 갱신 중 762 가 quantity 0(레벨 삭제), Spot Testnet 이 가상 자금으로 executionReport(주문 상태 전이)를 준다(호스트에서 수신 확인, 시세는 프로덕션과 동일 호가, 체결 0.9/s, 월 1회 리셋). B 설계 대안 = 우리 규칙 + 테스트넷 엔진 체결. 교훈: 구독해 보기 전에 "없다"고 말하지 않기(feedback_no_absence_claims 세 번째 사례)
- 09-19 07:50~08:00 UTC 사용자 "그렇게(전 필드 뜯어서) 찾아볼래, 아니면 진짜 없어?" → **전수 조사**(docs/28 B-0-2): Upbit 문서 인덱스로 엔드포인트 전부 나열(시세 12·거래 40여·WS 공개 4·비공개 3), 후보 페이지 응답 필드 전부 확인 + 우리 대조 68,088셀에서 "우리 > 거래소" 0. 결과: 체결 취소·정정은 진짜 없음. 그러나 상태 변경 데이터는 있음 — 공개 candle·ticker(market_state DELISTED·delisting_date·is_trading_suspended)·market_event, 키만 있으면 announcement(CREATED/UPDATED)·service-status(wallet_state), 실돈 있어야 myOrder(state 6종)·myAsset·입출금 state. 새로 안 것: WS announcement 스트림(주문 없이 키만으로 생성·갱신 이벤트)
- 09-19 08:00~08:10 UTC **Binance 전수 조사**(docs/28 B-0-3): 저장소 파일 전부 → 스트림 15종·거래 REST 14종·유저 데이터 이벤트 6종·enum 전부. 새로 안 것 ① `order/amend/keepPriority` = orderId·큐 우선순위 유지한 채 수량 축소(진짜 in-place UPDATE, executionReport x=REPLACED) ② **Demo Mode**(demo-api/demo-stream, 라이브 시세 기반 시뮬레이션 체결, 실계정 필요) — 우리 호스트에서 demo 스트림 5초 7건·REST 200 확인 ③ Testnet 은 월 1회 예고 없는 리셋(키 보존), 2026-03 listenKey 폐지 → WS-API 구독. **B-0 최종 결론** 기록: 체결 취소·정정은 두 거래소 다 없음(진짜), 상태 변경 데이터는 둘 다 있음(내가 빠뜨림), 실돈 없는 주문 생애주기는 Binance Testnet/Demo 뿐. B 권고 = 우리 규칙 + Binance 엔진 상태 전이 + Upbit 마켓 상태·공지 SCD
- 09-19 08:15~08:45 UTC **B 착수(사용자 승인, docs/28 B-1~B-6)**: MySQL 원장 4테이블 + `ledger` 사용자, 두 번째 Debezium 커넥터(거울, 삭제 유지, RUNNING), `ledger.*` 토픽 4개, ClickHouse Kafka 엔진+MV → RMT(version, is_deleted). **키 없는 스모크 통과**: 주문 c→u→u→d 가 ClickHouse FINAL 에서 재구성(CANCELED/v3)→삭제(0행), 체결·원문 동일. 실수 2건(MV 의 payload 경로 → 0행, raw 컬럼 충돌). `virtual-trader` 코드(Ed25519 WS-API, 규칙 A/B/C, last_exec_id 기반 멱등 갱신, 리셋 감지) + 테스트 7/7(client id 구분자 버그 1건 잡음), compose profile ledger, 키 생성 스크립트. 남은 것 = 사용자 키 발급 → 기동 → B-5 검증
- 09-19 08:12~08:35 UTC **B 실행·검증 완료**(docs/28 B-7): 사용자 키 등록 뒤 기동 → 주문 71·체결 59, 전이 NEW/FILLED/REPLACED(amend)/CANCELED/EXPIRED 전부 실증, MySQL version 1→3 = ClickHouse FINAL. **3자 대조 불일치 0**(거래소 REST = MySQL = CH, 5심볼, dbt dq_ledger_daily + health_check 알림). 시각 사슬 p50: 거래소→수신 21ms→binlog 6→Debezium 3→Kafka 192(거래소→Kafka 219ms). 실수 6건 기록(clientOrderId 문자 규칙, WS 메서드명, REST 서명 순서, dbt 별칭, _timestamp_ms 초 잘림, docker cp 중복 경로) + ch_inserted_at 이 폴링 시작 시각이라 지연 계산 불가 발견 → kafka_ts_ms 추가
- 09-19 08:40~08:50 UTC 사용자 "C 가 뭐였나, Binance 원장과 Upbit 시세를 섞는 게 의미 있나" → 답: CDC 층의 의의는 거래소와 무관(변경 캡처·재구성·외부 정답 대조), 시세×원장 조인은 "같은 시장"이 아니라 **같은 코인** 단위로만(교차 거래소라고 명시) 의미가 있다(유의 지정·폐지 공지 ↔ 우리 주문). 냉정 평가: 한계 효용 꺾임, 운영 부담 증가(컨테이너 ~20, 외부 테스트넷 의존), 케이스는 사람이 판정 안 하면 빈 테이블, 진짜 남은 구멍은 기능이 아니라 승격 판단·README·교훈 통합. 추천 = C 하루(마켓 상태 SCD + 케이스 최소 + 교차 신호 1) → 정리 주간. 사용자 승인 "진행해, 기록 꼼꼼히"
- 09-19 08:50~09:10 UTC **C 완료**(docs/28 C-0~C-3): `dim_market_flag_scd`(거래소 이력 16,202구간·현재 61 + 폴링 WARNING 6, 첫 판 transition 필터 실수로 0행 → 상태 변화 검출로 수정), MySQL `cases` 거울 테이블 + 커넥터·토픽·CH RMT + Airflow `cases_hourly`(근거 3종, 첫 실행 0건, MySQL 쓰는 첫 DAG — 컨테이너 재생성은 --no-deps 필요), 케이스 거울 스모크 c→u→u→d 통과(CLI 한글은 utf8mb4 플래그 필요), `sig_cross_venue_flag_overlap` 0행(우리 코인 9월 경보 4건이 거래 시간과 안 겹침 — 조인 맞음). DAG 테스트 12/12
- 09-20 00:12 UTC(KST 09:12) **아침 점검**: ① A-1 첫 자동 파티션 유지보수 00:05 실행 확인 — p20260912 DROP, p20260921 생성, p_max 0 (EVENT last_executed 00:05:00) ② 적재 60초 8,210행·p95 3.94s, 커넥터 2개 RUNNING, Flink 3잡 RUNNING, DLQ 오프셋 1(주입분 그대로 = 실제 실패 0) ③ 생성기 16시간 무오류: 주문 238·체결 214·amend 32·취소 21, 원장 340건(FILLED 295·CANCELED 33·EXPIRED 11·PARTIALLY_FILLED 0), 시간당 3자 대조 마지막 23:38 불일치 0, 케이스 0 ④ 디스크 28%, MySQL 컨테이너 메모리 986MiB/1GiB(한도 근접 — 확인 필요) ⑤ 옛 표 3개 보존 중(DROP 09-25/26). 다음 01:15 UTC 동등성 판정
- 09-20 00:20~00:45 UTC **정리 주간 시작**: README "증명하는 것"을 결론 8줄(각 실측·문서)로 재작성 + 2층 경로 그림 + 지표(시각 6개·원장 219ms·프루닝 1.0→0.10s) + Q15~18(Kafka 분산 여부·INSERT 만인 CDC·파티션 키·부재 단정), 여정 인덱스 에피 19~27 + 블로그 9~12 제안, `docs/30-lessons-consolidated.md`(실수 7범주·체크리스트). MySQL 컨테이너 메모리 986MiB/1GiB(버퍼 풀 512M + 기본 오버헤드, OOM 이력 0) — 한도 1.25G 상향은 재시작이 필요해 사용자 결정으로
- 09-20 00:18 UTC **둘째 동등성 판정(수동 선실행, docs/22 §4-5)**: 09-19 SQL 116 = Flink 116 = 매칭 116, 미매칭 0 → v2.1.1 승격 기준 충족. state_recall 0.559 는 미탐이 아니라 ① 배포(01:16) 전 10건은 v2.1 알림으로 존재 ② ASOF 최신 상태와 분 단위 플래핑의 어긋남 — 직접 대조 시 59건 전부 근처 전이 있음. 재정렬률 8.09%(키 변경 전) → 0.995% → 09-20 0% 로 docs/29 창1 검증 닫음. 01:15 스케줄 실행은 재확인용
- 09-20 00:30~00:45 UTC **Binance 실시간 확장 1단계 착수·가동**(사용자 승인 "Flink 가 의미 있을 정도로", docs/31): 해체 분석 뒤 결정 — 분산은 안 살리고 파티션·병렬·상태 설계를 살린다. 수집기(493 USDT 심볼, 연결 2개 400+93 스트림, 333 msg/s, lag p50 27ms), 토픽 6p zstd 3일, ClickHouse RMT(recv_ms) 일 파티션 30일, Flink 별도 잡(DLQ·카운터·배치 1,000/3s, 테스트 2), TM 슬롯 4→6 재생성(3잡 체크포인트 자동 복원, CDC 창 5,277=5,277). **실수**: 병렬 3 으로 제출 → 슬롯 산수(6 − CDC 2 − 호가 1 − 서킷 1 = 2)를 안 해 NoResourceAvailable 재시작 루프(2/3 서브태스크만 소비) → 병렬 2 로 재제출(00:41). health_check 잡 4·binance 적재 체크, reconcile_binance DAG(1h 캔들 체결 수, 00:40) + dq_binance_reconcile_daily, DAG 테스트 12/12
- 09-20 00:45~00:58 UTC **Binance 2단계 호가장 재구성 가동**(사용자 "24h 기다리지 말고 깎아나가자" → 가드레일: 상위 10 심볼, 슬롯 8 재생성 1회, docs/31 §5): depth 수집 모드(증분 70/s + 5분 REST 스냅샷 같은 키), OrderBookReconstructor(키별 TreeMap·lastUpdateId·synced, 공식 절차, 테스트 2), Upbit 집계기·싱크 재사용(접두 binance_). 첫 5분 unsynced 11,925 폐기(LATEST 시작·설계대로) → 스냅샷 뒤 gap 0, ETH ask 2627.11 = 거래소. Flink 잡 5/5, 슬롯 8/8. Kafka CPU 152% 첫 샘플은 CLI JVM 착시(docs/23 재현), 기준선 35~45%. **호스트 load 4.0~4.7/4코어(확장 전 2.3)** — 평일 피크 관찰이 핵심
- 09-20 01:20 UTC 포트폴리오 문서(로컬 전용 docs/portfolio-de-highlights.md)를 JD 문장별 매핑으로 재작성: 업무내용 5개·필요 역량 4·선호 3 각각 한 일·왜(대안)·실측·문서, 결정 12, 실수 정정, 숫자 카드(조건), 못 하는 말, 30초/2분 답변
- 09-20 01:20~01:46 UTC **사고: Airflow 스케줄러 cgroup OOM 루프**(kern.log 01:20·01:25·01:30·01:35·01:41, 재시작 12회/40분, health_check 01:10 실행이 running 에 고착, cases_hourly 첫 실행 못 함 → 감시 공백 30분). 원인(추정, 근거): 컨테이너 한도 640M 에 LocalExecutor 병렬 기본 32 — health_check 체크 12개(오늘 +2: 원장 대조·Binance)가 동시에 프로세스로 뜨고 DAG 7개(오늘 +2) 파싱이 겹침. 조치: 한도 1024M + `AIRFLOW__CORE__PARALLELISM=8`·`MAX_ACTIVE_TASKS_PER_DAG=8` + health_check `max_active_tasks=6`(두 물결), 스케줄러 재생성 01:45:50. 발견 경로: 내 백그라운드 작업 2개가 "호스트 메모리 부족"으로 죽은 알림 → free 394MB(available 6.4GB, swap 3.3GB) → kern.log. 편집 실수: compose 블록 경계를 `'  airflow-scheduler:'` 문자열로 잡아 depends_on 의 같은 이름에 걸림(3번 빗나감) → 줄 시작 앵커로
- 09-20 02:05 UTC **Binance 첫 시간 대조 100.000%**(01:00 UTC, 491 심볼, 1,193,357 = 1,193,357, 불일치 셀 0; 스크립트 첫 판은 epoch 를 손으로 써서 5시간 틀림 → calendar.timegm). 스케줄러 재생성 뒤 10분: 재시작 0·health_check success 3회. 디스크 +10GB 는 빌드 캐시(4.3GB 회수)·이미지 계층, query_views_log 2GB(2월~, TTL 없음)
- 09-20 02:17 UTC 디스크 증가 귀속(10.5분 창): Kafka +77MB(≈10GB/일이지만 토픽 보존 7d/1d/3d 로 상한이 있어 정상 상태), ClickHouse −1,817MB(query_views_log 옛 파티션 DROP), MySQL +7MB, 컨테이너 쓰기 계층 변화 없음 → 01시의 +10GB 는 빌드 캐시·이미지 계층이었음이 확정. load 2.0~2.8(일요일 KST 11시), Binance 체결 278/s
- 09-20 02:28~02:31 UTC **운영 정리 3건(사용자 승인, 케이스 판정만 제외)**: ① kafka-ui·Prometheus·statsd-exporter 를 compose `profiles: ["ops"]` 로 빼고 정지·제거(컨테이너 19→16, Airflow `STATSD_ON=false`, webserver 의 statsd 의존 제거, Grafana 의 airflow-operations 대시보드는 데이터 없음 상태로 남김) ② MySQL 한도 1G→1.25G 재시작: healthy 17초, producer 는 연결 끊김으로 재시작(restart 2회째)→ 기동 gap-fill 83.2초 창 289마켓, Debezium 2개 RUNNING 유지, 생성기 재연결 오류 0 ③ 검증: 재시작 전후 trade_id 창 MySQL = ClickHouse(아래 수치), 호스트 available 6.4→7.0GB. 승격은 하지 않음(받는 사람 없음)
- 09-20 02:40~02:55 UTC **SLO·알럿 체계(docs/32)**: SLO 표(15 지표, 실측 기준선·행동), 알럿 3층 설계(실무 뒷단 ↔ 개인 규모 대응표), `ops_metrics_5m`(5분 cron, Prometheus 대체)·`alert_events`·`ops_digest` 테이블, `quality_alerts` DAG(매시 :50, dq 4~5 규칙 하루 1회 dedup), `weekly_digest` DAG(월 09:00 KST: 트래픽 피크·지연·알럿·품질·자원·고칠 것), health_check 알럿 → alert_events. 실수 4건: 수집기 read 분할 열 어긋남(Code 27), 별칭이 WHERE 열을 가림(500), 훅 latin-1(한글 저장 실패 → utf-8), tasks test 미래 시각. 첫 다이제스트: Upbit 피크 KST 9시 75/s, Binance 11시 394/s, 대조 100%
- 09-20 02:56 UTC cron 첫 자동 행 확인(02:55:25): load1 5.79, Binance 5분 248,671행(**829/s**, KST 정오), e2e p95 2.56s, Flink 5. **발견**: cpu_kafka 133% — 수집 스크립트 자신의 Kafka CLI(docker exec JVM) 뒤에 docker stats 를 찍어서(docs/23 147% 착시 재현, 세 번째) → docker stats 를 스크립트 맨 앞으로 이동
- 09-20 03:00~03:20 UTC **세 관점 구조 평가(docs/33)**: 기록 점검(README 트리 14·31·32 누락 보완, 여정 에피 28 추가) → 돌아가는 것 직접 조회(테이블 52·dbt 19/테스트 17·DAG 9·포트·자격증명·중복·FINAL·죽은 산출물). 발견: ① 0.0.0.0 노출에 3306·8123 포함, producer 가 MySQL root ② RMT 를 FINAL 없이 읽는 모델 6개(지금 중복 0 이라 잠재) ③ 재처리 런북 없음 ④ 하루 정의 KST/UTC 혼재·명명 불일치·코인 차원 없음 ⑤ 죽은 산출물 6종(anomaly_alerts 09-17 이후 정지·Grafana 패널 참조, coin_metadata 참조 0 등) ⑥ 새 테이블 테스트 0 ⑦ 금액 Float64(회계 불가). 우선순위 10건, 예상 질문 12개
- 09-20 04:00~04:05 UTC **보강 #1 보안(docs/34)**: 3306·8083·8123 → 127.0.0.1, producer 전용 MySQL 사용자(root 제거). 재생성 뒤 Debezium 태스크 2개 FAILED(MySQL 다운 순간 접속 오류) → 즉시 재시작, 유실 0(MySQL 6,001 ⊂ CH 6,190). 남은 노출: Kafka/ZK(정지 창)·Flink 8081(JM 재생성 필요)·Grafana/Airflow(인증 UI). 방화벽은 사용자 sudo 대기
- 09-20 04:05~04:08 UTC **보강 #2 FINAL**: RMT 를 읽는 모델 6개에 FINAL(7/7), 빌드 6~9초, 중복 주입 실험 원본 2 → stg 1. 실수: 주석을 config 블록 안에 넣어 Jinja 깨짐(4초 만에 조용히 끝남)
- 09-20 04:10 UTC **정정**: #2 검증에서 "원본 2" 는 틀림(한 INSERT 의 두 행은 RMT 가 삽입 때 접음). 별도 INSERT 두 번으로 재검증 → 원본 2, stg(FINAL) 1. 검증 자체의 검증을 안 한 실수
- 09-20 04:08~04:15 UTC **보강 #3 시간 규약**: day_utc / day_kst·hour_kst 로 15모델·소비자 4·대시보드 6패널 동시 변경, 검증 통과. 드러난 것 2: MySQL trade_amount DECIMAL(20,4) 로 먼지 체결 64,669행이 amount 0(→ #5), no_long_gaps 테스트 전제가 287마켓에 틀림(스테이블 3~5h 공백 정상) → 유동성 마켓만
- 09-20 04:15~04:20 UTC **보강 #4 차원·사이드·환율**: binance_symbols(3,665)·dim_venues·coin_alias seed·dim_coins(289, 양쪽 207, 별칭 MANTRA↔OM)·taker_side 통일(Binance 반반)·int_fx_usdt_krw_hourly(KRW-USDT)·sig_kimchi_premium_hourly(BTC ±0.1%, 극단 LSK/EGLD 얇은 마켓). 실수 3(LEFT JOIN ''·FINAL 위치·LowCardinality CAST)
- 09-20 04:36 UTC **세션 끊김 지점 확인·기록**: 보강 #1~#4 완료·커밋·푸시(f0aac63), 작업 트리 깨끗, 파이프라인 정상(Flink 5잡·커넥터 2·Binance 250/s p95 2.94s·Upbit 22/s p95 4.89s·load 4.11). **#5 Decimal 은 조사만 하고 코드 미변경** — 바꿀 지점 6개(모델·파서·싱크·Binance 3파일·ClickHouse 타입, MarketAlertDetector 는 비율 판정이라 double 유지)를 docs/34 §5-준비에 표로 남김. docs/34 상태 열을 실제와 동기화
- 09-20 05:00~05:10 UTC **보강 #5 중 발견·정정: ClickHouse `CAST(Float64 → Decimal)` 은 반올림이 아니라 버림**. 1차 복사에서 0.00518(Float64 실제값 0.005179999…)이 0.00517999 가 되어 KRW-PEPE 한 행 금액이 42.86 KRW 어긋났고(차이 = volume × 1e-8), 월별 **금액합이 2e-8 만큼 작게** 나와 잡혔다 — 행수·수량합만 봤으면 통과했을 오류. `toDecimal64`·`accurateCast`·`CAST(round(x,8))` 전부 같은 버림. 정답은 `toDecimal128(toString(x), 8)`(그 double 로 되돌아가는 최단 십진 표기 → 원본 DECIMAL(20,8) 복원). MySQL 원본 대조로 확인: volume 4285662548.26254840, amount 22199732.0000000007120000 정확히 일치. 지수 표기(1e-8·3.2e-7)도 파싱됨. 표 비우고 재복사
- 09-20 04:40~05:19 UTC **보강 #5 완료(docs/34 §5-실행)**: 금액·수량 Float64 → Decimal. Flink 7파일 BigDecimal(탐지기는 double 유지 — 세이브포인트·판정 불변, 테스트 9/9 무수정 통과), amount·quote_qty 는 price×volume 계산, ClickHouse 두 표 무정지 EXCHANGE(창 86초), dbt 비율 7모델 toFloat64(Decimal 0 나눗셈은 예외를 던져 모델이 죽는다 — if 가드 무용), 분 마트 12일 Decimal 재생성. **검증**: 복사 219일 행수·수량합·금액합 정확 일치, 재기동 구간 2,969=2,969, 새 행 amount≠price×volume 0/2,715, 원천 금액합 = 마트 금액합 소수 20자리 동일, dbt 24/24·41/41. **발견**: ClickHouse CAST(Float64→Decimal) 은 버림 → toString 경유 필수(1차 복사에서 KRW-PEPE 42.86 KRW 오차, 금액합 2e-8 로 적발 — 행수만 봤으면 통과). **실수 2**: 돌고 있는 스크립트 편집(09-18 반복), --vars 날짜 무따옴표로 12회 조용한 실패
- 09-20 05:20~05:30 UTC **보강 #6 완료: 재처리 런북**(`scripts/ops/reprocess-day.sh`, docs/34 §6-실행). Kafka 재소비 + 같은 Flink 잡(CDC_START/END_TS_MS → setBounded, 잡이 스스로 종료) + lab TaskManager 2슬롯 임시 사용. **발견**: Kafka 구간은 도착 시각이라 체결 시각 창을 못 덮는다 — 1차에 29행 누락(10:59:59 체결·11:00:00.46 binlog), 여유 10분 + 커버리지 직접 측정으로 2차 **0**. 실측 1시간 121,236행 31초, FINAL 4개 값 재처리 전후 완전 동일(멱등). 보존 경계 정리: Upbit 체결 7일 밖은 Kafka·MySQL·거래소 REST 전부 같은 경계라 백업 복원뿐, 호가 Parquet 120일(09-17 아카이브 16,333,731행 = 당시 표와 일치 검증)
- 09-20 05:32~05:40 UTC **보강 #7 완료: 죽은 산출물 정리**(docs/35 데이터 인벤토리, docs/34 §7-실행). 표 8개 DROP(48.1 MiB, 전부 참조 0 확인 후). **죽은 표보다 죽은 참조가 위험했다** — `sync-annotations.sh` 가 매분 cron 으로 09-17 이후 빈 결과를 돌고 있었고 `collect_metrics.sh` 알림 4열은 계속 0, Grafana 주석도 죽은 표 조회 → 살아 있는 `market_alerts` 로 교체·동작 확인(24h 149전이). **n8n 알림 워크플로 3개가 전부 비활성**임을 발견 → README 의 "n8n 매분 실시간 알림" 이 09-17 이후 사실이 아니었다, 문서를 사실과 맞추고 알림은 Airflow 일원화(정의는 비밀값 마스킹해 보관). 한시 보관 4종(15.8 GiB)은 삭제 예정일을 표로 못 박음(09-25·26·27) — "나중에 지우자"가 안 지켜진다는 걸 오늘 6종이 증명
- 09-20 05:42~06:05 UTC **보강 #8 완료: 테스트·계약**(docs/34 §8-실행). 테스트 0 이던 모델 9개에 테스트 부여(열 테스트 보유 13→22, **데이터 테스트 68개 전부 PASS**, 모델 24/24 커버), `assert_repairs_daily_grain` 단일 테스트로 복합키 입도 계약(dbt 기본 unique 는 단일 열만), exposure 2→7, **docs/36 지표 사전**(수식·단위·산출 위치·함정: taker_side 방향·김프 분모·e2e 지연 여섯 타임스탬프), **schemas/ 토픽 계약 5종 + `scripts/ops/validate-topic-schemas.py`**(의존성 없는 JSON Schema 부분집합 검증기). 계약이 필요한 이유 = Debezium `schemas.enable=false` 라 메시지에 스키마가 없고, 09-19 열 추가가 안 깨진 건 파서가 이름 기반이라 운이 좋았던 것. **검증 3단계**: ① `--self-test` 로 검증기가 위반 4종(필드 누락·타입 변경·허용값 밖·null)을 실제로 잡는지 먼저 확인 — 통과만 하는 검증기는 없느니만 못하다 ② 살아 있는 5개 토픽 표본 대조 전부 OK ③ dbt test 68/68. **발견 2**: (가) 드문 토픽(원장 주문)은 끝에서 읽으면 표본이 안 잡혀 조용히 건너뛰었다 → 비면 `--from-beginning` 재시도, 원장도 검증됨. (나) `assert_trades_unique_market_seq` 가 **단독 12초 통과 / 전체 실행에선 실패** — 원인은 쿼리 한도(600MB)가 아니라 **서버 총 메모리 1.57 GiB** 였다(하루 1,600만 행을 (market, sequential_id) 로 묶으면 그룹이 1,600만 개, Flink 적재와 겹치면 초과) → `max_bytes_before_external_group_by=300MB` 로 디스크 흘림, 실측 최대 1.13 GiB(실패) → 556 MiB(통과), 12.0s → 13.9s. **품질 테스트가 부하 때만 실패하면 거짓 경보 생성기가 된다**. 부수 발견: 로컬 `dbt test` 는 `DBT_LOG_PATH`·`DBT_TARGET_PATH` 를 옮겨야 한다 — logs/·target/ 파일 주인이 Airflow 컨테이너(uid 50000)라 47건이 ERROR 로 보였는데 전부 파일 권한이었다
- 09-20 06:06~06:20 UTC **보강 #9 완료: 마켓 상태 SCD**(docs/34 §9-실행). **계획이 틀려서 먼저 고쳤다** — 원래 계획은 "market/all + ticker 의 market_state·delisting_date 를 일 1회 스냅샷"이었는데, 코드 쓰기 전에 호출해 보니 `GET /v1/market/all?isDetails=true`·`/v1/ticker`·`/v1/ticker/all` 셋 다 그 필드가 없다. **웹소켓 ticker 에만 있다**(market_state·delisting_date·is_trading_suspended·market_warning). 같은 이름의 REST 와 웹소켓이 다른 필드 집합을 준다 — REST 만 보고 "없다"고 했으면 이 항목을 통째로 못 했다(부재 단정 교훈이 네 번째로 값을 한 자리). **첫 스냅샷에서 KRW-RVN(폐지 10-12)·KRW-ICX(폐지 10-19) 발견** — 10월에 마켓 289→287 이 되는데 기록이 없었으면 그날 "유실인가" 조사에 시간을 썼을 것. 만든 것: `upbit_market_state_events`(전이만, kind 에 snapshot/transition/**gone**=목록에서 사라진 순간), `poll_market_state.py`(10분 — 소비자인 커버리지 판정이 10분마다 돌기 때문), `scripts/lib/minws.py`(표준 라이브러리 웹소켓 클라이언트 — 호스트에 websockets 없고 apt 는 sudo 필요, 쓰는 기능이 셋뿐이라 의존성 대신 직접), `dim_market_state_scd`(SCD2, is_tradable 한 열이 하류가 보는 전부), health_check 커버리지 판정에서 거래불가 제외 + 알럿에 상태·폐지일 표기, 주간 다이제스트 ⑥ 마켓 상태. **PREDELISTING 을 거래가능으로 둔 이유**: 폐지일까지는 거래된다, 여기서 빼면 그 기간의 진짜 유실을 놓친다. **검증 5가지**: ① 직접 쓴 웹소켓 클라이언트 ↔ 라이브러리 결과 289 마켓 전 필드 일치 ② 폴러 2회 연속 실행 0행(멱등) ③ `--dry-run` + 조작 상태 파일로 transition·gone 경로 확인(프로덕션 표 289 그대로, 가짜 행 없음) ④ 5행 가상 입력에 모델과 같은 윈도 연산 → 반복 접힘·정지 전이·구간 연결·is_current 하나 ⑤ 제외 분기를 `coverage_verdict()` 로 빼고 단위 테스트 6케이스(14/14) — 프로덕션에 거래불가 마켓이 0개라 안 타는 코드라서. dbt build 102 PASS. **알게 된 것**: `leadInFrame` 은 다음 행이 없으면 NULL 이 아니라 1970-01-01 을 준다 → 마지막 구간은 `next_at <= valid_from` 으로 잡아야 한다(④에서만 드러남)
- 09-20 06:20~06:30 UTC **보강 #10 완료: 백업 제외 목록·복원 리허설**(docs/34 §10-실행). **원래 계획을 실측으로 뒤집었다** — "Binance 표를 제외"가 아니라 기준은 "지금 복구에 쓸 수 있는 유일한 사본인가"다. 그 기준으로 전환 롤백본 4개(`crypto_trades_rmt`·`_v2`·`_float_bak`·`binance_trades_float_bak`, 11.7 GiB, 삭제 예정일 있음)를 제외하고, `binance_orderbook_raw`(23 MiB)는 Parquet 아카이브가 없어 **오히려 유지**했다. 09-20 증분이 8.7GB 였던 원인이 #5 Decimal 전환이 만든 롤백본이었다 → 같은 base 로 재생성 시 **8.7GB → 5.0GB(−43%), 25초**. 목록은 `BACKUP_EXCLUDE` 상수에 이유 주석과 함께, `_except()` 가 실재하는 표만 골라 넣는다(롤백본 삭제 뒤 백업이 깨지지 않게). **복원 리허설 스크립트화**(`scripts/ops/restore-rehearsal.sh`): 3표 복원 18초, 체결 117,273,698 = 백업 시각 원본과 정확히 일치, 09-19 FINAL 집계가 금액 합 소수 20자리까지 동일. 배운 것 3: (가) **데이터베이스째 복원 금지** — cdc_pipeline 에 Kafka 엔진 표 6개(ledger_*_queue)가 있어 복원본이 같은 컨슈머 그룹으로 프로덕션 원장 메시지를 가로챈다(백업 메타데이터에 실제 포함 확인) → 표 단위 복원 (나) pipeline 사용자는 ACCESS_DENIED — 복원은 관리자(default)로, 런북에 못 박음 (다) 복원본을 FINAL 없이 읽어 행 수가 달라 보였다(3,489,987 vs 3,228,857) — 양쪽 다 FINAL 이면 일치. **곁가지 발견**: 가상 트레이더 STATS 가 4시간 멈춰 정지로 의심했으나 `MAX_ORDERS_PER_SYMBOL_PER_DAY=60` 상한 도달(5심볼 모두 정확히 60건)이었다 → 장애와 구분이 안 되는 것이 문제라 상한 도달 시 이유를 로그로 남기게 함(심볼당 하루 1회, UTC 자정 리셋), 유휴 구간에 재빌드·재기동, 테스트 8/8
- 09-20 06:55~07:10 UTC **2층 원장 주기·상한 개정 (docs/28 B)**: 사용자 질문 "심볼당 60건이면 파이프라인이 의미없는 거 아니야? 다 못 받아온단 거잖아" → **상한은 시세 수집과 무관하다**(Upbit 체결 3.23M/일·호가 20.1M/일, Binance 체결 7.76M/일·호가 227K/일, 전부 무제한, 거래소 캔들 대조 Upbit·Binance 모두 100%). 60건은 **우리가 직접 내는 주문**에만 걸린다. 원장은 양을 만드는 층이 아니라 **UPDATE·DELETE 전이를 만드는 층**이다(시세는 전부 INSERT뿐이라 CDC 의 본질인 상태 변경이 없다). **다만 지적의 절반은 맞았다** — 상한보다 소진 속도가 문제였다. 300초 주기로 심볼당 3건씩 내서 **100분 만에 하루치를 소진**하고 22시간 유휴. 양이 아니라 **커버리지**가 빈 것: 매시 3자 대조가 새 데이터 없이 돌고 CDC 경로 모니터링이 볼 트래픽이 없다. 이 원장은 합성 거래 카나리아이므로 실무 synthetic monitoring 처럼 **낮은 비율로 24시간 끊김 없이** 돌아야 한다. → `CYCLE_SEC` 300→**900(15분)**, `MAX_ORDERS_PER_SYMBOL_PER_DAY` 60→**288**. 288 은 임의값이 아니라 `3건 × (86400/900)` 의 결과이고 매시 4 사이클이라 빈 시간이 없다. **사전 확인(추측 아님)**: testnet exchangeInfo 실측 한도 ORDERS 160000/1DAY·50/10SECOND, REQUEST_WEIGHT 6000/1MINUTE → 주문 1,440건/일 = 일 한도의 **0.9%**; 잔고 9,919 USDT·심볼별 순포지션 드리프트 0 수준(최대 0.2 XRP, unwind 가 되돌림)이라 4.7배 물량도 포지션이 쌓이지 않는다. **함정**: 코드 기본값만 바꾸면 안 된다 — docker-compose 의 environment 가 덮고 있어 재빌드 후에도 60 이 그대로였다(로그가 "일 상한 도달 60/60" 을 찍어 잡았다). compose 도 같이 고침. **검증 end-to-end**: 재기동 즉시 주문 재개, 원장 토픽 1,314→1,343, ClickHouse 5분간 주문 14건·행 18개(1.29 버전/주문 = UPDATE 가 흐른다), 심볼별 60→63. 테스트 8/8
- 09-20 07:25~07:40 UTC **PRICE_24H v2.1.1 승격 (docs/22 §6)**: 기준(전이 10건 이상에서 1분봉 재계산과 미매칭 0)을 09-19 에 **116건 전부 일치**로 충족 → `rule_version` `v2.1.1-shadow` → **`v2.1.1`**, 발송 시작. **승격이 바꾸는 건 판정이 아니라 '사람에게 보내는가' 뿐** — 탐지기 테스트 9개 기대값이 버전 문자열 하나 빼고 전부 그대로 통과한 것이 증거(19/19). 발송은 새 DAG `market_alerts_notify`(10분, 창 30분 겹쳐 읽기, 승급만, dedup=(마켓·종류·등급·시)). 원래 계획인 n8n 대신 Airflow 인 이유는 n8n 3개가 전부 비활성이었기 때문(docs/35 §4). VOLUME_24H 는 `v2-shadow` 그대로라 자동 제외 — 발송 여부를 **데이터의 버전 문자열**로 판단하므로 09-23 판정 뒤 승격하면 DAG 수정 없이 보내기 시작한다. **성공처럼 보였던 실패 3건을 검증으로 잡았다**: ① 재배포 스크립트가 **옛 JAR 을 배포하고 성공이라고 찍었다** — `flink-cdc-redeploy.sh` 는 이미지 `cdc-flink-build` 에서 꺼내는데 정상 빌드 경로 `build-flink-job.sh` 는 `flink-cdc-builder` 라는 다른 이름으로 빌드하고 그 이미지를 지운다. 배포된 JAR 안의 문자열을 직접 열어 `v2.1.1-shadow` 인 것을 보고 잡음 → 재배포가 스스로 빌드하고 JAR 이 소스보다 오래되면 중단하는 가드 추가 ② **dedup 이 플래핑에 뚫렸다** — 첫 실행에서 KRW-ZIL 이 같은 키로 2건 발송(07:32·07:34 에 0→1 두 번). alert_events 조회만으로는 같은 실행 안의 중복을 못 막는다(적재가 맨 끝 1회) → 실행 내 seen 집합 추가, 재검증 후보 2·발송 1 ③ **DAG 4개가 만들어진 뒤 한 번도 돈 적 없었다** — `quality_alerts`·`weekly_digest`·`reconcile_binance`·`cases_hourly` 가 paused=True·실행 0건인데 문서엔 "매시 품질 판정"·"월요일 다이제스트"로 적혀 있었다(결과는 전부 수동 tasks test 였다). **n8n 비활성과 같은 유형 — 만든 것과 도는 것을 구분 안 함** → catchup=False 확인 후 5개 전부 활성화. 앞으로 "만들었다"는 스케줄 실행 이력으로만 말한다
- 09-20 07:45~08:05 UTC **계약 검증 상시화 · 거래소 공지 라벨링 (docs/37)**: 둘 다 docs/34 #8·docs/33 이 한계로 남긴 것. **① 토픽 계약 매시 검증** — 검증기는 Kafka CLI(docker exec)가 필요한데 모든 DAG 은 "네트워크 API 기반, 소켓 불필요"를 지키므로(docs/20), **호스트 cron 이 검증해 `schema_validation_runs` 에 적고 Airflow `quality_alerts` 가 그 표를 보고 판정**한다(알림은 한 경로로만). 보는 것 둘: 위반이 있나, 그리고 **검증기가 아직 도나**(180분 이상 기록 없으면 알림) — 검증기가 죽으면 위반이 0 으로 보이므로 "위반 없음"과 "검사 안 함"은 다르다. 규칙 SQL 첫 실행 500: `sum(violations) AS violations` 가 `argMax(detail, violations)` 를 집계 안의 집계로 만들었다(**별칭이 원본 열을 가리는** docs/34 #3 과 같은 함정) → `violation_count` 로 개명. 평소 통과만 하는 판정이라 **단위 테스트 6케이스로 증명**(정상 안 울림·위반 울림+메시지에 깨진 필드·정지 울림·경계 180/181). **② 공지 라벨링** — 공지 1,200건(15개월) 실측: `category='점검'` 은 **대부분 신분증·원화 입출금이라 거래를 안 멈춘다**. 거래가 멈추는 건 제목 `^서버 점검 안내`(전 마켓, 3건)와 `… 거래 지원 일시 중단`(코인별, 5건)뿐 — **카테고리로 걸렀다면 8건을 놓치고 멀쩡한 시간을 점검으로 오인**했을 것. 전체 점검은 본문 시각 중 최소=시작·최대=재개라는 규칙으로 창을 뽑았다(3건 전부 확인, KST−9h: 415·330·280분). 코인별은 본문에 재개 시각만 있는 경우가 있어 **창을 지어내지 않고 ±24h 후보로만** 둔다. 만든 것: `exchange_notices`(RMT — 공지는 사후 갱신된다), `poll_exchange_notices.py`(매시, 최초 1,200건 5분12초·증분 4.5초, 최근 15건은 갱신 위해 재수신), `dim_exchange_notices`, `dq_ingest_gaps_labeled`, 주간 다이제스트 ⑦. ClickHouse 는 JOIN ON 범위 조건 불가(403) → 공지가 8건뿐이라 배열 CROSS JOIN + arrayFilter. **검증**: 겹침 로직을 가상 공백 4개로 확인(한복판 ○·시작 걸침 ○·끝난 뒤 ✗·없는 날 ✗, 쓰기 없음) — 기계는 증명됐고 적용 사례는 아직 없다(우리 공백 8건이 전부 09-16 이후). 다이제스트 기준을 **60초 이상**으로 둔 이유: 공백 8건 중 6건이 재기동이 만든 수 초짜리이고 전부 자동 복구됐다, 그것까지 올리면 '고칠 것'이 늘 차서 아무도 안 본다(60초 = 늦은 이벤트 가드와 같은 기준) → 3건 남음. dbt build 111 PASS, DAG 테스트 15/15
- 09-20 08:10 UTC **방화벽 차단 목록 확장 (scripts/ops/lan-firewall.sh)**: 사용자 "방화벽은 무조건 닫는 게 맞잖아" → 실행 전에 `ss -ltnp` 로 **실제로 0.0.0.0 에 열린 것을 다시 세었다**. 기존 목록(9092·2181·8083·8081·8123) 밖에 이 저장소가 아닌 컨테이너 둘이 더 있었다: **`fds-redis` 6379 는 `redis-server --appendonly yes` 뿐 requirepass 가 없다**(LAN 의 누구나 읽고 쓸 수 있고 CONFIG SET 으로 파일을 쓰는 알려진 경로가 있다 — 이 목록에서 가장 위험), `my-postgres` 5432 는 비밀번호는 있으나 DB 포트를 LAN 에 둘 이유가 없다. 둘 다 이 프로젝트 것이 아니지만 같은 미니PC 이고 뚫리면 같은 호스트다 → PORTS 에 6379·5432 추가. 일부러 열어 두는 것: 22(SSH), 3000(Grafana 인증), 8085(Airflow UI 인증), 8089(circuit-connect-api 자체 인증). WireGuard 10.88.0.0/24 는 RETURN 이라 Oracle 접근은 유지. 확인 절차를 스크립트 출력에 넣었다(막혀야 할 5개 timeout / 살아야 할 2개 succeeded) + 재부팅 시 사라지므로 영구화 안내. **실행은 sudo 라 사용자 몫**
- 09-20 08:10~08:30 UTC **접근 통제 재점검 (docs/21 §5)**: 사용자 "또 다른 건?" → 방화벽을 실행하기 전에 실제 상태를 다시 셌고, **목록에 없던 더 큰 구멍**을 찾았다. **Grafana 가 익명 Viewer 를 허용하는데(`GF_AUTH_ANONYMOUS_ENABLED=true`) 그 데이터소스가 쓰기 권한 계정 `pipeline` 으로 돌고 있었다** — `POST /api/ds/query` 는 `rawSql` 을 그대로 실행하므로, 인증 없이 `SELECT count() FROM crypto_trades` 를 넣어 **117,460,641** 을 받아 실증했다. `pipeline` 권한에는 INSERT·ALTER·**DROP TABLE**·TRUNCATE 가 있다. **즉 포트 3000 은 "인증 있는 UI" 가 아니라 인증 없는 SQL 게이트웨이였고, 방화벽 계획은 그 전제로 3000 을 열어 두기로 하고 있었다.** docs/34 #1 이 이걸 못 본 이유는 **포트만 세고 그 뒤에 무엇이 있는지 안 봤기 때문**. 조치: 데이터소스를 `readonly_user`(SELECT 전용, system 읽기 GRANT 선행)로 내림 → 검증 익명 읽기 200 유지(대시보드 안 깨짐), 익명 쓰기 `code 497 Not enough privileges`·표 미생성. 익명 자체를 끌지·3000 도 막을지는 사용자 판단으로 남김(현재 = LAN 에서 로그인 없이 읽기만). **방화벽 목록도 부족했다**: `ss -ltnp` 재확인으로 `fds-redis` 6379(**requirepass 없음** — 가장 위험), `my-postgres` 5432 발견 → PORTS 에 추가. **`.env` 가 644(모든 DB 비밀번호가 누구나 읽기 가능) → 600 으로 조치.** 문제 없던 것: secrets/ 700·개인키 600, oci_key 600, git 이력에 비밀 파일 없음, 하드코딩 비밀번호 없음, **Docker 소켓 받은 컨테이너 0**(DAG 의 "네트워크 API 기반" 원칙 결과), privileged 0, SSH 키 전용, Airflow UI 로그인 강제·API 401, ops 프로필 꺼짐. 곁가지: `ClickHouse - Circuit Connect` 데이터소스는 `default` 로 붙으려다 code 516 으로 **죽어 있다**(다른 프로젝트 소유, 기록만). **배운 것: 포트를 세는 것과 그 뒤에 무엇이 있는지 보는 것은 다른 일이다** — 다음 점검은 포트 목록이 아니라 "인증 없이 무엇을 할 수 있나"를 실제로 해 보는 것으로 시작한다
- 09-20 09:10~09:30 UTC **방화벽 실행 + Kafka·ZooKeeper 포트 바인딩 (docs/21 §5-6)**: 사용자가 `lan-firewall.sh` 를 처음 돌리자 즉시 실패 — **09-19 에 쓰고 한 번도 실행한 적이 없었다.** ① `iptables: Port "9092,2181,..." does not resolve to anything` — conntrack `--ctorigdstport` 는 포트 하나만 받는데 multiport 와 같은 쉼표 변수를 넘기고 있었다 → 포트별 한 줄로. ② 고치면서 `-A`(끝에 추가)를 쓸 뻔했는데 **DOCKER-USER 의 마지막은 도커 기본 RETURN 이라 그 뒤 규칙은 평가조차 안 된다**(들어간 것처럼 보이고 아무것도 안 막음) → `-I DOCKER-USER 4`. 실행 전에 잡았고, 검증도 개수가 아니라 **자리까지** 보게 바꿈("들어갔다"와 "작동한다"는 다르다). 결과: 허용 3줄(도커·WireGuard·루프백) + DROP 7줄, 파이프라인 무영향. **그다음 포트 퍼블리시를 내렸다** — Kafka EXTERNAL 리스너가 `advertised=localhost:9092` 라 **다른 기기는 애초에 못 쓴다**(브로커가 localhost 로 오라고 답함), 즉 쓸모 없이 공격면만이었다. ZK 2181 은 호스트 사용처 0. `9092:9092` → `127.0.0.1:9092:9092`, 2181 퍼블리시 제거. **실측: 재생성 5.3초·브로커 복귀 47초, 정지 구간 MySQL 3,057 = ClickHouse 3,057 차이 0, 수집기 deliv_err·buf_err 0, Debezium 두 커넥터 FAILED 없이 자동 재연결(MySQL 재생성 때와 다름), Flink 5잡 체크포인트 재개, e2e p95 4.4s → 51s(한 분) → 4.9s.** 밀렸다 따라잡은 것이지 잃은 것이 아니다. 버퍼 근거: 큐 20만 건·timeout 10분 = Binance 9분치·호가 13분치. 남은 0.0.0.0: 22·3000·8085·8089·80/443, 그리고 8081(Flink)은 방화벽으로만 — 바인딩은 JM 재생성(잡 5개 재제출)이라 별도 창. **오늘 "안 돌려 본 런북" 이 세 번째**(잠든 DAG 4개, 옛 JAR 배포하던 재배포 스크립트, 이 방화벽 스크립트)
- 09-20 09:35 UTC **Redis 제거(사용자 실행)**: `fds-redis` 는 7개월 전 프로젝트 `fds-pipeline-lab` 의 잔재(같은 프로젝트 generator·consumer 는 7개월 전 종료, Redis 만 11일째 단독 가동, **키 0개·클라이언트 0개**)였다. 비밀번호를 거는 게 아니라 **지우는 것이 맞는 조치** — 아무도 안 쓰는 서비스에 인증을 붙이면 공격면은 그대로 두고 운영 부담만 는다. 6379 listen 사라짐 확인. 볼륨은 남아 되살리기 가능. **현재 0.0.0.0 잔여**: 22(SSH 키 전용)·80/443(웹)·3000(Grafana, 익명은 읽기 전용으로 내림)·8085(Airflow UI 인증)·8089(타 프로젝트 API)·5432(타 프로젝트 DB, 방화벽으로만)·8081(Flink, 방화벽으로만 — 바인딩은 JM 재생성 필요)
- 09-20 09:45 UTC **Grafana 익명 범위 실측 확인 (docs/21 §5-8)**: 사용자 "다른 사람은 뷰잉만 되는 거 아냐?" → 쓰기는 맞게 막혔고(INSERT·CREATE TABLE 모두 code 497), **'뷰잉'의 범위가 대시보드보다 넓다**는 점을 확인. 익명은 `cdc_pipeline`·`circuit_connect`·`system` 세 DB 에 **임의 SELECT** 가 된다. 자격증명은 안 샌다(`system.users.auth_params` 전부 `{}` 마스킹 — 평문 비밀번호인 default 도 값 안 보임). `system.query_log` 는 읽히지만 비밀번호 패턴 0건. **system 권한은 회수하면 안 된다** — 대시보드가 24시간에 587회 조회 중. 민감도: 시세는 공개, 원장은 테스트넷, 다만 `circuit_connect.dim_users` 780행에 nickname 존재(이메일·전화 없음). 파이프라인 관점 이상 없음, 남는 판단은 "게임 닉네임 780건을 LAN 에서 로그인 없이 읽혀도 되나" 하나 → 좁히려면 익명 끄기 / 3000 방화벽 추가 / 데이터소스 분리 중 택1
- 09-20 10:00~10:20 UTC **Grafana 인터넷 공개 발견·차단 (docs/21 §5-9~5-11)**: 앞에서 "익명은 LAN 범위"라고 적은 것이 **틀렸다.** 포트 3000 은 공유기가 막고 있었지만(셀룰러 테스트 무응답) **Caddy 가 `grafana.calmee.store` 를 basic_auth 없이 3000 으로 넘기고 있었다**(airflow·code 블록에는 있는데 grafana 블록에만 없었다). 실측: `https://grafana.calmee.store/api/search` 200, `/api/ds/query` 로 **인증 없이 SQL 실행 200**. 즉 §5-1 의 "LAN 에 열린 SQL 게이트웨이"는 실제로는 **인터넷에 열린** 것이었고, 오늘 오전까지 그 경로의 계정은 DROP TABLE 권한이 있는 `pipeline` 이었다. **교훈: 포트만 세면 놓친다 — 점검 순서는 ss → iptables → 웹서버(리버스 프록시) 설정 → 각 서비스 인증.** 사용자 결정("Grafana 는 밖에 안 보여주는 게 일반적이면 그 규율을 따르겠다, 포폴은 캡처로 충분") → `GF_AUTH_ANONYMOUS_ENABLED=false`, 데이터소스는 `readonly_user` 유지. 검증: 인터넷 `/api/search` **401**, `/api/ds/query` **401**, 로그인 후 200. 중간에 만든 `grafana_public` 계정은 공개를 닫으면서 **삭제**(안 쓰는 계정도 공격면). 남은 단계는 Caddy grafana 블록에 basic_auth(사용자 sudo) — 지금은 Grafana 설정 하나가 유일한 잠금. **곁가지 2건**: ① `scripts/sync-annotations.sh` 에 관리자 비밀번호 하드코딩(`-u admin:cdc_grafana_2025`, 공개 저장소) — 옛 값이라 유출 피해는 없었으나 **그 말은 이 cron 이 매분 403 으로 실패 중이었다는 뜻**. #7 에서 "죽은 참조"라며 고쳤는데 인증까지는 안 봤다(고쳤다고 믿고 결과를 안 봄) → .env 에서 읽도록 수정 + 실패를 버리던 `>/dev/null` 을 상태코드 출력으로 교체, 현재 비밀번호 POST 200·옛 것 403 확인 ② **내 1차 비밀값 스캔이 이걸 놓쳤다** — 정규식이 `password=`·`key:` 꼴만 보고 `-u user:pass` 형태를 안 봤다. 스캔은 패턴이 아니라 자격증명이 들어갈 수 있는 모든 형태를 생각해야 한다
- 09-20 10:25 UTC **결정 수정: Grafana 를 닫는 대신 문을 단다 (docs/21 §5-12)**: 사용자 "진입 자체에 암호를 거는 건? 결국 누가 궁금해하면 보여주긴 해야 하는데" → 맞다. 완전히 닫으면 못 보여주고 열어 두면 아무나 본다. 이미 airflow·code 도메인에 쓰는 방식이라 일관된다. 3층 구조: ① Caddy basic_auth(`scripts/ops/caddy-grafana-auth.sh`, sudo — 백업·`caddy validate`·실패 시 자동 롤백 포함) ② Grafana 익명 Viewer(문 통과자는 두 번 로그인 안 함) ③ **데이터소스 계정 `grafana_public` = 보여주기로 한 범위 그 자체**(cdc_pipeline 읽기 + 대시보드가 24시간 실측으로 쓰는 system 표 6개만, 쓰기 없음, circuit_connect 게임 닉네임 780행 없음). ①②는 '누가 보는가', ③은 '무엇까지 보이는가'를 정한다 — 문을 통과해도 `/api/ds/query` 로 임의 SELECT 가 되므로 계정이 곧 공개 범위다. **`readonly=1` 은 주지 않는다** — 그렇게 만들었더니 Grafana ClickHouse 플러그인이 세션 설정을 걸다 500. 쓰기 차단은 GRANT 로 충분(실측 cdc_pipeline 200 / circuit_connect code 497). **순서**: Caddy 문을 먼저 달고 그다음 익명을 켠다(반대로 하면 그 사이가 완전 공개) → 지금은 익명 off 로 대기
- 09-20 09:50~10:40 UTC **Grafana 데모 공개 완성 (docs/21 §5-13)**: Caddy basic_auth(`demo`) 적용 → 무인증 401·오답 401·정답 통과 확인. 그 뒤 Grafana 익명 Viewer 를 켜 **방문자가 비밀번호를 한 번만** 치게 함(순서 지킴 — 문 먼저, 익명 나중). 데이터소스는 `grafana_public`(cdc_pipeline + 대시보드가 24h 실측으로 쓰는 system 표 6개) → 게임 DB·쓰기 모두 **code 497** 거부 확인. **Grafana 11 OSS 함정 발견**: 폴더 권한에서 Viewer 를 빼도 **익명에게는 적용되지 않는다**(폴더 생성·이동·권한 수정·재시작까지 했는데 익명이 6개를 다 봤다). **대시보드 단위 권한(`/api/dashboards/id/{id}/permissions`)은 먹는다** — 같은 payload 로 즉시 403·검색 제외. 다른 프로젝트 대시보드 2개를 그렇게 숨겨 방문자에게는 4개(CDC 3 + Airflow)만 보이게 했다. 삭제가 아니라 권한이라 관리자에게는 그대로 보이고 정의도 `~/circuit-connect/grafana/` 에 남아 있다. **구조 한 줄**: 문(Caddy)=누가, 익명=편의, 계정=무엇까지. 남은 다듬기: grafana 블록에 `header_up -Authorization` 이 없어 데모 자격증명이 Grafana 까지 전달된다(airflow 블록엔 있다) — Grafana 에 demo 사용자가 없어 무시되므로 동작 문제는 없음
- 09-20 10:45~11:15 UTC **대시보드 정비 (docs/38)**: 사용자 "공개 데모가 됐으니 대시보드도 손봐야 하지 않나" → 전수 점검하니 **보여줄 내용이 바뀐 것보다 먼저 깨진 게 있었다. 쿼리 50개 중 13개 실패.** ① `cdc-dbt-analytics` 10패널이 아침부터 `Missing columns: 'trade_date'` 로 죽어 있었다 — docs/34 #3 에서 `trade_date→day_kst`·`hour_start→hour_kst` 로 바꾸며 소비자를 **저장소 grep** 으로 찾았는데 **UI 로 만든 대시보드는 Grafana DB 에만 있어 grep 에 안 걸렸다**(교훈: 열 이름 변경의 소비자 목록은 저장소가 아니라 '그 열을 읽는 모든 주체' — Grafana DB·cron·n8n 정의 등 저장소 밖 소비자가 있다) ② `airflow-operations` 12패널 **전부 빈 화면** — Prometheus 를 #1 에서 ops 프로필로 내려 평소 꺼져 있는데 대시보드만 안 옮겼다(대체재 ops_metrics_5m 는 docs/32 에서 이미 만들어 둠). **오늘만 네 번째 '만든 것과 도는 것' 유형**(잠든 DAG 4개·옛 JAR 배포·실행된 적 없는 방화벽 스크립트·이것). 고친 것: 메인은 승격 반영(섀도 제거) + 거래소별 유입·2층 원장 전이·원장 최근 주문 3패널 추가, 품질은 Binance 대조·원장 3자 대조·토픽 계약·공백 라벨 4패널 추가, DBT 는 죽은 표 참조 3패널 교체 + 열 이름 10곳 정정, 운영은 ClickHouse 기반 12패널로 재작성. **대시보드를 만들다 진짜 버그를 잡았다**: 원장 3자 대조가 **열린 날엔 구조적으로 항상 불일치**였다(오늘 5심볼 전부, 실제 데이터는 MySQL 479=ClickHouse 479 로 멀쩡). 원인은 두 값의 **측정 시각 차이** — my_* 는 매시 스냅샷, ch_* 는 dbt 실행 시점 현재값. 고침: 자르는 기준이 열마다 다르다(주문 수=created_ms, 상태 집계=updated_ms, 체결=filled_ms) → 1차로 주문 수만 맞춰 5→2, 남은 2건은 기준시각 전 생성·이후 체결(BTC·SOL 각 1건)이라 상태 집계까지 자르니 **0**. **검증: 방문자 경로로 50개 쿼리 전부 실행 → 실패 0, 빈 결과 0**, 3자 대조 두 날 모두 0
- 09-20 11:15~11:40 UTC **대시보드 2개로 재편 (docs/38 §5)**: 사용자 "파이프라인 관측·오류 탐지 1개, 이상탐지 1개로 나누는 건 어때? 근데 전자랑 airflow 랑 겹치나?" → **겹쳤다.** 주제별로 세어 보니 유입 3패널(메인2+운영1), 지연 6패널(메인2+품질1+운영3), 이상탐지 산출물 7패널(4개 대시보드 전부), 시장 데이터 14패널(메인4+DBT10)이 흩어져 있었다. 원인은 대시보드가 **만들어진 경위**(원본/dbt/운영)로 나뉜 것 — 보는 사람은 경위를 모르고 "파이프라인이 멀쩡한가 / 시장에 뭐가 터졌나" 두 질문으로 찾는다. → **파이프라인 관측·오류 탐지(23패널, 6섹션: 지금상태·유입·지연·정확성·오류공백·자원알럿)**, **이상탐지 시장경보(18패널, 5섹션: 지정상태·전이·규칙평가·급등·마켓상태+시장맥락)** 둘로 재편. 지연을 종단·적재·CDC 구간 셋으로 나눈 이유는 구간을 나눠야 병목을 말할 수 있기 때문(docs/08 교훈). **패널은 옮긴 것이지 다시 쓴 게 아니다** — 검증된 SQL 을 그대로 들고 자리만 새로 줬다(재작성하면 오타로 새 버그가 난다). 내려간 정의 3개(quality·operations·dbt-analytics)는 `grafana/archive/` 에 '어디로 갔는지' 표와 함께 보관, 복구 절차도 적음. **검증: 방문자 경로로 쿼리 41개 전부 실행 → 실패 0, 빈 결과 0.** 방문자에게 보이는 건 정확히 이 2개
- 09-20 11:40~11:50 UTC **대시보드 새로고침 복구 + Airflow 실무 점검 (docs/39)**: 사용자 "실시간으로 반영되는 거 맞아? 이전 코인 대시보드는 실시간이었는데" → **내 회귀였다.** 재편하면서 refresh 를 10s → 1m 으로 바꿔 버렸다(원래 값 확인: git 이전 커밋의 `refresh: 10s`, `time: now-1h`) → 파이프라인 10s·now-1h 로 복구, 이상탐지는 분 종가 판정이라 30s·7d. 실측으로 데이터가 계속 바뀌는 것 확인. **Airflow 점검(DAG 10개, 설정·의존성·날짜 파라미터·재시도·모니터링)**: 잘 된 것 — 실패 콜백 10/10, max_active_runs 10/10, catchup=False, 대조·백업이 `{{ ds }}`·data_interval 사용(과거 재실행 가능), upbit_rest 풀, **dbt source freshness 를 첫 단계로**(소스가 멈춘 채 마트를 만들어 '정상 완료'로 위장하는 것 차단), quality_gate, DAG 테스트 15개. 부족한 것 7가지 — ① **dbt 가 날짜 파라미터를 안 받아 백필 불가**(증분 모델이 전부 `now()` 기준, daily_pipeline 도 `dbt run` 을 변수 없이 호출 → "9/12 마트만 다시 만들어라"에 답이 없다. 대조·백업은 ds 를 쓰는데 dbt 만 안 써서 한 파이프라인 안에서 재실행 가능 여부가 갈린다) ② **Dataset 선언만 하고 소비자 없음**(DBT_COMPLETED 를 dbt_test 가 생산하는데 아무도 구독 안 함 → quality_alerts 가 매시 :50 시간 추측으로 돌아 dbt 가 늦으면 옛 데이터로 판정) ③ **호스트 cron 9개의 정지를 알아채는 장치가 계약 검증기 하나뿐** ④ dbt_run 단일 태스크(모델 24개 한 덩어리) ⑤ SLA 가 daily_pipeline 한 곳뿐(실패는 알리고 **지연은 안 알림**) ⑥ 재시도 백오프 거의 없음 ⑦ depends_on_past 없음. **3단계 계획 수립**(1단계 Dataset 연결·cron 신선도 판정·SLA·백오프 / 2단계 dbt 날짜 파라미터화+depends_on_past / 3단계 dbt 태스크 분할)
- 09-20 12:00 UTC **dbt 점검 (docs/40)**: 사용자 "dbt 자체 개선사항? DE 관점 + 데이터 넘겨받는 쪽 관점에서 꼼꼼히" → 모델 27·소스 22·테스트 80을 실제 데이터까지 확인하며 점검. **잘 된 것**: 모델 설명 27/27, **소스 freshness 22/22**, 테스트 레이어 고루, 시간·타입 규약 통일, RMT FINAL 규칙, exposure 7. **문제 10가지, 최상위는 staging 우회** — `stg_trades` 를 쓰는 모델이 2개뿐이고 **9개가 원본을 직접 읽으며 필터가 제각각**(op='c' 적용 6개, 무필터 3개). 게다가 **`op='c'` 자체가 틀렸다**: op 는 c(117.66M)·r(23,100, 02-13 최초 스냅샷)·backfill(7,288, 06-27 gap-fill)·d(3) 네 값인데, 실측으로 **r·backfill 모두 c 와 trade_id 가 한 건도 안 겹친다**(두 창 모두 중복 0) = 그 행에만 있는 진짜 체결. op='c' 로 자르면 **06-27 −2.77%(262,971→255,683), 02-13 −5.6%** 가 조용히 사라진다. 대조 표가 09-09 부터라 **지금까지 아무 테스트에도 안 걸렸다**. 올바른 술어는 `op != 'd'` 이고 한 곳(staging)에서만 정의해야 한다. 나머지: ② **컬럼 설명 17/71**(하류가 받는 건 컬럼인데 docs/36 사전이 dbt 밖이라 카탈로그에 안 뜸) ③ 입도 명시 2/27 ④ **contract 미사용**(오늘 trade_date→day_kst 가 대시보드 10패널을 죽인 것이 정확히 이 부재) ⑤ persist_docs 꺼짐 ⑥ 죽은 모델 stg_binance_trades(오늘 만들고 테스트까지 붙였는데 하류 0) ⑦ exposure 가 11모델 미커버(int_ohlcv_1h 는 대시보드가 쓰는데 dbt 는 모름) ⑧ dimensions·signals 레이어 미선언(config 없는 모델은 조용히 view) ⑨ 매크로 1개, 공통 필터 9곳 복붙 ⑩ 증분이 now() 기준이라 백필 불가(docs/39 ① 과 같은 뿌리). **3단계 계획**: 1단계 정합성(술어 정정+9모델 ref 전환+매크로, 검증은 행수가 아니라 합계) → 2단계 계약·정의(컬럼 설명 71개·입도 한 줄·contract enforced·persist_docs) → 3단계 운영(exposure·레이어 선언·증분 날짜 파라미터화는 Airflow 2단계와 동시). **순서 근거: 값 → 전달 → 운영. 뒤집으면 잘 문서화된 틀린 숫자를 배포하게 된다**
- 09-20 12:20 UTC **"Airflow 를 쓰는 이유" 5기준 대조 (docs/39 §4)**: 사용자가 제시한 일반 기준(코드화 / 의존성·조건부 / 복구·멱등성 / 생태계·확장성 / 모니터링)을 실제 코드와 대조. **되어 있는 것**: Git·CI 3종(Flink 단위·DAG 구조·dbt parse), **동적 태스크 생성은 이미 사용 중**(daily_pipeline 이 `.partial().expand()` 로 코인 수만큼 검증 태스크 생성), 커스텀 Operator 2·Hook 1·공용 콜백, 재시도 10/10, UI·Gantt·로그, 자격증명은 Airflow Connection/Variable 로 올바르게 관리. **가장 약한 축은 조건부 제어**: 분기 연산자 0, **센서 0**, Dataset 선언만, TriggerRule 1곳. **그리고 자가 정정 — `quality_gate` 는 하류를 막지 않는다**(`ti.log.warning` 후 dict 반환, 예외 없음). 앞 문서에서 "막는다"고 적은 것이 틀렸다 → 지금은 검증 실패 코인이 있어도 리포트가 Slack 으로 나간다. **불필요로 확정한 것도 기록**: Celery/Kubernetes Executor(1호스트 16GB·동시 태스크 8에서는 관리 비용만 증가), TaskGroup(DAG당 태스크 3~8개라 아직), 외부 스토리지 센서(S3 등이 없다) — 남의 규모를 흉내 내지 않기 위해 '불필요'를 명시적으로 남긴다. **새로 나온 항목 4개**: ⑧ quality_gate 를 실제 게이트로(AirflowFailException/ShortCircuit) ⑨ 대조 DAG 에 캔들 존재 센서(시간 오프셋 추측 → 사실 확인) ⑩ Airflow 로그 3.4GB 보존 정책 없음 ⑪ Slack Provider 검토
- 09-20 12:35 UTC **⑨ 캔들 센서 철회 (docs/39 §4-2)**: 사용자 "파이프라인 헬스체크 관점에서 정말 필요한가? 객관적으로 재검토" → **필요 없다.** 근거 4: ① 대조 DAG 은 남의 적재를 기다리는 게 아니라 **스스로 REST 로 캔들을 받아오고 한 마켓이라도 실패하면 RuntimeError** — 이미 하드 게이트가 있다 ② 대상일은 실행 6.5h 전에 닫히고 표에 **미래 시각 캔들 0건**(부분 캔들 유입 이력 없음) ③ 재적재 중복은 `upbit_hourly_candles` 가 RMT(market, hour_utc)이고 대조 모델이 FINAL 로 읽어 이미 접힌다(09-15 실제로 2회 적재·6,852 중복인데 결과 정상) ④ 대조 11일 전부 100%. **센서는 '남이 만드는 데이터를 기다릴 때' 쓰는 도구인데 우리는 생산자를 직접 소유한다** → 넣으면 기다리는 시늉만 하고 워커 슬롯만 먹는다. 부수 확인: 캔들이 마켓당 23.5~23.9개인 것은 정상(체결 없는 시간엔 캔들이 없다) — "24개여야 한다" 테스트를 넣었으면 매일 거짓 경보(docs/34 #3 no_long_gaps 와 같은 함정). 센서가 의미 있어지는 조건은 캔들 적재를 별도 DAG·외부 적재로 분리할 때
- 09-20 12:45~13:05 UTC **dbt 1단계 완료: 정합성 (docs/40 §4)**: 술어를 `real_trade_filter()` 매크로 하나로 통일(`op='c'` → **`op != 'd'`**)하고 **원본 직접 읽는 모델 9개 → 0개**(staging 2개만 원본을 본다). **검증**: 06-27 10~12시 창에서 stg_trades 13,984행·금액 9,707,754,870.79349754 가 원본 `op != 'd'` 와 **소수점까지 일치**, 옛 술어는 6,696행/4,665,969,699.95 — **그 창에서 체결의 52% 를 버리고 있었다.** **고쳐진 값의 델타가 원인과 정확히 일치**: int_ohlcv_daily 2026-02-13 186,388→209,488(**+23,100** = op='r' 행 수), 2026-06-27 257,669→264,957(**+7,288** = op='backfill' 행 수) → 늘어난 게 중복이 아니라 빠져 있던 진짜 체결임이 증명됨. **회귀 없음**: 9월엔 r·backfill 이 없으므로 같아야 하는데 동등성 09-18(70/68/66)·09-19(116/116/116/parity 1)·Upbit 대조 100%·Binance 대조 100%/491셀 전부 그대로, dq_ingest_daily 는 stg 직접 집계와 행 수 완전 일치. dbt build **111 PASS**. **걸린 것 3**: ① 주석 안에서 매크로를 호출해 SQL 이 깨짐(개행 때문에 마침표가 독립 줄로) → `{%- -%}` 공백 제어 + 주석에선 호출 금지 ② **모델이 staging 을 우회한 진짜 이유는 열 부족**이었다(source_ts·op·UTC 축·최우선 호가가 없었다). 뷰라 열 추가 비용이 0인데 안 하고 있었다 ③ **이름 충돌 선제 제거** — staging 의 체결 기준 하루를 `day_utc` 로 두면 하류의 `toDate(source_ts) AS day_utc`(적재 기준)와 겹쳐 '별칭이 원본 열을 가리는' 함정에 들어간다 → `trade_day_utc`·`trade_hour_utc` 로 명명, 이름만으로 체결/적재 기준이 구분된다. **새 발견**: `int_volume_surge_daily` 가 거래대금 하루를 binlog 시각(`toDate(source_ts)`)으로 자른다 — 급등 규칙은 체결 시각 기준이어야 한다(평소엔 차이 미미하나 적재 지연 사고 때 엉뚱한 날에 잡힌다). **한 번에 한 가지만 바꾼다**는 원칙에 따라 이번엔 술어만 고치고 별도 항목으로 남김
- 09-20 13:05~13:20 UTC **dbt 2단계 완료: 계약과 정의 (docs/40 §5)**: **컬럼 설명 17/71 → 314/314**(선언 자체를 314개로 확대), **입도 명시 2/27 → 27/27**(모든 모델 설명이 `한 행 = …` 로 시작), **contract: enforced 0 → 9**(마트2·신호2·차원5), **persist_docs 켬**(설명이 ClickHouse 컬럼 주석으로 내려가 BI 에서 보인다 — 넘겨받는 쪽은 dbt docs 가 아니라 SQL 클라이언트에서 컬럼을 본다), `dimensions`·`signals` 레이어 기본값 선언(⑧, 없으면 config 빠뜨린 모델이 조용히 view 가 된다). **계약이 실제로 막는지 증명**: `mart_daily_summary.close_change_pct` 를 일부러 `close_chg_pct` 로 바꾸니 `This model has an enforced contract that failed` 로 빌드 중단 — **오늘 대시보드 10패널을 죽인 것과 같은 유형의 변경이 이제 빌드에서 멈춘다.** 증분 모델은 `on_schema_change=fail` 로 뒀다(append_new_columns 는 '조용히 늘린다'는 뜻이라 계약 취지에 반함). **하다가 찾은 것 4**: ① **같은 모델을 선언한 yml 이 3개**(schema/reconcile/volume_surge) — 내 설명이 반영 안 돼 알아챔, 하나로 합침 ② `reconcile.yml` 설명이 **없어진 열 `stg_trades.trade_date`** 를 가리키고 있었다(문서가 코드보다 오래 산 사례) ③ `stg_trades` 설명의 "2026-02 재소비 중복 ~15.4%, dedup 필요" 경고가 **이미 해소된 문제**였다 — 실측 2026-02-14 00~06시가 FINAL 유무와 무관하게 127,445행 = 고유키 127,445개(RMT 전환으로 해결) → 문구를 실측으로 갱신 ④ **`dim_coins` 조건부 유일성 검사를 내가 덮어써서 깨뜨렸다** — `binance_symbol` 이 바이낸스 미상장 코인에서 빈 문자열이라 82건 중복, 원래 있던 `where: binance_symbol != ''` 를 날렸다가 복원하고 빈 값의 의미를 주석으로 못 박음. dbt build **110 PASS**
- 09-20 13:20 UTC **dbt 3단계(일부): exposure·레이어 (docs/40 §6)**: 재편한 대시보드 2개를 exposure 로 등록(7→9), 덮는 모델 16→19. **소비자를 모르는 모델 0/27** — 나머지 8개는 다른 모델이 쓰는 내부 모델이라 dbt 가 이미 안다. exposure 는 문서가 아니라 **삭제 방지 장치**다: 등록 전에는 `int_ohlcv_1h`·`int_volume_surge_daily` 가 '하류 없음'으로 보여 죽은 모델로 오해할 수 있었는데 실제로는 이상탐지 대시보드가 쓰고 있었다. 남은 ⑩(증분 날짜 파라미터화)은 Airflow 백필과 뿌리가 같아 함께 진행
- 09-20 13:25~13:55 UTC **Airflow 1단계 + 단계별 사건 이력 (docs/41)**: ⑧ **quality_gate 를 진짜 게이트로** — 전에는 log.warning 후 dict 반환이라 실패 코인이 있어도 리포트가 Slack 으로 나갔다 → `AirflowFailException`. 임계를 0건이 아니라 **90%** 로 둔 이유는 얇은 마켓 한두 개가 순간적으로 조건을 못 맞추는 일이 늘 있고 그때마다 막으면 **사람이 게이트를 꺼 버리기** 때문(중복 적재는 0건 아니면 무조건 차단 — 정도의 문제가 아니라 사고). ② Dataset 구독으로 바꿨더니 **매시 실행이 사라졌다** — `DatasetOrTimeSchedule` 은 2.9+ 인데 **우리는 2.8.1**, 매시를 잃으면 dbt 안 도는 시간대의 cron 신선도·계약 검증을 놓친다 → 매시 유지 + `daily_pipeline` 이 dbt_test 직후 `TriggerDagRunOperator` 로 명시 호출(추측이 사실로). ③ **호스트 cron 신선도 판정** 추가 — 허용 지연을 주기로만 정하면 안 된다: `upbit_market_state_events` 는 **전이가 있을 때만 쓰는 표**라 조용한 게 정상(실측 312분 무기록)이어서 1500분을 줬다, 30분을 걸었으면 매일 거짓 경보. ⑤ SLA 4개(대조·백업 2h, 품질 20m, 발송 5m) — **실패는 알리는데 지연은 안 알리고 있었다.** ⑥ 외부 API DAG 4개에 지수 백오프. ⑩ **로그 3.4GB → 688MB**(`log_retention` DAG, 주1회 30일 보존, 70,888 파일 삭제) — 호스트 cron 이 아니라 DAG 으로 둔 이유는 로그가 Airflow 자신의 산출물이고 cron 으로 두면 ③ 문제를 키우기 때문. 첫 실행 `errors 58,699` 인데 30일 초과 파일은 0개 남아(삭제는 전부 성공) **숫자만 있고 이유가 없으면 무시되거나 겁먹게 되므로** 오류를 종류별로 세도록 고침 → 두 번째 실행 errors 0. ⑪ **Slack Provider 도입 안 함** — 웹훅은 이미 Airflow Variable 에 있고(하드코딩 아님) 지금 겪는 문제가 아니라 의존성 추가 비용이 이득보다 크다, 2.9 업그레이드 때 재검토. **새 항목: `pipeline_incidents`** — Airflow 는 자기가 실행하는 태스크만 보여 주고 실시간 경로는 컨테이너가 돌려서 "어느 단계가 왜 깨졌나"를 한 곳에서 못 봤다(이력이 alert_events·ingest_repairs·DLQ·Flink 메트릭으로 조각나 있었다) → 단계 축 표 신설(180일), health_check 가 alert_events 와 함께 적재, 알럿 이름→단계 매핑(**모르는 이름은 unknown, 아는 척하지 않는다**), 대시보드 ⑦ 섹션 2패널. 알럿이 안 울리면 안 도는 코드라 **단위 테스트로 6단계 커버·접두사 매칭·unknown 을 증명**. DAG 테스트 17/17
- 09-20 13:40 UTC **작업 중 실수 기록: 일괄 편집이 DAG 2개를 깨뜨렸다 (docs/41)**: SLA 를 네 DAG 에 정규식으로 한 번에 넣었는데 `quality_alerts`·`market_alerts_notify` 는 **한 줄짜리 연산자**(`PythonOperator(task_id="…", python_callable=…)`)여서 주석이 나머지 인자를 삼켰다 → `SyntaxError: '(' was never closed`, DAG 2개 사망·테스트 4개 실패. **같은 패턴이 두 형태로 존재하는 파일을 정규식으로 일괄 편집하면 이렇게 된다.** 손으로 여러 줄 형태로 고치고 **전 DAG 문법 검사 + 테스트 17/17** 로 확인. 교훈: 일괄 편집 뒤에는 전수 문법 검사를 반드시 한다(docs/30 '검증 전 커밋' 과 같은 부류)
- 09-20 13:55~14:10 UTC **Airflow 2단계 완료: 백필 (docs/41 §3)**: 증분 모델이 창을 `now()` 로 잘라 **과거 날짜를 다시 만들 수 없었다**(대조·백업은 `{{ ds }}` 를 쓰는데 dbt 만 안 써서 한 파이프라인 안에서 재실행 가능 여부가 갈렸다) → `macros/run_window.sql` 의 `run_anchor()` 도입, **기본값이 now() 라 평소 동작은 그대로**이고 `--vars '{"run_date": "YYYY-MM-DD"}'` 를 주면 그 날만. 백필 모드에서는 **상한도 건다**(`< 기준일+1일`) — 상한이 없으면 백필이 오늘까지 쓸어버린다. `daily_pipeline` 의 dbt run/test 가 `{{ ds }}` 를 넘기도록 연결(렌더링 확인: `--vars '{"run_date": "2026-09-19"}'`). **검증**: `dq_ingest_daily` 를 09-16 으로 백필 후 `stg_trades` 직접 집계와 대조 — 09-15 2,459,434행/286마켓, 09-16 2,027,655/287 **완전 일치**, 창 밖인 09-17 은 **건드리지 않음**, 소요 17초. 평소 모드 dbt build 110 PASS · 백필 모드 12 PASS. ⑦ `depends_on_past` 는 daily_pipeline 에만 — 증분은 전날 위에 쌓으므로 전날 실패 시 오늘이 돌면 구멍이 남는다, 다만 **health_check 같은 '지금 상태' DAG 에는 걸지 않는다**(과거 실패가 현재 점검을 막으면 안 된다). 남은 것은 3단계 dbt 태스크 분할(관측성 개선, 없어도 값이 틀리지 않음) — 포트폴리오 뒤에
- 09-20 14:20 UTC **최종 점검 (docs/42)**: 포트폴리오 전 마지막 객관 점검. **규모** 6억 2,806만 행/21.9 GiB·표 60개, Flink 5잡·커넥터 2·수집기 4·DAG 11 활성·dbt 모델 27. **정확성** Upbit 대조 100%(최소 셀 100%·0행 셀 0), Binance 대조 100%(491셀), 원장 3자 대조 ex≠my 0·my≠ch 0, e2e p95 Upbit 4.64s·Binance 2.91s. **테스트 125개**(Flink 19·DAG 17·트레이더 8·dbt 81), 계약 9, exposure 9, 소스 freshness 22/22, 컬럼 설명 314/314, 입도 27/27. **약점도 그대로 기록**: 단일 호스트·단일 브로커(의도된 선택이나 이 기계가 죽으면 전부 멈춤), Kafka 인증 없음(방화벽·바인딩으로 대체), `int_volume_surge_daily` 가 binlog 시각으로 하루를 자름(오늘 발견, 별도 항목), dbt_run 단일 태스크, 분기·센서 미사용(센서는 철회가 맞음), SCD 첫 구간이 '적어도 그때부터', PREVIEW 미검증, Grafana 폴더 권한 익명 미적용(11 OSS 제약), 로그 정리가 주 1회. **오늘 34건의 문제를 원인·해결까지 기록**했고 그중 값이 틀렸던 3건(op='c' 술어가 진짜 체결 30,388행 버림·3자 대조가 열린 날 항상 불일치·CAST 버림으로 금액 합 2e-8 오차)은 **전부 행 수로는 통과하고 합계·델타로만 잡혔다** — '검증은 행 수가 아니라 값으로'가 규칙이 된 이유. **같은 실패 유형이 하루에 다섯 번**(잠든 DAG 4개·옛 JAR 배포·실행된 적 없는 방화벽 스크립트·빈 Prometheus 대시보드·옛 비밀번호로 403 나던 cron) = **만든 것 ≠ 도는 것** → 대응으로 검증기 `--self-test`, 대시보드 전 패널 쿼리 실행, 판정식 단위 테스트를 붙였다
- 09-20 14:30~15:00 UTC **Airflow 전용화 + 대시보드 가시성 개선 (docs/38 §6)**: 사용자 "에어플로우를 이 파이프라인 전용으로 못 빼? 다른 것들도 섞인 느낌" → **사실이었다.** `/home/calme/icepush/airflow/dags` 가 마운트돼 타 프로젝트 DAG 1개(`icepush_dbt_daily`, 실행 이력 121건)가 섞여 있었다. **이미 일시정지 상태**(마지막 예약 실행 09-05 실패, 09-06 수동 성공이 마지막)라 제거해도 기능 변화 없음 → 마운트 2줄 제거 + **복구 절차를 주석으로** 남김(실행 이력은 메타DB 에 그대로). 결과 DAG 11개, 전부 이 파이프라인 것. 별도 인스턴스는 웹서버+스케줄러+Postgres 1.5~2GB 라 16GB 에서 과함 — 한 대에서 자원을 배분한다는 것이 오히려 설명할 거리. **대시보드 가시성**: 사용자 "이전 게 더 가시성 좋았다, 지금은 난잡하고 즉각적으로 안 들어온다" → 보관본(2월판)과 비교해 **원인 확정: `fieldConfig` 가 통째로 비어 있었다**(2월판은 unit·thresholds·color.mode·options 가 다 있는데 내가 만든 건 `{}`) → 단위 없는 회색 숫자만 나와 `4.64` 가 초인지 밀리초인지, 정상인지 위험인지 알 수 없었다. **값은 맞는데 읽을 수 없는 상태.** 패널마다 단위·임계·색 적용, **임계는 알럿 임계와 동일하게**(화면의 빨강과 Slack 알림이 다른 기준이면 둘 중 하나를 안 믿게 된다). 둘째 원인은 첫 화면이 패널 25개·높이 70 이었던 것 — 사용자 "대시보드를 더 쪼갤까? 개수가 많아지나?" → **쪼개지 않고 접었다**(문제는 개수가 아니라 한 번에 보이는 양이고, 대시보드를 늘리면 '어느 대시보드였더라'가 새 비용). 첫 화면 **25→8패널·높이 70→14**, 대시보드는 2개 유지. 펼쳐 두는 건 '지금 정상인가'에 답하는 두 섹션뿐. 배치도 손봄(이상탐지 ④에서 요약 지표 4개를 가격 차트 위로 — 요약 먼저, 차트 나중이 읽는 순서, `Bid vs Ask` → `테이커 방향 비율`). **검증: 스타일 미적용 0개(접힌 행 안까지), 방문자 경로 쿼리 43개 전부 성공**
- 09-20 15:10~15:25 UTC **시각화 종류·문구 정리 (docs/38 §7)**: 사용자 "각 섹션에 맞는 그래프를 넣어야 하지 않나", "이상탐지 보드의 현재가는 뭐야", "하이픈(—)은 AI 티가 난다". **원칙 수립**: 시각화는 그 패널이 답하는 **질문의 모양**으로 정한다(지금 값 하나=stat 배경색 / 연속 변화=선 / 날짜별 건수=막대 / 항목 비교=가로 막대 / 개별 사건=표). **바꾼 것 5**: 대조 일치율 2개는 값이 늘 100 이라 선이 평평 → **stat + 30일 스파크라인**, 어긋난 셀 수·늦은 행은 이산값이라 **막대**, 단계별 사건 수는 크기 비교라 **가로 막대**, 분당 유입은 경로별 차이가 10배 넘어 선형 축이면 바이낸스가 나머지를 눌러 **로그 축 선**으로. **'현재가' 정체**: `mart_daily_summary` 의 최근 KST 하루 종가였고 **실시간 가격이 아니었다**. 경보 지표만 있는 ① 섹션에 가격 하나가 끼어 있어서 → **④ 선택한 종목 살펴보기로 옮기고 `선택 종목 최근 종가` 로 개명**, 설명에 "실시간 아님, 실시간은 ②의 차트" 명시. **문구**: 제목에서 엠대시 전부 제거, 영어·약어·내부용어를 풀었다(`host load1`→`서버 부하`, `Flink 소스 busy max`→`Flink 소스 포화도`, `Markets Tracked`→`감시 중인 마켓 수`, `BTC Price … 🔴 빨간 점선`→`비트코인 가격과 경보 시점`). **섹션 제목은 전부 질문 형태로**(지금 정상인가 / 어디서 밀리나 / 값이 맞나 / 무엇이 빠졌나 / 어느 단계가 왜 깨졌나) — 보는 사람은 지표 이름이 아니라 자기 질문으로 찾기 때문. 검증: 제목에 — 남은 것 0, 제목 중복 0, 방문자 경로 쿼리 43개 전부 성공
